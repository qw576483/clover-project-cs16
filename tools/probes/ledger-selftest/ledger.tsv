# dispatch ledger -- SAMPLE fixture for the gate self-test (judgement asset, slice BW-G 2026-09-23).
# It is NOT the real ledger and the self-test NEVER writes .ai-tmp/test/dispatch-log.tsv:
# the real one is shared mutable state across parallel slices, so the samples run on a
# throwaway COPY of this file through verify.ps1's -Ledger test seam.
# Shape mirrors the real ledger: four-column rows ABOVE the marker (historical residue that
# must not be back-filled, SKILL 1.5 item 8) and one six-column in-scope row BELOW it.
2026-09-01 10:00	selftest-sample	historical-row-one	tools/probes/**,.ai-tmp/**
2026-09-01 10:05	selftest-sample	historical-row-two	tools/probes/**,.ai-tmp/**
# team-member-required-below-this-line
# ^^^ every row BELOW this line must carry a team (col 5) and a member (col 6); rows above
#     this line predate the rule and are history -- they are never judged and never patched.
2026-09-02 09:00	clover-impl	selftest-sample-in-scope	tools/probes/**,.ai-tmp/**	cs16-fix	bw-gate
