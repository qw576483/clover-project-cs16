# agent-52 · 修 `BuildHeightReach` 高度层（A1/A2 的真因，前一片已改判）

## 背景：**连通性已修完，真因在这一层**（前一片 `astar-connectivity` 的结论，别重查）
- 可走位图**已是单连通**：分量 **53 → 1**、非主分量格 **939 → 0**（sha `f9837072…` → `0e06d2fe…`；B→W 88 / W→B 862；可走 5308→4534）。实机加载行 `阻挡=13881 = 13103+862−88` 证明新位图生效。
- **但 A1/A2 仍 2/8**（阈值 ≥6/8）。引擎侧当天读数：`终点不可走=146` / `无可达路径=160`。
- **真因**：运行时高度一致性层 `BotNavigator.BuildHeightReach`（本次扩张 **4400 格** / 高度判死 **162 格**）把"**落脚面比脚面高 > 0.45 m**"的目标格剔出可达图 ⇒ `walkable(to) = false` ⇒ 目标选择全 false ⇒ **退化成直线走**。
- 行为侧证据（净位移/总路径比）：`ZBot` 总路径 447.5 m / 净 35.8 m（**ratio 0.080**）、`Darrell` 247.4 / 30.4（0.123）⇒ 绕路极严重。
- ⛔ 业务日志里那句"位图不连通"是**误标**（位图已统一），别照它改。

## 目标
1. **搞清 `BuildHeightReach` 到底在判什么、阈值从哪来**：
   - `0.45 m` 这个门槛**有没有出处**？（原版 bot 判"这一步能不能走过去"的判据是什么 —— 台阶高度 / 落脚面高差 / 是否要查 `TryStepUp` 的实际上限）
   - 找得到出处 ⇒ 按出处改；找不到 ⇒ 说明它可能是本项目自定值 ⇒ **如实标注**并给出你改后行为改善的实测数字（⛔ 不许谎称原版）。
2. **修"目标格被剔出图"这件事**：目标是让 bot 在几何上真能走到的地方，`walkable` 判为真；走不到的地方（真高差/真墙）仍要剔。
   - ⛔ 不要用"把门槛放大到什么都通过"来凑数字 —— 那会让 bot 尝试走进它上不去的地方（第二个 bug）。
3. **收尾数字**：**A1/A2 ≥6/8**（⛔ 不许下调阈值）、A5 ≥1、A6/A7 不回退、且**净位移比明显改善**（当前 0.080 / 0.123）。

## 硬约束
1. ⛔ 不改引擎（前一片已确认**不需要**；若你得出的结论是"必须改引擎" ⇒ 先回报我 + 写因果链 + 记 `引擎问题.md` 三段）、⛔ 不改 skill。
2. 写入范围：`client/Assets/Scripts/Module/Bot/**`、`.ai-tmp/**`、`策划/差异登记.tsv` **第 68 行**。
   ⛔ 不写 `Module/Map/**`、`Module/Combat/**`、`Core/CsWeapons.cs`、`UI/**`、`Module/View/**`、`client/Assets/Editor/**`、`Resources/MapData/**`（位图已定稿，别动）。
3. **Play 是独占资源**：现在另一片 `fetch-more` 全程离线 ⇒ 基本只有你用。进前看 `.ai-tmp/test/play-log.tsv` 末行是否 `RELEASE` 且 `play-driver-log.tsv` 近 90 秒无新行；`OCCUPY`/`RELEASE` 配对、格式 `2026-09-25 HH:mm`。
   ⚠️ 已知：既有驱动 `round-play.ps1` 的 `Paths.State` 是**硬编码共用 `state.txt`**，且进局链路**缺"选兵种"一步** ⇒ 必须补 `Clk 'urban'`（前一片已这么做）。若你要用共用驱动，**照前一片的做法**（唯一 Play 使用者时不改它）。
4. **判据 = 一条命令 + 原始输出**（A1/A2/A5/A6/A7 五项 + 净位移比）+ 必要截图；⛔ 不填验收表、⛔ 不建覆盖矩阵。
5. 注释按 skill §3 第 3b 条：只写「现在是什么」——⛔ 不写批次 / 本轮 / 任务书 / 用户报的 / 日期 / 别的工程名。
6. 命令一律显式超时；超 12 分钟把进度落盘 + 给 `main` 一条回报（**第一行写模型标识**）。

## 回报（`send_message` 给 `main`）
① `BuildHeightReach` 语义 + `0.45 m` 门槛的出处（或如实标自定）；② 改了哪些文件 + 每处一句话；③ 前一片修的位图**未被回退**的证据（sha 一致）；④ A1/A2/A5/A6/A7 + 净位移比 前后数字；⑤ 没做到的逐条 BLOCKED。⛔ 不要长解释。
