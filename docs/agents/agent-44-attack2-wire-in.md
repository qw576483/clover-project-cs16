# agent-44 · 把武器右键数值接进代码（差异 #68 后半）

## 背景（数值已全部查清，可直接用）
- 前序成员已交付 **`策划/武器右键数值出处.md`（22,457 B）** —— 每条数值都带 `mp.dll` 的 VA + 文件偏移 + 字节 hex + 反汇编，**先完整读它**。
- 关键数值（该文档 §0 速查表，逐字节级出处）：
  - **USP** 伤害 未装消音 `34.0` / 装消音 `30.0`；射程修正 `0.79`（两态共用）；**散布两态相同**
  - **M4A1** 伤害 未装 `32.0` / 装 `33.0`；射程修正 未装 `0.97` / 装 `0.95`；静止站散布 `0.02×acc` / `0.025×acc`
  - **Glock18** 伤害 `25.0`（两态同）；连发 **3 发**；首/续发间隔 **0.1 s**；burst/semi 循环 `0.5 / 0.15`
  - **Famas** 伤害 全自动 `30.0` / 连发 `34.0`；射程修正 `0.96`；连发 **3 发**；首发 `0.05 s` / 续发 `0.1 s`；burst/semi `0.55 / 0.0825`
- 该文档 §6 已明确：**没有数据表 / 覆盖表**，全部是"每类字段 + 分支里的常量"。⇒ 接入 = 改代码分支，不是灌表。

## 目标
把这批数值接进工程，使「装消音后伤害/散布」「连发发数与节奏」在**代码里真的按原版数值走**。
- 落点自己找（`Core/CsWeapons.cs` 的能力表/武器参数、`Module/Combat/**` 的伤害与开火节奏），先读一遍现状再定。
- **优先复用既有结构**：若 `CsWeapons.cs` 已有 per-weapon 字段，就填数；缺分支就按原版语义补分支。
- ⛔ **不要把数值"看起来合理"地拍进代码** —— 每个改动点必须在注释或提交说明里写清它对应文档里哪一行（数值 + VA）。

## 硬约束
1. ⛔ 不改引擎源码（`client/Packages/com.clover.unity-engine/**`）、⛔ 不改 skill。
2. 写入范围：`client/Assets/Scripts/Core/**`、`client/Assets/Scripts/Module/Combat/**`、`.ai-tmp/**`、`策划/差异登记.tsv` **#68 行**。
   ⛔ 不写 `Module/Bot/**`、`Module/Net/**`、`Module/Map/**`、`Module/View/**`、`UI/**`、`client/Assets/Editor/**`（别的片在用）。
   ⛔ **不要动 `client/Assets/Editor/Views/ModelData/**`**（`modeldata-swap` 正在那附近待命，正准备替换角色动画）。
3. **Play 是独占资源**：本片**优先做离线**（改代码 + 离线断言），Play 取证放最后；进 Play 前先看 `.ai-tmp/test/play-log.tsv` 最近 2 分钟有无 `OCCUPY` 未释放，有 ⇒ 错峰。
4. 注释按 skill §3 第 3b 条：只写「现在是什么」——⛔ 不写批次 / 本轮 / 任务书 / 用户报的 / 日期 / 别的工程名。
5. 判据 = **一条命令 + 原始输出**：对每把枪给出「改前 / 改后」的实际生效值（伤害、连发发数、首续发间隔），
   并保证**未装消音的那几条不回归**。⛔ 不填验收表、⛔ 不建覆盖矩阵。
6. 命令一律显式超时；超过 12 分钟把进度落盘 + 给 `main` 一条回报（**第一行写你的模型标识**）。

## 回报（`send_message` 给 `main`）
① 改了哪些文件 + 每处一句话；② 每把枪「改前 / 改后」的实际值表（含"未装消音不回归"那一列）；
③ 判据原始输出；④ 没做到的逐条 BLOCKED 原因（写清缺什么）。⛔ 不要长解释。
