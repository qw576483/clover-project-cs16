# agent-10b：续做 agent-10 的产物二 / 产物三（clover-project-cs16）

> 上一棒（agent-10）被宿主中止，**产物一已完成并落盘**：`原版资源/解包产物/原版数值表.md`
> （§1 cvar 默认值含偏移 / §2 引擎 cvar / §3 武器表 `WeaponInfo[]` 25 条 / §4 买枪期 / §5 与我方差值 / §6 BLOCKED / §7 复现命令）。
> **你在它的基础上只做剩下两件**，别重做产物一。

## 0. 契约（原任务书仍有效）

原任务书：`docs/agents/agent-10-reverse-original-data.md`（**先读它**）。本片 = 它的 §2 + §3 + 收尾清理。

## 1. 产物二：`原版资源/解包产物/原版HUD布局.md`

按原任务书 §2 执行：

- **首选**：从 `原版资源/cs16src/cs16game/app/cstrike/cl_dlls/client.dll`（1,074,496 B，ImageBase `0x01900000`）定位 HUD 元素的坐标/字号/颜色。
  上一棒已确认该包 5 个 PE 的 **文件偏移 == RVA**，且 cvar_t/武器表都用「字符串 → 4 字节指针引用 → 结构体」的读法解出来了 —— 同一套读法可直接复用（脚本逻辑见 `原版数值表.md` §0.4）。
  **给它一个时间盒（最多 2 小时/约 60 次工具调用）**；到点没定位到就转降级，并在文件里写清"试过什么、卡在哪"。
- **降级（§0.5 载体链第 4 级，合法）**：用**原版截图量化成表**。载体在你项目内：先 `list_dir` 看清 `原版资源/cs16-maps/screenshots/**`、`原版资源/cs16-maps/screenshots_to_conv/**`、`原版资源/de_dust2/IMG/**`、`原版资源/解包产物/**` 各有什么、分辨率多少，再选能读出 HUD 的图量化。
- 表格式：`元素 | 原版像素（x,y,w,h；注明基准分辨率与出处图/偏移） | 换算到 1080p | 我方值（文件:行） | 差值 | 类别`；
- 覆盖：血量/护甲/金钱/弹药/回合时间/比分/雷达/准星/击杀条/购买区提示（对照表 §4 已列 U-01…U-28，逐条给出能给的、其余进 BLOCKED）；
- 两个来源都覆盖不到的 ⇒ 如实 BLOCKED，⛔ 不许编。

## 2. 产物三：更新 `策划/对照表.md`

- 把 **BLOCKED-1 / BLOCKED-2** 里**已解除**的条目改成正常行（原版值带新出处：`mp.dll:偏移` 或截图坐标）；
- 未解除的**保留在 BLOCKED**，并补写"本次试了什么、卡在哪"（这一句是硬要求）；
- 「数值与公式」块：按 `原版数值表.md` §5 的对比重写相关行；
- ⛔ **表格结构（列数、类别列口径）不许改；⛔ 不许删既有行**。

### 2.1 主 agent 的决策（照此写，别自己另定）

原版有**两套口径**，两套都要登记；**判定"我方该对齐哪套"时以「玩家实际体验」为准 = 随包 `server.cfg` 覆盖值**：

| cvar | mp.dll 出厂默认 | 随包 server.cfg（实际生效） |
|---|---|---|
| `mp_freezetime` | 6 | `原版资源/cs16src/cs16game/app/cstrike/server.cfg` 里的值 |
| `mp_roundtime` | 5 min | 1.75 min = 105 s |
| `mp_c4timer` | 45 | 35 |
| `mp_buytime` | 1.5 min | 0.25 min = 15 s |
| `sv_gravity` | 800（引擎 `hw.dll.orig`） | 800 |
| `sv_maxspeed` | 320 | 320 |
| `sv_accelerate` | 10 | 5 |

⚠️ **必须先把 `SRC/` 这个简写消歧**：上一棒/ agent-09 写的 `SRC/server.cfg:51`＝freezetime `4`，而项目内 `原版资源/cs16src/cs16game/app/cstrike/server.cfg` 实测该行是 `3`（另有一份 `原版资源/cs-docker/files/server.cfg` 是社区 docker 配置，**不算原版**）。
请**核实 `SRC/` 到底指哪个真实文件**，把对照表里这个简写**换成真实相对路径**（两份 server.cfg 都要标明身份与是否采信），并把该文件里 `mp_freezetime/mp_roundtime/mp_c4timer/mp_buytime/mp_startmoney` 的真实行号实测出来。

## 3. 收尾清理（上一棒没做）

删除上一棒遗留在 `.ai-tmp/test/` 的一次性脚本（先确认它们只属于本片范围）：
`refdata.py`、`p21_draw.py`、`p22_tail.py`、`p23_sv.py`、`exact2.py`、`verify_doc.py`，以及上个 agent-08 提到的历史遗留 `compile-check.ps1`（含 444 个非 ASCII 字节且无 BOM ⇒ 会被 ANSI 解析、中文匹配静默失效；**先确认没有别的片还要用它**，若保留则必须改成 ASCII-only 或加 BOM，二选一，写进回报）。
⛔ 其余历史文件（`.ai-tmp/test` 下的 100+ 个旧驱动脚本）**不归你**，别动。

## 4. 判据（自己跑，原始输出贴进回报）

1. 两份产物文件都在，条目数如实报；
2. 抽查 ≥5 条新写的"原版值"，用**可复跑命令**重读一次，输出原文贴出；
3. 报表里凡"原版值"都有 `文件:偏移`（或截图坐标）—— 0 条例外；
4. 对照表里**不再出现 `SRC/` 这类无对应真实路径的简写**（逐条给出真实相对路径 + `Test-Path` 结果）。

## 5. 不许

- ⛔ 不许改 `client/**`、`策划/验收表.md`、`策划/策划案/**`、`docs/步骤文档.md`、任何 skill；
- ⛔ 不许编造偏移/数值/截图像素；拿不到 ⇒ BLOCKED；
- ⛔ 不许读别的 `clover-project-*` 工程；⛔ 不许开子 agent；
- 临时脚本只放 `.ai-tmp/test/`，用完删；不许产出交接/进度类 md。

## 6. 回报格式

```
产出物：<文件绝对路径清单>
自检：<实际执行的验证命令 + 原始输出>
未决：无 / <具体条目>
```
