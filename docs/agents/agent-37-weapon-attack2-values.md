# agent-37 · 差异 #68 剩余主体：消音 / 连发的**数值**出处

> 用户报的：「很多枪右键没效果，就像警的默认小手枪，右键不是拆消音吗？」→ 复查仍报「很多枪械的右键还是无效」。
> 已在盘的：状态链（`CsInputState.Attack2` + `CsWeaponDef.CanSilence/CanBurst` + `CsActor.Silenced/BurstMode` + 消费点）已落地并实机 PASS。
> **本片只补数值** —— 差异 #68 行末逐字写着这是本行主体剩余：
> 「消音后的伤害/散布、连发的发数与节奏的**具体数值**无出处（要反汇编 `client.dll`）」+「表现类并排图未采（另片）」。

## 一、载体（都在盘）

| 文件 | 说明 |
|---|---|
| `原版资源/cs16src/cstrike/cl_dlls/client.dll`（1,093,128 B） | 客户端：`+attack2` 输入、消音器模型/声音、切连发音效 |
| `原版资源/cs16src/cstrike/dlls/mp.dll`（1,640,960 B） | **服务端武器逻辑**：伤害 / 射速 / 散布 / 连发发数**多半在这里**（CS 1.6 的武器伤害是服务端算的） |
| `原版资源/hlsdk/dlls/weapons.cpp` | HL1 的武器实现 ⇒ **只能证明机制形状**，⛔ 不是 CS 语义出处 |

## 二、要取出的数值（逐条，缺一条就如实标 BLOCKED）

1. **USP / M4A1 装消音器后的伤害**（相对未装：倍率或绝对值）。
2. **USP / M4A1 装消音器后的散布 / 精度**（同一把枪两种状态的差异）。
3. **Glock18 / Famas 连发的发数与节奏**（每次点右键打几发、两发间隔多少 ms）。
4. 上述每条的**改动点**：是"覆盖表"还是"分支里改常量"。

## 三、口径（⛔ 硬要求）

- 每条数值必须带**出处**，形如：`文件 + 文件偏移 + 该处字节（hex）+ 反汇编片段（若可反汇编）`，
  或**公开载体**（如 Valve 官方 wiki / 官方 SDK 源码）逐字引用 + URL。
- ⛔ 不许写「看起来合理」；⛔ 不许把 HL1 的数值当 CS 的；找不到就 BLOCKED（写清试过什么）。
- 降级链：原始数据（反汇编）→ 程序里的常量 → 专用格式原始结构 → 联网（公开 wiki / 官方文档）→ BLOCKED。

## 四、产出

1. 台账 `策划/武器右键数值出处.md`（**新增文件**）：逐条 = 数值 | 出处（偏移/字节 或 URL 逐字）| 可信度（逐字节 / 反汇编 / 公开资料）| 是否可用于改代码。
2. 若反汇编确实取到值：**改代码那一步另片做**（本片只到"数值 + 出处"为止）。
   ⇒ 回报里要给出"这些值该写进哪个字段"的建议（`client/Assets/Scripts/Core/CsWeapons.cs` 的 `CsWeaponDef` 现状读一遍即可，⛔ 别改它）。
3. 工具：先查本机可用反汇编器（capstone / objdump / dumpbin / radare2）。
   都没有 ⇒ `pip install capstone`（装不上就 BLOCKED 并说清）。

## 五、边界

- 写入范围：`策划/武器右键数值出处.md`（新增）、`tools/probes/**`（**只许新增** `attack2-*` 前缀的文件）、
  `.ai-tmp/**`、`docs/agents/agent-37-*.md`。⛔ 不写任何 `client/Assets/**`。
- **另一条线正在清理 `client/Assets/Scripts/**` 若干文件与 `tools/probes/*.cs` 的注释** ⇒ 本片只新增文件，不碰既有文件。
- ⛔ 不改引擎源码、⛔ 不改 skill。注释按 skill §3 第 3b 条。
