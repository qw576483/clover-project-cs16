# agent-24：天空盒 `up` 面朝向定案 + `viewsize` 结论登记（离线为主）

> 项目根：`clover-project-cs16`
> 本片**基本离线**（像素邻接计算 + 注释/文档），最后若要改 `Dust2Builder.cs` 才需要一次 Play 采一张抬头图。

## 0. 两条已核实的事实（直接用，别重查）

### 0.1 `viewsize` 默认 = 120 ⇒ viewmodel 无补偿 ⇒ 我方已对齐

- `view.cpp:662-665`：`view->origin[2] -= 1;`（viewmodel 原点比眼位低 **1 unit**）；
- `view.cpp:667-684`：**只**为 `viewsize ∈ {110,100,90,80}` 做 z 补偿（+1 / +2 / +1 / +0.5）；
- **`viewsize` 默认值 = `"120"`**，出处 = `原版资源/cs16src/cs16game/app/hw.dll` 偏移 **`0x177684`** 的 cvar 字面量区
  （实测片段：`…"30" "viewsize" "120" "viewsize" …`；HLSDK 里 `viewsize` 只在 `common/ref_params.h:50` 是字段、**没有注册** ⇒ 它由引擎填）。
- ⇒ **120 走"无补偿"分支** ⇒ 原版 viewmodel 净偏移 = **−1 unit = −0.0254 m**；
  我方 `Module/View/CsViewTuning.cs` 的 `ViewModelLocalPosition = (0, −0.0254, 0)` **正是该值** ⇒ **差值 = 0，不要改它**。

### 0.2 四侧面环序已定案

`策划/基线图/skybox/des{ft,bk,lf,rt,up,dn}.png`（256×256，原版像素）实测环序 = **`ft→lf→bk→rt`**（环代价 **42.77**，镜像装配是 152.60）⇒
`Editor/MapGen/Dust2Builder.cs` 已按此绑定；**只剩 `up` 面（与可能的 `dn`）的旋转未定**（agent-21 报"区分度只 4%"）。

## 1. 只做这两件

### 1.1 用**立方体邻接约束**把 `up` 面朝向算出来（⛔ 不许猜）

- 输入：`策划/基线图/skybox/` 的 6 张 256×256 PNG（原版像素）。
- 判据（纯离线、可复跑）：**`up` 面的四条边必须分别与四个侧面的顶边像素相接**（立方体的几何约束）——
  枚举 `up` 面的 **4 个旋转 × (是否水平翻转)**（共 8 种），对每种算
  「`up` 第 i 条边 vs 对应侧面顶边」的**逐像素差之和**，取**最小者**为定案。
  - 四侧面与 `up` 边的对应关系由 0.2 的环序决定，不许另起一套；
  - `dn` 面同法（与四侧面**底边**相接）。
- **⛔ 区分度门槛**：若最优解与次优解的代价比 **< 1.5×** ⇒ 如实写 `BLOCKED：像素区分度不足（给实际比值）`，
  **⛔ 不许硬选一个、不许编**。（28.455 vs 29.606 这种 1.04× 就是"判不了"。）
- 定案且**与我方当前绑定不同** ⇒ 改 `Dust2Builder.cs` 的对应绑定（保持幂等、可重跑），
  然后**进 1 次 Play** 采一张**抬头看天**的帧（含四个角落），确认四角云/山脊连续；
  定案但**与我方相同** ⇒ 只写结论 + 出处，不进 Play。

### 1.2 登记 `viewsize` 结论（文档 + 注释，⛔ 不改数值）

- `client/Assets/Scripts/Module/View/CsViewTuning.cs` 的 `ViewModelLocalPosition` 注释里补上这条出处链：
  `hw.dll:0x177684`（`viewsize=120`）+ `HLSDK cl_dll/view.cpp:665`（`-=1`）+ `:669-684`（120 无补偿）⇒ 净 −1 unit = −0.0254 m；
- `策划/对照表.md` 的 **A2 / viewmodel** 行：把差值改成 **`0`** 并写上这条出处（原版值 = −0.0254 m）；
- `策划/外观差异清单.md` 的 A2 行：把 `[待定]` 改成**已定案**（依据同上），并注明"FOV 修复后手臂观感已正常（联络图 4 的 `G-04-1` 格）"。

## 2. 判据（自己跑，原始输出贴进回报）

1. **邻接计算的完整输出**：8 种（`up`）/ 8 种（`dn`）各自的代价表 + 最优/次优比值 + 定案；
2. （若改了 `Dust2Builder.cs`）一次编译 `compile-check.ps1` ⇒ `csc exit=0` + 一次 Play 的抬头帧（放 `<项目根>/.ai-tmp/screenshots/`，命名 `24_sky_*.png`）；
3. `策划/对照表.md` 与 `策划/外观差异清单.md` 的 A2 行更新后的原文；
4. `tools/verify.ps1` ⇒ 除 `HUMAN-ONLY` 外无 FAIL（若因改了 `Dust2Builder.cs` 导致某行证据过期 ⇒ 只重采**那几行**，⛔ 不许全量重采）。

## 3. 不许

- ⛔ 不许改 `tools/ai-skill/**`、任何 skill、`.ai-tmp` 历史文件、别的 `clover-project-*` 工程；
- ⛔ 不许编造像素代价/出处/最优解；判不了就 BLOCKED（写清试过什么）；⛔ 不许改 `ViewModelLocalPosition` 的值（它已对齐）；
- ⛔ 不许动与本片无关的功能；⛔ 不许开子 agent；⛔ 不许产出交接/进度类 md；
- 进 Play 前先在 `.ai-tmp/test/play-log.tsv` 记账（本片最多 1 次）；临时脚本放 `.ai-tmp/test/` 用完删。

## 4. 回报格式

```
产出物：<文件绝对路径清单>
自检：<邻接代价表 + 定案 + 编译/帧（若需）+ 两处文档更新原文 + 闸门逐行>
未决：无 / <具体条目>
```
