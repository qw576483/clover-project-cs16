# agent-58 · UV 口径 V 方向（角色小腿/靴子全黑）—— 影响面最大的一处

## 病灶与已到位的证据（前一片定位，别重查）
用户看到的症状：**角色小腿与靴子近景纯黑、无贴图**（9/9 兵种、左右脚、idle/walk/crouch 全一样，上半身迷彩正常）。
前一片已**证伪**"透明键"假说（角色贴图走 `cs16_build.py:166-192 export_textures`，写 **RGB 无 alpha**；腿覆盖的调色板 255 占比 12.4% vs 整张背景 10.8% ⇒ 无富集），并把真因定位到 **UV 的 V 方向**：
- 按贴图 4×4 网格统计：**腿 76% 落在 x128-256/y128-384 的"靴区"**，而**躯干落在"裤子"区（右下 40.4%）**。
- **把 V 方向取反后**：腿 **50.8% 落到裤子区**、腿覆盖平均亮度 **36.8 → 66.9**；躯干同时回到**背心/夹克**区（右下 40.4% → 5.2%）。
- ⇒ 指向 `.cs16anim` 的 UV 口径 `v = t/h` **与 Unity 左下原点不匹配**（GoldSrc 上传时翻图、Unity 不翻）。

## 目标
1. **先把这个推断变成可判定的结论**（有出处/有对照，⛔ 不是"看起来更像"）：
   - 可用参照：源 mdl 的 UV 语义（GoldSrc 贴图上传时是否翻转）、以及**本工程已正确显示的贴图**（上半身迷彩正常 ⇒ 它落点对，可作为"V 方向对不对"的内部对照）。上半身正常而下半身落在靴区，本身就是"部分 UV 错位"的证据，请把它讲清。
2. **修 UV 口径**（先定改在哪一层）：候选 `AnimSetup.BuildSkinMesh` 的 uv 写入，或 `原版资源/cs16src/cs16_anim.py` 的 UV 口径 ⇒ 选一处改，并说明为什么选它。
3. **⚠️ 影响面必须全面复核**（这是本片最大的风险）：该改动会同时影响 **9 个角色 + 全部视模型（`viewmodel_*`）+ 全部世界道具（`w_*`）**。修完逐类抽查：
   - 角色：上半身**没有变差**（迷彩仍正常）+ 小腿/靴子出现布料/靴子贴图；
   - 视模型：`viewmodel_ak47` 等贴图**没被翻坏**；
   - 世界道具：`w_awp` / `w_glock18` 等**没被翻坏**；
   - 给出"逐类抽查正常 / 异常清单"。

## 硬约束
1. ⛔ 不改引擎、⛔ 不改 skill。
2. 写入范围：`client/Assets/Editor/Views/**`、`client/Assets/Resources/Art/**`、`client/Assets/Editor/Views/ModelData/**`、`原版资源/cs16src/cs16_anim.py`（若选它）、`.ai-tmp/**`、`策划/差异登记.tsv` **#71（行号 72）**。
   ⛔ 不写 `Module/Combat/**`、`Module/View/**`、`Core/**`、`UI/**`（`muzzle-geometry` 可能还在改前两者）。
3. **改资产前 `editor_stop`**；⛔ 绝不 `Stop-Process`；⛔ 不开第二个编辑器；`Art/**` 重烘会动大批文件 ⇒ **备份 `.ai-tmp/test/a58-bak/`** + 报改动数。
4. **必须能回退**：若抽查发现视模型或世界道具被翻坏 ⇒ **立刻回退**并向 `main` 报（⛔ 不许"先留着看看"）。
5. **⚠️ 无原版客户端可仲裁** ⇒ 你**没有**"与原版逐像素比"的参照物；所以判据是 **① 内部对照**（上半身/已正确部位不被改坏）**② 语义合理性**（腿该落裤子/靴子区、躯干该落背心/夹克区）**③ 逐类抽查不被翻坏**。⛔ 不许声称"与原版一致"。
6. **Play 是独占资源**：进前看 `.ai-tmp/test/play-log.tsv` 末行是否 `RELEASE` 且 `play-driver-log.tsv` 近 90 秒无新行；`OCCUPY`/`RELEASE` 配对、格式 `2026-09-26 HH:mm`（照抄文件里既有格式）。
   ⚠️ **已知坑（前一片实测）**：`cs16-play-driver.cs:40` 的 state 路径是**写死的 `state.txt`** ⇒ 改成 `state-<片名>.txt` 会让 `Clk` 写的 `click=` **没人读、前链全废**（会白跑且每张图字节数完全相同）。**用共用 `state.txt`，用前备份、用后还原**。
   ⚠️ **另一条已知坑**：编辑器**不自动重编译** ⇒ Play 可能跑旧程序集；采证前先 `unity command recompile` 并等 `recompile_status=completed`。
7. **判据**：修前/修后同机位近景（角色腿 + 上半身 + 一把视模型 + 一件世界道具）+ 逐类抽查结论 + 贴图 UV 落点统计（腿/躯干各落在哪个区）。⛔ 不填验收表、⛔ 不建矩阵。
8. 注释按 skill §3 第 3b 条：只写「现在是什么」。命令显式超时；超 12 分钟落盘 + 回报（第一行写模型标识）。

## 回报（`send_message` 给 `main`）
① 推断→结论的证据（V 方向判定）；② 改在哪一层 + 为什么；③ 修前/修后近景路径；④ **逐类抽查**（角色/视模型/世界道具）；⑤ 位图/`Art/**` 改动数 + 备份路径；⑥ 没做到的逐条 BLOCKED。⛔ 不要长解释。
