# agent-09：原版对照表 + 素材调研 + 原版资源清单（clover-project-cs16）

> 项目根：`c:\Work\Server\full-dev\clover-project-cs16`
> 本片产出**三份文档**（全是 §2 / §4 闸门 3 / §1.9 的硬要求），⛔ 不许碰业务代码。

## 0. 开工必做

1. `use_skill("clover-engine")`；重点读 skill 规则层 §0.5（禁写条款）、§1.9（原版资源）、§2（对照表）、§4 闸门 3（素材）。
2. 现状（已核实）：
   - 参考游戏 A = **Counter-Strike 1.6**（单机 de_dust2）；形态判定在 `策划/策划案/CS1.6单机参考规格.md` 顶部；
   - 项目**已经拿到并用上了 A 的原版素材**（最高级 ①）：`client/资源欠缺清单.md` §0 记录了来源（archive.org 的 `cstrike-LiON.iso`）与解包方式（`innoextract`）；
   - 原版载体已在项目内：`原版资源/**`（`cs16src/`＝解包脚本+SDK+ISO，`cs16-maps/`、`cs_docker/`、`de_dust2/`、`de_dust2_scene/`、`解包产物/`、`cs109/`）。
3. ⛔ 不许读工作区里**别的** `clover-project-*` 工程（红线）。

## 1. 产物一：`策划/对照表.md`

**它是 §0.5「复刻 = 解析 + 搬运」的执行形态**，也是闸门第 8 条的判据。

格式（每行一条）：

```
| 元素 | 原版值（出处：文件:行 / 字节偏移 / 图片坐标） | 我们的值（代码 文件:行 或资产路径） | 差值 |
```

必须覆盖（至少这五块）：

1. **几何与碰撞（含通行性）** —— 原版 `de_dust2.bsp`（`原版资源/cs16-maps/counter-strike/maps/de_dust2.bsp` 或 `cs_docker/maps/**`）里的碰撞/可走数据，对比我们的 `client/Assets/MapData/de_dust2.bytes` + `client/Assets/Editor/MapGen/Dust2Builder.cs` / `Dust2Layout.cs` 的 218 阻挡盒与 127×145 位图；
2. **模型与动画数据** —— 原版 `models/player/*.mdl`、`models/v_*.mdl` 的骨骼数 / 序列（idle1/run/walk/crouch/death1/shoot…）名称与帧数 / 帧率（GoldSrc ~10fps 与 `mdl` 内 `fps` 字段），对比我们生成的 prefab / 动画资产（当前**动画未做**，这一列就照实写「未实现」并进 BLOCKED 行）；
3. **贴图** —— 原版 `cstrike/**/*.wad` / `models` 内嵌贴图 vs `client/Assets/Resources/Art/Tex/*.png`（242 张）、`Assets/ThirdParty/Dust2/Textures/*.png`（26 张）；
4. **界面元素坐标/尺寸/颜色** —— 原版 HUD 布局（在 `cstrike/sprites/` 的 HUD 精灵与 `hud.txt` 之类定义里；去哪找**必须实测确认**，找不到就在 BLOCKED 小节写明"原版该数据不可得，实际依据是什么"）vs 我们的 `client/Assets/Scripts/UI/InGame/CsHudTheme.cs`、`HudPanel.cs`、`CsRadarWidget.cs`、`CsCrosshairWidget.cs` 等的像素坐标/字号/颜色常量；
5. **数值与公式** —— 原版 CS 源码（`原版资源/cs16src/hlsdk/**`，找 `weapons.cpp` / `player.cpp` / `weapon_*.cpp` 里的伤害/价格/射速/护甲/移动速度/重力/跳跃）vs 我们的 `client/Assets/Scripts/Core/CsWeapons.cs`（32 把）、`CsConst.cs`、`CsDamage.cs`、`CsEconomy.cs`。

硬规则：

- **写不出出处的量不许进表**（§0.5 判据）。出处必须是 `文件:行` / 字节偏移 / 图片坐标 —— **"看起来合理"不是出处**；
- 查不到原版值的**单列「BLOCKED」小节**：写明缺什么、为什么拿不到、谁能给。⛔ 不许自己编一个值填进去；
- 差值列：`0` / `差 x` / `未实现` 三选一；**不许留空**；
- 表格外的元素**不许存在**（实现了但没进表 = 没人搬过）。

## 2. 产物二：`策划/素材调研.md`

§4 闸门 3 的落盘要求。**必须如实**：写**真实发生过的**检索与获取历程。

- 已确认的事实（照实写，并给出来源）：最终拿到的是 **A 自己的原版素材**（ISO → innoextract → `cstrike/**`），细节见 `client/资源欠缺清单.md` §0；
- 「穷尽」的量化定义要能对上：**≥4 轮不同关键词（中英各半）× ≥3 类站点 × 换过格式与打包方式**。
  你能离线复现的部分：`原版资源/` 里已有的各类rip/镜像/解包产物（`cs16-maps`、`cs_docker`、`cs_16_maps`、`de_dust2`、`de_dust2_scene`、`cs109`、`解包产物`）—— 逐个查清**它们是哪来的、是什么格式、为什么用/不用**，这本身就是"格式与打包方式"这一维度的证据；
- 若某轮检索你**无法离线证实**（例如具体站点的搜索页），**如实标注"该轮无留痕，不可核验"**，⛔ 不许编造访问记录、⛔ 不许编造下载 URL；
- 结尾给一行结论：① 是否已用上 A 自己的素材（是/否）② 若否，①②为什么拿不到、登记在哪。

## 3. 产物三：`原版资源/清单.md`

§1.9 第 4 条硬要求：**换台机器照着它要能完整复现**。

必须含：来源 URL / 包名（含体积、校验和若有）/ 解包命令 / 导出了什么 / 复制到工程哪些路径。

- 逐个盘 `原版资源/` 下 8 个子目录（`cs-docker`、`cs109`、`cs16-maps`、`cs16src`、`cs_16_maps`、`de_dust2`、`de_dust2_scene`、`解包产物`）：**来源 + 格式 + 用途 + 是否仍需要**；
- 把 `client/资源欠缺清单.md` §2「生成/重建方式」里的三步（`cs16_build.py` → ArtSetup 生成器 → 只读自检）**按项目内真实路径**搬进来（那份文件刚被修过路径，照它的新路径写）；
- 明确标注 **`原版资源/` 与 `.ai-tmp/` 不进 git / 不进交付**（`.gitignore` 已有两行）；
- 每条命令注明**是否可离线复跑**（依赖 ISO 的写清依赖）。

## 4. 判据（自己跑一遍，原始输出贴进回报）

1. `策划/对照表.md` 存在；五行覆盖块齐；**抽查 ≥5 行的出处**，用命令实际打开确认（`Select-String` 命中行 / `Test-Path`），把结果贴出来；
2. 无"出处空缺"的行混在表里；BLOCKED 单列成节；
3. `策划/素材调研.md` 存在且含 ≥4 轮关键词、≥3 类站点、格式维度；
4. `原版资源/清单.md` 存在，8 个子目录逐个有"来源/格式/用途/是否仍需要"；
5. 三份文档里**没有任何指向已不存在路径**的引用（`Test-Path` 全过）。

## 5. 不许

- ⛔ 不许改任何 skill、不许改 `策划/验收表.md`、`策划/策划案/**`、`docs/步骤文档.md`；
- ⛔ 不许碰 `client/**` 下任何文件（本片纯文档）；
- ⛔ 不许编造出处、不许编造检索记录、不许编造 URL；
- ⛔ 不许开子 agent；临时脚本只放 `.ai-tmp/test/`，用完删；不许产出交接/进度类 md。

## 6. 回报格式

```
产出物：<文件绝对路径清单>
自检：<实际执行的验证命令 + 原始输出>
未决：无 / <具体条目>
```
