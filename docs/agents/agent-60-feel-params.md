# agent-60 · 「手感」维度：原版移动/射击参数 vs 本工程（反汇编取证）

## 为什么是你
用户点名 **手感不一致**。手感 = 一堆参数的合力（移动加速度/摩擦/跳跃/空中控制、射击速率/后坐力/散布/准星展开、切枪/换弹时长、脚步与转身速率）。
**这块不需要原版录屏**⇒ 直接从盘上 DLL 取原版立即数，与工程现值逐条比 ⇒ **今天就能出结论**。

## 载体（都在盘）
- `原版资源/cs16src/cstrike/dlls/mp.dll`（1,640,960 B，服务端武器/移动逻辑；ImageBase `0x10000000`，`.text` 文件偏移 = VA − `0x10000C00`）
- `原版资源/cs16src/cstrike/cl_dlls/client.dll`（1,093,128 B，客户端预测/后坐力/准星）
- `原版资源/cs16src/hlsdk/**`（HL1 侧，**只能证机制形状，⛔ 不是 CS 出处**）
- 工具：`python` + `capstone`（本机可用）；前序片脚本在 `.ai-tmp/test/attack2/`（`disasm.py`、`xargs.py` 类）可参考
- 工程现值所在的表：`client/Assets/Scripts/Core/CsConst.cs`、`Module/Combat/CsCombatTuning.cs`、`Module/Match/**`（自己 `grep` 定位）

## 目标：一张"原版 vs 我们"的手感参数表（逐条带出处）
覆盖至少这些（取不到就如实 BLOCKED，⛔ 不许拿 HL1 的冒充 CS）：
1. **移动**：最大速度（走/跑/蹲）、加速度、摩擦/停止、空中加速度、跳跃初速、台阶高度、蹲/站身高
2. **射击**：每把枪的射速、后坐力（`punchangle`）、散布（首发的"第一枪准"、连发递增）、准星展开/回收
3. **交互时长**：切枪、换弹、开镜、投掷
4. **其它直接可比的**：转身速率、脚步间隔、开火时抖动

**产出格式**（每条一行）：`项目 | 原版值 | 出处（文件 + 偏移 + 字节/反汇编） | 工程值 | 出处（文件:行） | 差值 | 结论（一致 / 不一致 / 未取到）`
⛔ 工程值必须有 `文件:行`；原版值必须有偏移+字节；**两边都写不出就标 BLOCKED**，不许填"看起来合理"的数。

## 硬约束
1. ⛔ **本片不改任何代码**（只出表 + 出处）；⛔ 不改引擎、⛔ 不改 skill。
2. 写入范围：`.ai-tmp/**`、`策划/手感参数对照.md`（新建）。
3. 差值本身**不是判决**：写明"不一致"就够，改不改由后续片决定（⛔ 别顺手改）。
4. 命令显式超时；超 15 分钟把进度落盘 + 给 `main` 一条回报（**第一行写模型标识**）。

## 回报（`send_message` 给 `main`）
① 表里"不一致"的条目**按影响排序**（移动/射击/交互各几条）；② 逐条一句话（项 | 原版 | 工程 | 差值）；③ 取不到的逐条 BLOCKED（写清试过什么）；④ 产物路径。⛔ 不填表、⛔ 不建矩阵。
