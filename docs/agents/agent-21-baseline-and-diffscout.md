# agent-21：外观只读取证 —— 补「基线图 + 差异清单」（用户报的 3 组问题）

> 项目根：`clover-project-cs16`
> **本片只读取证（① 拍），⛔ 不许写实现代码、⛔ 不许进我方工程的 Play。**
> 目的：给用户报的外观问题建立**原版基线**与**可执行目标值**，让下一片能"批量改 + 一次取证"。

## 0. 用户本轮报的问题（原话）

1. **相机有点鱼泡眼，正常的 cs 胳膊好像没这么长**
2. **A2 平台地面材质不对**
3. **UI 和模型移动 / 第三视角全是问题**（"还有很多很多问题"）
4. 主 agent 看图补充：`client/Assets/Screenshots/91_fix_post_thirdperson.png` 里**天空盒左右两侧有接缝/破洞**（右上两块白蓝拼贴错位）。

按 skill §3：**缺 ③ 基线图（`策划/基线图/`）与 ④ diff 闭环器 ⇒ 不许碰外观/UI**。本片就是补 ③④ 的取证。

## 1. 输出（三件产物）

### 1.1 `策划/基线图/` + `策划/基线图/场景清单.md`

- **一级来源（首选）：项目内的原版客户端本体** ——
  `原版资源/cs16src/cs16game/app/hl.exe`（+ `hw.dll` / `cstrike/**`，`-game cstrike`）。
  能跑起来就在**原生 1920×1080**下采这几张：
  1. **第一人称 · 站在 dust2 T 出生点**（要含 HUD + 手持 USP + 手臂）—— 用于定 **FOV 与 viewmodel 占比**；
  2. **第一人称 · A 点平台**（要含 A 点地面材质 + 平台边缘）—— 用于定 **A2 平台地面 miptex**；
  3. **第三人称 · 观战/旁观**（`+` 控制台进观战，或先死后观战）—— 用于定**第三视角**的机位/构图；
  4. **天空盒**：同一位置**抬头 + 左右各转 90°**（用于查天空盒朝向与接缝）。
- 若原版客户端**跑不起来**（显卡/兼容/缺授权）⇒ **不许自己画**：按 §0.5 降级链退到
  **项目内已有的原版图**（`原版资源/cs16-maps/screenshots/**`、`cs16-maps/screenshots_to_conv/**`、
  `原版资源/de_dust2/IMG/**`），并在 `场景清单.md` 写清"哪一级、为什么上一级拿不到、缺哪些视角（如"缺第一人称 HUD"）"。
- `场景清单.md` 每张图一行：`图名 | 分辨率 | 视角/场景 | 采集方式（命令行/降级来源） | 能锚什么 | 锚不住什么`。

### 1.2 `策划/外观差异清单.md`（本片的核心产出，下一片的输入）

逐项写清，每项**必须有出处**：

| # | 现象 | 原版值（出处：文件:偏移 / 文件:行 / 图片坐标） | 我方值（文件:行） | 目标值 | 判据（怎么算过） |
|---|---|---|---|---|---|
| A1 | 视野过宽（"鱼泡眼"） | | | | |
| A2 | viewmodel 占比异常（"胳膊太长"） | | | | |
| B1 | A2 平台地面材质 | | | | |
| C1 | 天空盒接缝/朝向 | | | | |
| D1 | 第三视角机位/构图 | | | | |
| E1 | 模型移动表现 | | | | |
| F1 | UI（逐项列，别写"UI 有问题"） | | | | |

**必查的几处（给方向，不许跳过）**：

- **A1 FOV**：原版 `default_fov` 是**引擎 cvar** —— 用项目已有能力读（`原版资源/cs16src/cs16_build.py` 里的 cvar 扫描法、
  或 `原版资源/解包产物/原版数值表.md` §2 的做法）在 `hw.dll.orig` / `sw.dll` / `swds.dll` 里找它的 cvar_t。
  然后**必须回答**：GoldSrc 的 fov 是**水平**还是垂直？宽屏（16:9）下原版是 Hor+ 还是固定垂直？
  ⇒ 给出 Unity `Camera.fieldOfView`（**垂直**）的换算值与公式。
  我方现值：`client/Assets/Scripts/Core/CsConst.cs` 的 `DefaultFov` / `ZoomFov`（先读，别猜）。
- **A2 viewmodel**：我方 `Module/View/CsViewTuning.cs` 现在写 `ViewModelLocalPosition = zero`、`ViewModelScale = 1`，
  注释理由是"v_ 模型自带相机空间坐标"。**要去验证这个理由**：GoldSrc 画 viewmodel 时用的 FOV 与缩放
  是什么（引擎侧：`hw.dll` 的 `V_RenderView` / studio 绘制路径；或 `cstrike/cl_dlls/client.dll`），
  以及 `viewmodel` 相关的 cvar（如 `cl_righthand` 之外还有哪些）。
  ⛔ 若确实拿不到引擎侧常量 ⇒ 用**原版截图量化**（viewmodel 的屏幕 bbox 占比），并写明"上一级为什么拿不到"。
- **B1 A2 平台 miptex**：从 `原版资源/cs109/maps/de_dust2.bsp`（或其副本）解析 **A 点平台**那些 face 的 `miptex` 索引与名字
  （筛选条件自己定但要写清：按 face 中心/法线/在 BombsiteA 标记附近），
  与 `client/Assets/Editor/MapGen/Dust2Builder.cs`（21 材质组）和 `Assets/ThirdParty/Dust2/Textures/*.png` 对照。
- **C1 天空盒**：GoldSrc 的 `gfx/env/{name}{ft,bk,lf,rt,up,dn}.tga` 与方向的对应关系（引擎约定），
  对照我方 6 面是否装错朝向；接缝的根因（朝向错 / 缺一面 / 分辨率不符）写成可判据的结论。
- **D1 第三视角 / E1 模型移动**：以原版图为基准，列出**具体差异**（机位高度/与角色的距离/是否跟手/是否穿墙、
  移动时序列切换是否与位移匹配…），⛔ 不许写"有问题"这种无量化的结论。

### 1.3 把可对照的行追加进 `策划/对照表.md`

按**它现有的列结构**（元素 | 原版值（含出处） | 我们的值（出处） | 差值 | 类别）追加 A/B/C/D/E 各块的新行。
⛔ 不许改表结构、不许删既有行。

## 2. 现场事实与方法（别重复踩坑）

| 事实 | 说明 |
|---|---|
| unity 命令必须在 `client` 目录内执行 | 否则 `unity status` 空表（不是编辑器没开）；每条命令也可带 `--project-path` |
| `unity command eval` 有 ~5s 主线程上限 | 长任务用 `--detach` / job 轮询（见 `clover-tools/ai-skill/reference/pipeline-and-unity-cli.md`） |
| `--code` 里不要写字符串字面量 | PowerShell 会吃掉引号 |
| **本片不要求进我方工程 Play** | 用户报的是"与原版不一致"，判据在**原版那一侧**；我方现状用**只读**方式（读代码/读已有截图）确定即可 |
| 驱动脚本 | 本轮要复用的放 `<项目根>/.ai-tmp/drivers/`；真一次性的放 `.ai-tmp/test/` 用完删 |
| 长任务/原版游戏进程 | 原版 `hl.exe` 属于 GUI 程序，启动/截图若需要交互 ⇒ 在回报里写 `BLOCKED：需要用户点一下` 并说清"点哪里" |

## 3. 判据（自己跑，原始输出贴进回报）

1. `策划/基线图/` 有图 + `场景清单.md` 每张一行（含"能锚/锚不住"）；
2. `策划/外观差异清单.md` 每行都有出处与目标值；**A1 必须给出换算公式与数值**；
   **B1 必须给出原版 miptex 名与"我方错在哪一组"**；
3. 基线图你**自己读过**（读图前先自检通道：读不到 ⇒ `BLOCKED：图像通道不可用`，⛔ 不许编画面描述），
   并对 A1/A2 各写一句"图上实测到的占比/位置"（像素）；
4. 对照表追加行的**分块统计**（新增 N 行，各块多少）；
5. `powershell -NoProfile -ExecutionPolicy Bypass -File tools\verify.ps1` 的当前状态（本片不应引入新的 FAIL）。

## 4. 不许

- ⛔ 不许改任何业务代码（`client/**`）、不许改 `策划/验收表.md`、`策划/策划案/**`、`tools/ai-skill/**`、任何 skill、
  `.ai-tmp` 里的历史文件；本片**只许写** `策划/基线图/**`、`策划/外观差异清单.md`、`策划/对照表.md`；
- ⛔ 不许编造原版值 / 截图像素 / 出处；拿不到 ⇒ 按降级链退，退到底写 BLOCKED 并说清试过什么；
- ⛔ 不许读别的 `clover-project-*` 工程；⛔ 不许开子 agent；
- 一次性脚本只放 `.ai-tmp/test/`（复用驱动放 `.ai-tmp/drivers/`），用完删；不许产出交接/进度类 md。

## 5. 回报格式

```
产出物：<文件绝对路径清单>
自检：<场景清单 + 差异清单逐项 + 读图实测 + 对照表新增统计 + 闸门状态>
未决：无 / <具体条目>
```
