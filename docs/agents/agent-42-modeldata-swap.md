# agent-42 · 把新 mdl 数据接进工程（#71 角色模型 / #75 地面枪）

## 背景（都已落盘，可直接用）
- `mdl-converter` 已重建转换链（`原版资源/cs16src/cs16_{mdl,build,anim}.py`），产出：
  - `client/Assets/Editor/Views/ModelData/` 里 **7 组 / 14 个文件**：`player_T_orig.{cs16mdl,cs16anim}` + 6 组 `w_*`（**这 6 组是 1.8 m 归一旧口径**）
  - `.ai-tmp/test/mdl-out/` 里 **6 组 0.0254 m 口径的 `w_*`**（正确版本，字节数与旧版相同）
- 对账结论（已写进 `策划/差异登记.tsv` #71 行）：**工程内既有 `player_T.*` 来自另一份更低模的 mdl**（347 顶点 / 254 三角），而本轮 `terror.mdl` 是 372 顶点 / 740 三角 ⇒ 用户那句"感觉你是社区版本的模型，不是原版模型"成立。

## 目标（三件，缺一不算完）
1. **角色模型替换**：先读 `client/Assets/Editor/Views/ArtSetup.cs` + `Cs16ModelData.cs`，搞清 **9 个角色键各对应哪份 mdl**。
   本轮只取回了 `terror.mdl` 与 `urban.mdl` ⇒ **只替换能对上的那两把**（T 与 CT 各一个代表），⛔ **不要**把 9 个角色全换掉（其余没有新 mdl，换了就是破坏）。
2. **世界枪换口径 + 接进掉落物**：
   - 用 `.ai-tmp/test/mdl-out/` 的 0.0254 版**覆盖** `ModelData/` 里那 6 组 `w_*`（⛔ 别改字节结构，只换数值口径）；
   - 接进**掉落物渲染**（#75 的"表现类那一半"：枪躺在地上 + 拾取后消失）。掉落物逻辑在 `Module/Match`/`Module/View` 里，自己找齐（⛔ 不要动 `Module/View/CsLanRemoteView.cs`，那是 LAN 片的）。
3. **复采**：Play 里采 ① 角色近景（换模型后，能看出与原版一致的细节）② 掉落枪躺在地上 + 拾取后消失 的帧。

## 硬约束
- ⛔ **改资产前必须先 `editor_stop` 释放窗口**（`ModelData/**` 改动会触发重导入）；⛔ 绝不 `Stop-Process`；⛔ 不开第二个编辑器实例。
- ⛔ 不改引擎源码、⛔ 不改 skill、⛔ 不写 `Module/Net/**`、`Module/Bot/**`、`Module/Map/**`、`Core/**`、`UI/**`（别的片地盘）。
- ⛔ 不改 `策划/差异登记.tsv` 里不属于你的行；你只写 #71 / #75 两行（追加"落地"段）。
- 判据 = **一条命令 + 原始输出**（替换后各键的字节数 / 掉落物实例计数 / 拾取后计数）+ **截图**（落 `.ai-tmp/screenshots/`）。
  ⛔ 不填验收表、⛔ 不建矩阵。
- 注释按 skill §3 第 3b 条：只写「现在是什么」——⛔ 不写批次 / 本轮 / 任务书 / 用户报的 / 日期 / 别的工程名。

## 排期（别插队）
`bot-astar`（完整回合 5 min）→ `buymenu-original`（第 3/4 条）→ **你** → `collision-mesh`。
⇒ **先把离线准备做完**：角色键 ↔ mdl 映射表、要覆盖的文件清单（7 组 / 14 文件 + 6 组旧口径 `w_*`）、掉落物链路清点、覆盖脚本。
窗口到了（`play-log.tsv` 前一位记"释放"或心跳静默 ≥2 分钟）再动资产 + 复采。
- 命令一律显式超时；超过 12 分钟把进度落盘 + 给 `main` 一条回报（**第一行写模型标识**）。
