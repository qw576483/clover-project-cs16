# agent-48 · 死亡尸体（差异 r74）

## 一句话
用户报的：「**死亡后没有尸体，且死亡动画播完立刻整具身体消失**」。目标是**给出尸体**（留下 + 姿态 + 可穿过与否按原版口径）。

## 前情与建议路径（台账逐字在 `策划/差异登记.tsv` **r74**）
- 台账**已给出首选证据路径**：① **对已在盘的 `mp.dll`（1,640,960 B）做字符串/反汇编取证**（⛔ 比找原版实拍更快）定死「尸体留多久 / 什么姿态 / 是否可穿过」；或 ② 取一份原版死亡实拍。
- **新素材（本轮刚取回，顺手可用）**：原版死亡/受击序列已在产物里 —— `crouch_die`(31 帧)、`death1`(36) / `death2`(41) / `death3`(106)、`gut_flinch` / `head_flinch`、`head` / `gutshot` / `left` / `right` / `back` / `forward`（`client/Assets/Editor/Views/ModelData/player_T.cs16anim` 与 `player_CT.cs16anim` 都含，clips=69）。⇒ 死亡姿态可直接用原版序列，⛔ 不用自己编。

## 目标与判据
1. **先定口径**（出处必须落地）：尸体**留多久**（时长/是否整回合）、**什么姿态**（用 `death1..3` 哪一条 / 是否随伤害方向）、**能不能穿过**（碰撞体留不留）、**是否流血/变暗**。出处 = `mp.dll` 偏移+字节 / 符号字符串 / 或原版实拍 —— **写不出来就如实 BLOCKED 列名**，⛔ 不许拍"看起来合理"。
2. **实现**：死后**不立刻销毁**整具身体；给一个"尸体态"（停止动画推进 / 冻结在末帧 / 保留渲染网格、剥掉碰撞与逻辑，按第 1 条定的口径来）。
3. 判据 = ① 实机帧：**死亡后 N 秒仍能看到尸体**（同一机位，死亡前/后两张）② 一条命令的原始输出（尸体对象计数 / 持续时间 / 碰撞体数）。
   ⛔ 不填验收表、⛔ 不建覆盖矩阵。

## 硬约束
1. ⛔ 不改引擎源码、⛔ 不改 skill。
2. 写入范围：`client/Assets/Scripts/Module/View/**`、`client/Assets/Scripts/Module/Match/**`、`client/Assets/Scripts/Core/**`（**仅当必要**且回报说明）、
   `client/Assets/Resources/Art/**`、`.ai-tmp/**`、`策划/差异登记.tsv` **r74**（只这一行）。
   ⛔ **不写** `Module/Combat/**`（`fx-decals` 正在改特效）、`Core/CsWeapons.cs`、`Module/Bot/**`、`UI/**`、`Editor/Flow/**`。
   ⚠️ **注意**：`Module/View/CsLanRemoteView.cs` 是 LAN 片的已完成产物，⛔ 别动；`Module/View/ActorView.cs` 是本片主要落点，你可以改。
3. **Play 是独占资源，但用户已明确"可以实机验证"** ⇒ 死亡前后帧必须采。进前看 `.ai-tmp/test/play-log.tsv` 最近 2 分钟有无 `OCCUPY` 未释放，有 ⇒ 错峰；⛔ 别开第二个编辑器会话。
4. 注释按 skill §3 第 3b 条：只写「现在是什么」——⛔ 不写批次 / 本轮 / 任务书 / 用户报的 / 日期 / 别的工程名。
5. 命令一律显式超时；超过 12 分钟把进度落盘 + 给 `main` 一条回报（**第一行写你的模型标识**）。
6. 若反汇编 `mp.dll` 取证本身超时 ⇒ 先把"取到哪几条、缺哪几条"落盘再回报，⛔ 别闷头空转。

## 回报（`send_message` 给 `main`）
① 口径结论（留多久/姿态/可穿过/流血，各带出处）；② 改了哪些文件 + 每处一句话；③ 判据原始输出 + 两张实机帧路径；④ 没取到的口径逐条 BLOCKED（写清试过什么）。⛔ 不要长解释。
