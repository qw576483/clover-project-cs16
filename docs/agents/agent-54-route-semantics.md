# agent-54 · 路点数据修正 + 路线游标语义（A1/A2 收尾，⛔ 不许下调阈值）

## 背景（前两片的结论，别重查）
- **位图侧已完成**：分量 53→1、非主分量格 939→0，sha `0e06d2feaf31797ccb02c4c15f6920c3cb832826ffd932e6752903824c79349b`（两处副本同值）。⛔ **不要再动位图**。
- **高度层已查实不是真因**：离线重算与运行时逐项相等（4400/154/237/83），误剔仅 **2 格** ⇒ 高度层≈99.96% 与独立多层模型一致。`0.45 = sv_stepsize 18u × 0.0254=0.4572 下取整`（`hlsdk/pm_shared/pm_shared.c:1024/1034/1211` + `:1462 zstep=18.0`），且必须与移动侧同值 ⇒ **不动它**。
- 已落一处行为修：`BotNavigator.ResolveNextTarget` 第三道跳点（路点求不出路径时在退化成直线前跳过，`SkipUnreachableWaypoint`）。效果：净/总比 `ZBot 0.080→0.215`、`Darrell 0.123→0.493`；A1/A2 **2/8→3/8**；`终点不可走` 14→10。
- **剩余不达标的形态**：长距离绕行/巡逻（`Rikk` 净/总 5.8/296.6=0.020、`Gooseman` 37.6/180.6），**与高度层无关**。
- **当天 14 次 `终点不可走` 全部落在 4 个格**：`Route_CT_Mid`(80,93)、`Route_T_To_A`(80,91)、`Route_Patrol`(45,15)/(21,125) —— 这 4 格在多层模型里**同样不属于主分量**（真爬不上去的台地/檐面）⇒ **路点坐标摆错了**。

## 目标（三件，范围限死；做完无论 A1/A2 多少都收口）
### 1. 搬那 4 个摆错的路点（数据修复）
- 4 个路点的标记数据在 `client/Assets/Resources/MapData/**`（前一片禁写，**本片已获准**：用户已批准动架构）。
- 做法：把它们搬到**同一条路线上、几何上真可达**的位置（就近找同层可走格）；⛔ 不要删路点、不要改路线拓扑。
- 判据：搬完在多层模型里**属于主分量**（给出搬前/搬后坐标 + 可达性判定）。
### 2. 修"换目标时整条路线重排 + 游标归零"（行为修复）
- 落点 `CsBotBrain` 的 `ApplyObjective` / `SetRoute`（自己定位）。现象：换目标时按"最近点重排整条路线并把游标归零" ⇒ 巡逻/长路线必然低净位移比。
- 修法自定，但**不许靠"放宽判据"实现**；改完给 `Rikk`/`Gooseman` 的净/总比前后数字。
### 3. 裁决 A1/A2 对 **Patrol 态**是否适用（量尺问题，必须给结论）
- 现判据：`净 ≥ 20m 且 净/总 ≥ 0.40`。巡逻横跨全图 ⇒ 这个量尺对 Patrol 态**天然不成立**。
- 请给一条**有依据的结论**（Patrol 该用什么量尺：例如"巡逻允许低比但必须有推进/往返不振荡"），并说明依据（原版 bot 行为口径 / 或工程内既有定义）。⛔ **不许直接把阈值降到能过** —— 要么换量尺并说清依据，要么保留阈值并把"Patrol 态不适用"写进 #67 行。

## 硬约束
1. ⛔ 不改引擎、⛔ 不改 skill。
2. 写入范围：`client/Assets/Resources/MapData/**`（仅路点数据）、`client/Assets/MapData/**`（同步）、`client/Assets/Scripts/Module/Bot/**`、`.ai-tmp/**`、`策划/差异登记.tsv` **第 68 行**。
   ⛔ 不写 `Module/Map/**`、`Module/Combat/**`、`Core/**`、`UI/**`、`Module/View/**`、`client/Assets/Editor/Views/ModelData/**`（另一片正在改）。
3. **位图 sha 必须不变**（改前改后都给 `Get-FileHash`；路点数据若与位图同文件 ⇒ 只改标记段、位图段逐字节不动）。
4. **Play 是独占资源**：`role-models-swap` 正在跑 ⇒ 进前看 `.ai-tmp/test/play-log.tsv` 末行是否 `RELEASE` 且 `play-driver-log.tsv` 近 90 秒无新行；`OCCUPY`/`RELEASE` 配对、格式 `2026-09-25 HH:mm`；⛔ 别写共用 `state.txt`。进局链路**必须补 `Clk 'urban'`**。
5. 判据 = 一条命令 + 原始输出（4 个路点搬前/搬后可达性 + `Rikk`/`Gooseman` 净比 + A1/A2/A5/A6/A7）+ 位图 sha 不变；⛔ 不填验收表、⛔ 不建覆盖矩阵、⛔ **不许下调阈值**。
6. 注释按 skill §3 第 3b 条：只写「现在是什么」。命令显式超时；超 12 分钟落盘 + 回报（第一行写模型标识）。

## 回报（`send_message` 给 `main`）
① 4 个路点搬前/搬后坐标 + 可达性判定；② 游标语义改了哪里、`Rikk`/`Gooseman` 净比前后；③ **Patrol 态量尺的裁决结论 + 依据**；④ 位图 sha 未变证据；⑤ A1/A2/A5/A6/A7 前后数字；⑥ 没做到的逐条 BLOCKED。⛔ 不要长解释。
