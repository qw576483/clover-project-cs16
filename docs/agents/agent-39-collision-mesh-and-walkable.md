# agent-39 · 机器人钻地（#76）+ 斜坡残留 4 格（#66）

> 两条都是用户报的（原话见 `策划/差异登记.tsv` 第 77 行 = #76、第 65 行 = #64/#66 段）。
> - #76 用户原话：「ai人机会钻地不是真正的地形碰撞ai吗？」
> - #66 用户原话：「斜坡会有概率卡住（点名：匪家的扶手）」（主诉已证伪：实机按住 W 能到顶、真实阻断 0 帧；**残留 4 格仍在**）

## 一、要做完的两件

### A. 钻地（#76）
根因（历史定案，逐字在 #76 行）：要判"机器人脚下的渲染面"时取不到网格 ——
`Mesh.isReadable=false`（33/33）+ Unity 6 移除了 `Mesh.Raycast`。⇒ 现在只能退到别的近似判据，观感就是"钻地"。

要做：
1. **让需要判地面的网格可读**（导入设置 `Read/Write Enabled` / `isReadable`，或改用碰撞网格 —— 选一条，写清依据）。
2. 机器人落地 / 贴地一致性用**真正的几何**判：离线断言（多帧连续采样，"脚底 y 与地面 y 的差"逐帧在容差内、
   不允许出现 y 穿到地面以下的帧）+ 一次实机观感帧（截图落 `.ai-tmp/screenshots/`）。
3. 若某处**必须**引擎侧才能解决 ⇒ 写 `引擎问题.md` 并用绕法做完（⛔ 不改引擎源码）。

### B. 斜坡残留 4 格（#66）
历史定案：`cell(27,63)` / `cell(9,102)` / `cell(10,102)` / `cell(10,109)` 这 4 格是
「**位图说可走、实际一格地面都没有**」—— 属**位图烘焙侧**的问题（不是运行时站立判定口径）。
⛔ **站立判定那套口径不归本片动**（那是 lead 的裁决项，历史已明确）。

要做：把这 4 格在**烘焙出来的可走位图**里改判为不可走（修生成器或修生成输入，写清是哪一种、依据是什么），
然后确认**全图不回归**：历史已通过的 71/71「地面挡」与 63 格矮墙/台阶沿**不许因为这次改动掉下来**。

## 二、载体与判据

- 运行时判据口径：`client/Assets/Scripts/Module/Map/CsMap.cs`（`CanStand` 两层判据 / `BodyHeightClearAt` / 9 点半径采样，约 `260-425` 行）
- 几何：`client/Assets/ThirdParty/Dust2/de_dust2_geo.bin`
- 位图 / 标记点：`client/Assets/Resources/MapData/**`（自己列一遍拿真名）；烘焙脚本自己找（`tools/probes/**` 或 Editor 下的生成器）
- 判据 = **一条命令 + 原始输出**：
  1. 4 格改判为不可走（逐格一行：格号 | 改前 | 改后）；
  2. 全图回归：71/71 地面挡 + 63 格矮墙/台阶沿（同一脚本、同一口径，改前/改后同表）；
  3. 钻地：多帧贴地断言的原始输出 + 一张实机帧。
- ⛔ 不填验收表、⛔ 不建覆盖矩阵、⛔ 不建台账。

## 三、边界

- 写入范围：`client/Assets/Scripts/Module/Map/**`、`client/Assets/Resources/MapData/**`、
  模型/网格的 `.meta`（若开了 `isReadable`）、`client/Assets/Editor/**`（位图生成器，若在那儿）、
  `.ai-tmp/**`、`策划/差异登记.tsv` 的第 65 行 / 第 77 行（只改这两行）。
- ⛔ 不改引擎源码（有缺口写 `引擎问题.md`）、⛔ 不改 skill、
  ⛔ 不写 `Module/Bot/**`（agent-38 的地盘）、⛔ 不写 `Core/**`、`UI/**`、`Module/Combat/**`、`Module/Net/**`、`Module/View/**`。
- **Play 是独占资源**：离线部分做在前面；进 Play 前先看 `.ai-tmp/test/play-driver-log.tsv` / `.ai-tmp/drivers/` 最近 2 分钟有无别人在跑，
  有 ⇒ 等或错峰，⛔ 别开第二个编辑器会话。
- 注释按 skill §3 第 3b 条：只写「现在是什么」——⛔ 不写批次 / 本轮 / 任务书 / 用户报的 / 日期 / 别的工程名。
- 命令一律显式超时；超过 12 分钟把进度落盘 + 给 `main` 一条回报。
