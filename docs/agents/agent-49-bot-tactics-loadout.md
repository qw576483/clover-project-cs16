# agent-49 · 机器人战术层剩余子项：武器偏好 / 投掷物分工（差异 r68 剩余）

## 背景：**r68 的主体已做完，只剩这两小项**
`策划/差异登记.tsv` **r68** 行里的收尾段已经写明主体成果（⛔ 别重做、别推翻）：
- 路线/计划：**4 槽位互异路线 + 互异目标点**、同难度档守点 7.5s vs 突破 1.8s、换向迟滞、起点节点修复 ⇒ 已落地；
- 寻路层：`BotNavigator.EnsurePath` 用引擎 `AStar.FindSmoothed`（**早已接好**，日志 `[A*] …（引擎 AStar.FindSmoothed，含视线拉直）`）；
- 下包：A5 实测 = 2（**旧记录"恒为 0"是分析脚本 `--since` 时间窗漏计，已更正**）；
- 护栏：`IsTrapCell`/`FloodComponentSize`/`StepsIntoTrap` + `CsBotConst.TrapComponentCells=32`，主分量占时 31%→99%；
- A6 包点驻留 61.5s、A7 守位换位 7。
- **已知缺口（不是你的活）**：A1/A2 达标 2/8，根因是**可走位图单层 2D vs 多层几何**（位图烘焙 + `Module/Map` 移动解算），⛔ 不在你写范围内，别去动。

## 你要做的两件事
1. **武器偏好**：bot 买枪/选枪要有偏好（按难度档或角色：步枪手偏 AK/M4、狙击手偏 AWP、等等）。**出处**优先从已在盘的 `原版资源/cs16src/dlls/mp.dll`（bot 逻辑）或 `原版资源/cs16src/cstrike/sprites/weapon_*.txt` + 武器表取；取不到原版口径 ⇒ **按现有工程数据定义并如实标注"自定"**（⛔ 不许谎称原版），但要在回报里写清哪些是原版、哪些是自定。
2. **投掷物分工**：bot 该用闪光/手雷/烟雾时要真的用（现在这条是空的 ⇒ 判据需从 0 到 >0）。

## 判据
- **武器偏好**：一条命令 + 原始输出 —— 一个完整回合里各 bot 实际购入/持有的武器名（能看出与角色/难度档对应）。
- **投掷物分工**：同回合里 `投掷物使用次数 > 0`（现在应为 0），并给出用的是什么、谁用的。
- 若某项客观上做不到（例如 bot 买枪链路根本不存在），**如实 BLOCKED 并写清缺什么**，⛔ 不许用"看起来对"的数字糊过去。

## 硬约束
1. ⛔ 不改引擎源码（有缺口写 `引擎问题.md`：`## 引擎缺口` / `## 引擎 bug` / `## skill 问题` 三段，用绕法做完）。
2. 写入范围：`client/Assets/Scripts/Module/Bot/**`、`.ai-tmp/**`、`策划/差异登记.tsv` **r68**（只这一行）。
   ⛔ **不写** `Module/Combat/**`（`fx-decals` 在改特效）、`Core/CsWeapons.cs`（同上）、`Module/View/**`（agent-48 在改尸体）、`Module/Map/**`、`UI/**`、`client/Assets/Editor/**`、`client/Assets/Editor/Views/ModelData/**`。
   ⚠️ `Module/Match/**` 若确需改动 ⇒ **先回报我**，那片有 agent-48 在用。
3. **Play 是独占资源，但用户已明确"可以实机验证"** ⇒ 回合数字必须来自实机（完整回合）。进前看 `.ai-tmp/test/play-log.tsv` 最近 2 分钟有无 `OCCUPY` 未释放，有 ⇒ 错峰；⛔ 别开第二个编辑器会话。
   **参考既有成果**：`.ai-tmp/test/astar-markers/`（`bot-round.cs` + `round-play.ps1` + `round-analyze.py`）是上一片跑完整回合的驱动，**优先复用**而不是新写。
4. 注释按 skill §3 第 3b 条：只写「现在是什么」——⛔ 不写批次 / 本轮 / 任务书 / 用户报的 / 日期 / 别的工程名。
5. 判据只要**一条命令 + 几行原始输出**；⛔ 不填验收表、⛔ 不建覆盖矩阵、⛔ 不建台账。
6. 命令一律显式超时；超过 12 分钟把进度落盘 + 给 `main` 一条回报（**第一行写你的模型标识**）。

## 回报（`send_message` 给 `main`）
① 改了哪些文件 + 每处一句话；② 武器偏好：回合内逐 bot 实际武器 + 出处（原版 / 自定分开标）；③ 投掷物：次数前后 + 谁用了什么；④ 没做到的逐条 BLOCKED。⛔ 不要长解释。
