# agent-14：对照表收口（把已对齐的差值改成 0，未对齐的写清现状）（clover-project-cs16）

> 项目根：`c:\Work\Server\full-dev\clover-project-cs16`
> 前几棒把**数值 / HUD / 动画 / 素材**都改完了；`策划/对照表.md` 还停留在"改之前"的对照状态。
> 本片只做**对照表的收口**（⛔ 不碰 `client/**`）。

## 0. 输入（先读）

| 文件 | 用途 |
|---|---|
| `策划/对照表.md` | 待收口的主体（G/M/N/P/T/U/W 七块 + BLOCKED 节） |
| `docs/agents/agent-11-value-and-hud-align.md` + 其回报结论 | 数值与 HUD 已改成什么（冻结 4 / 回合 105 / C4 35 / 买枪期 15 / 重力 20.32 / 跳跃 6.82 / Elite 800 / HUD 文字色 `#FFB000` / 准星 `50 250 50` / 击杀条 6 s / 比分计时搬右上） |
| `docs/agents/agent-12-character-animation.md` | 动画已实现（角色播原版序列；视模型播原版 v_ 序列；程序化动作已删） |
| `docs/agents/agent-13-asset-gaps.md` + 其回报结论 | 天空盒改本体、秒表图标已接入、SOURCES.txt 勘误、雷达 BLOCKED |
| `策划/验收表.md`（「允许的差异」小节） | 17 条差异的**权威措辞**（对照表里若引用差异，措辞要与它一致） |

## 1. 只做这四件

### 1.1 已对齐的行：差值 → `0`（保留出处）

逐块核对下列已被前几棒改过的元素，把"我们的值"更新成**代码里的现值**、差值改成 **`0`**（并在行内或紧邻注里写上"已对齐 + 改动出处 `文件:行`"）：

- **N 块（数值）**：`mp_freezetime` 4 / `mp_roundtime` 105 s / `mp_c4timer` 35 / **`mp_buytime` 15 s（新增买枪期）** / `sv_gravity`（20.32 m/s²）/ 跳跃初速（6.82 m/s）；
- **W 块（武器）**：Elite 价格 800、弹匣/备弹按 `mp.dll` `WeaponInfo[]`；
- **U 块（HUD）**：HUD 文字色 `#FFB000`、准星 `50 250 50`、击杀条 6 s、比分+计时**右上角**布局（坐标取 `原版资源/解包产物/原版HUD布局.md` 的截图量化值）、**秒表图标已实现**；
- **M 块（模型/动画）**：角色与视模型**已改为播原版序列**（出处 = `原版资源/cs16src/cs16_anim.py` + `.cs16anim` + `Editor/Views/AnimSetup.cs`）；
- **T 块（贴图）**：`T-06` 天空盒来源改成**原版本体** `gfx/env/des*.tga`（并注明与社区镜像 SHA256 全等）。

⛔ **不许删任何行**；⛔ 不许改表结构（列数、类别列口径）。差值列的取值仍限 `0` / `差 x` / `未实现`。

### 1.2 仍未对齐的行：写清现状（不许假装）

- `U-06` 雷达：仍差（`RadarSize` 未落 1080p 真值）⇒ 差值写实际差值，并在行内指到 `原版资源/解包产物/原版HUD布局.md` 的 **BLOCKED-D** 与验收表差异 #14；
- **BLOCKED-1**（HUD 血量/金钱/弹药那排绝对坐标）：前几棒已把"试过什么、卡在哪"写进 `原版HUD布局.md`——**对照表里同步这句结论**（引验收表差异 #16）；
- **BLOCKED-2**（武器伤害/射速/护甲穿透/射程/换弹/击杀奖励/每武器 maxspeed）：`原版数值表.md` §6 已写明实测结论 ⇒ 同步；
- `BLOCKED-3/4`（WAD 不可得 / 未引用 miptex）：保持，并按 `原版资源/清单.md` 核对路径仍可达。

### 1.3 引用验收表的「允许的差异」

对每一行"差值 ≠ 0"，检查它是否已在 `策划/验收表.md` 的「允许的差异」里有对应条目；
**有** ⇒ 在该行注明 `差异 #n`；**没有** ⇒ 在回报里列出来（⛔ 不许自己往验收表里加——那需要主 agent 定）。

### 1.4 复核：表里每条 `文件:行` / `文件:偏移` 仍可达

对整表做一遍可达性检查（`Test-Path` / 行号不超文件总行数），把不达标的一条一条修成真实路径或行号，
并在回报里给出"检查了多少条、修了几条"。

## 2. 判据（自己跑，原始输出贴进回报）

1. 对照表行数（分块统计）+ **差值分布**（`0` / `差` / `未实现` 各多少）；
2. 抽查 ≥5 行"原版值"重读一次（命令 + 输出原文）；
3. 全表引用可达性检查结果（总数 / 修复数 / 剩余缺失=0）；
4. `powershell -NoProfile -ExecutionPolicy Bypass -File tools\verify.ps1` 的 `reference-table` 与 `refs-reachable` 两行输出。

## 3. 不许

- ⛔ 不许改 `client/**`、`tools/**`、`docs/**`、任何 skill、`.ai-tmp/test` 历史文件；
- ⛔ 不许改 `策划/验收表.md` 与 `策划/策划案/**`（那是主 agent 的判定标准）；
- ⛔ 不许编造出处 / 不许删行 / 不许把"未实现"写成"0"；
- ⛔ 不许读别的 `clover-project-*` 工程；⛔ 不许开子 agent；
- 一次性脚本只放 `.ai-tmp/test/`，用完删；不许产出交接/进度类 md。

## 4. 回报格式

```
产出物：<文件绝对路径清单>
自检：<差值分布 + 抽查原文 + 可达性检查 + 闸门两行输出>
未决：无 / <具体条目>
```
