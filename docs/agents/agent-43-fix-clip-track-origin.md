# agent-43 · 修 `cs16_anim.py` 的 clip 轨道位置原点（#71 最后一块）

## 为什么单开这一片
`mdl-converter` 已修掉"骨骼段漏乘 scale"（`ml-out/player_T_orig.cs16anim` 根骨 `Bip01` y = **0.9926** = 米），
`modeldata-swap` 复核确认**骨骼半边已同口径**：逐骨世界坐标 Δ ≤ 0.036 m、共有 clip 四元数逐字相同。
**但 clip 那半边不行** ⇒ 还不许替换（替换 = 带病上线）。

## 判据（现成的，`modeldata-swap` 造的）
`python .ai-tmp/test/anim-pose-check.py` —— 拿**每条 clip 第 0 帧**做蒙皮、量世界包围盒 y：

| clip | 既有 `player_T`（正确口径） | 重出 `player_T_orig`（现状 = 错） |
|---|---|---|
| `idle1` | y = [0.000, 1.615]（脚贴地） | y = [**−1.055**, 0.660] |
| `walk` | y = [0.038, 1.678] | y = [**−1.123**, 0.708] |
| `crouch_idle` | y = [0.000, 1.189] | y = [**−0.833**, 0.708] |

⇒ 现象：**一播动画整个人沉进地里 ~0.83~1.12 m**。根因（`modeldata-swap` 的量化）：
根骨被搬走 —— 绑定 y `0.9926` − clip y `−0.0408` ≈ **1.03 m**。
⇒ 目标：**clip 轨道的位置与骨骼同原点** —— 修好后上表三行的 y 区间应与既有 `player_T` **同量级**（脚贴地，不再有负值）。

## 硬约束
- 只改 `原版资源/cs16src/cs16_anim.py`（+ 必要时 `cs16_build.py`），并**重出** `.ai-tmp/test/mdl-out/` 的产物。
  ⛔ **不要动 `client/Assets/**`**（替换是 `modeldata-swap` 的活，它等这一片）。
  ⛔ 不要改角色以外的口径（`w_*` 是世界枪、走 0.0254，与 clip 原点无关，**别碰**）。
- **骨骼半边已对，不要回退**：改完仍要满足"逐骨世界坐标 Δ ≤ 0.036 m + 共有 clip 四元数逐字相同"。
- 判据 = **一条命令 + 原始输出**（`anim-pose-check.py` 的三行 y 区间，改前/改后同表）+ 与既有的差值。
- 注释按 skill §3 第 3b 条：只写「现在是什么」——⛔ 不写批次 / 本轮 / 任务书 / 用户报的 / 日期 / 别的工程名。
- 全程**离线**（⛔ 不进 Play、不动资产、不碰 Unity）。
- 命令显式超时；超过 12 分钟把进度落盘 + 给 `main` 一条回报。

## 回报（send_message 给 main）
① 改了什么（`文件:行` + 一句话原理）；② `anim-pose-check.py` 改前/改后三行 y 区间 + 与既有的差值；
③ 骨骼半边是否仍满足 Δ ≤ 0.036；④ 做不到就 BLOCKED（写清卡在哪）。
