# agent-51 · 修 A1/A2：可走位图"单层 2D vs 多层几何"（用户已批准动架构）

## 背景（已知事实，别重查）
`策划/差异登记.tsv` 第 68 行（= 编号 #67）里已写明的缺口：
- 三回合实测 **A1/A2 = 2/9、3/8、2/8**（阈值 ≥6/8）。
- **根因不在 Bot 层**：可走位图是**单层 2D** 而几何是**多层** ⇒ **53 个连通分量 / 941 格非主分量（17.7%）**；另有**物理卡死**（0.5 s 位移 0.02 m，而目标格在位图里**可达**、`[A*] pathIdx=0/5`）。
- 修复域（前一片给的指引）：(a) 位图烘焙 `client/Assets/Resources/MapData/de_dust2.bytes` + `Editor/MapGen/**` 的连通性/seal；(b) 移动解算 `Module/Map/**` 的 `ResolveMove`/`TryStepUp`。
- ⚠️ **这两处正是 #66（斜坡残留 4 格）那片改过的地方** —— 改之前先读它留下的 seal 逻辑与判据，别把它的修复改回去。

## 用户已明确授权
用户本轮说"**可以去改**"（回应"要动架构"那条）⇒ 你可以动 `Resources/MapData/**`、`Editor/MapGen/**`、`Module/Map/**`。
⚠️ **引擎（`client/Packages/com.clover.unity-engine/**`）默认仍不许改**：若你判断"不改引擎做不到" ⇒ **先回报我并写清为什么**（用户口头允许改引擎，但我要先看你的因果链），同时在 `<项目根>/引擎问题.md` 记三段（`## 引擎缺口` / `## 引擎 bug` / `## skill 问题`）。

## 目标
1. **把"位图说可达、实际走不到"这一类清掉**（53 个连通分量 / 941 格非主分量的成因定位 + 修）：
   - 先判定 941 格非主分量里**哪些是真不可达**（几何上就该封）、**哪些是烘焙漏掉**（几何上能走但位图断开）；
   - 前者在位图里封掉、后者补通（⛔ 判据是几何，不是"看起来应该能走"）。
2. **物理卡死**那条（0.5 s 位移 0.02 m 但 `pathIdx=0/5`）单独定位：是 `ResolveMove`/`TryStepUp` 的问题，还是 bot 朝不可走方向推进。
3. **收尾数字**：A1/A2 **≥6/8**（⛔ 不许下调阈值）、A5 ≥1、A6/A7 不回退。

## 硬约束
1. ⛔ 不改引擎（见上，需先回报）；⛔ 不改 skill。
2. 写入范围：`client/Assets/Resources/MapData/**`、`client/Assets/MapData/**`（若两份都需同步 —— 注意 `de_dust2.bytes` **有两份副本**且历史上要求同步）、`client/Assets/Editor/MapGen/**`、`client/Assets/Scripts/Module/Map/**`、`client/Assets/Scripts/Module/Bot/**`（仅当第 2 条定位到 Bot 侧）、`.ai-tmp/**`、`策划/差异登记.tsv` **第 68 行**。
   ⛔ 不写 `Module/Combat/**`、`Core/CsWeapons.cs`、`UI/**`、`Module/View/**`。
3. **Play 是独占资源**（现在队里只有你与 agent-50，它全程离线）：进前看 `.ai-tmp/test/play-log.tsv` 末行是否 `RELEASE` 且 `play-driver-log.tsv` 近 90 秒无新行；`OCCUPY`/`RELEASE` 配对、时间格式 `2026-09-25 HH:mm`；⛔ 别写共用的 `state.txt`（用 `state-agent51.txt`）。
4. **位图改动要能回滚**：改前备份到 `.ai-tmp/test/a51-bak/`；每次重烘给**前后 sha + 逐格 diff 统计**（参考 #66 那片的做法：`W→B`/`B→W` 各几格、连通分量数、非主分量格数）。
5. 判据 = **一条命令 + 原始输出**（连通分量数前后 / 941 格分类结果 / A1/A2 五项数字）+ 必要截图；⛔ 不填验收表、⛔ 不建覆盖矩阵。
6. 注释按 skill §3 第 3b 条：只写「现在是什么」——⛔ 不写批次 / 本轮 / 任务书 / 用户报的 / 日期 / 别的工程名。
7. 命令一律显式超时；超 12 分钟把进度落盘 + 给 `main` 一条回报（**第一行写模型标识**）。

## 回报（`send_message` 给 `main`）
① 941 格非主分量的分类结果（真不可达 / 烘焙漏掉，各几格）；② 改了哪些文件 + 每处一句话；③ 位图前后 sha + 逐格 diff；④ A1/A2/A5/A6/A7 前后数字；⑤ 物理卡死的定位结论；⑥ 没做到的逐条 BLOCKED（含"是否必须改引擎"的因果链）。⛔ 不要长解释。
