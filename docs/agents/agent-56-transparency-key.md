# agent-56 · 透明键（调色板 255）未处理 —— 火焰黑底/白框 + 角色小腿靴子全黑

## 病灶（巡检片刚实测，只记录了没改）
一个疑似根因撑起两组症状，**先证实它，再修**：
1. **枪口火焰贴图**：`Art/UI/Art/fx_muzzleflash.png`（24 把共用）与 `fx_muzzleflash3.png`（m249）**把调色板索引 255 画成不透明** ⇒ m249 的十字带**一整片黑底**、其余 24 把是**能看到方形边界的白雾**。
2. **角色小腿/靴子**：近景**纯黑、无贴图多边形**（上半身迷彩正常），**9/9 兵种 × 左右脚 × idle/walk/crouch 全部一样**。
3. 巡检片的判断（**推测，未证实**）：两者同族 —— **调色板 255 / 透明键处理**。它在任务书里明确要求下一片"先确认"。

## 目标
1. **先证实或证伪"同族"这个假设**，给证据（不是"看起来像"）：
   - 查源 `.spr` 的调色板语义（255 是否为透明键）；查工程把 `.spr`/`.mdl` 贴图导出成 PNG 的那一步（生成器 `Editor/Views/ArtSetup.cs`、以及 `原版资源/cs16src/cs16_build.py` 的 `--tex-dir` 路径）**有没有把 255 写成 alpha=0**。
   - 证伪就直说（那就分两条独立 bug 各修），⛔ 别硬套一个根因。
2. **修**（以"画面正确"为准）：
   - 火焰：装上去**没有黑底/白框**，形状能看出是火焰；
   - 角色小腿/靴子：近景能看出**布料/靴子贴图**（不是纯黑块）。
3. **影响面要收住**：贴图管线是共用件（所有角色/武器贴图都走它）⇒ 修完必须**抽查**别的贴图没有被改坏（至少：一个角色上半身、一张武器贴图、一张世界道具贴图），并给出一句"X 张抽查正常 / 异常清单"。

## 硬约束
1. ⛔ 不改引擎、⛔ 不改 skill。
2. 写入范围：`client/Assets/Editor/**`（生成器/导出）、`client/Assets/Resources/UI/Art/**`、`client/Assets/Resources/Art/**`、`original 资源侧导出脚本`（`原版资源/cs16src/cs16_*.py` 若确需）、`.ai-tmp/**`、`策划/差异登记.tsv` **#89（行号 90）与 #71（行号 72）**。
   ⛔ **不写 `Module/Combat/**`、`Module/View/**`、`Core/**`**（另一片 `muzzle-geometry` 同时在做火焰落点/尺寸与悬空）。
3. **Play 是独占资源**：`muzzle-geometry` 可能同时跑 ⇒ 进前看 `.ai-tmp/test/play-log.tsv` 末行是否 `RELEASE` 且 `play-driver-log.tsv` 近 90 秒无新行；`OCCUPY`/`RELEASE` 配对、格式 `2026-09-25 HH:mm`；⛔ 别写共用 `state.txt`；进局链路**必须补 `Clk 'urban'`**。
4. **改资产前 `editor_stop`**；⛔ 绝不 `Stop-Process`；⛔ 不开第二个编辑器；`Art/**` 重烘会动一批文件 ⇒ **备份 `.ai-tmp/test/a56-bak/`**，并报 `git status` 里 `Art/**` 的改动数。
5. **判据**：修前/修后**同一机位**的火焰近景（各枪至少 m249 + 一把普通枪）+ 角色小腿近景 + 贴图放大图（能看到 alpha）+ 抽查结论一行。⛔ 不填验收表、⛔ 不建矩阵。
6. 注释按 skill §3 第 3b 条：只写「现在是什么」。命令显式超时；超 12 分钟落盘 + 回报（第一行写模型标识）。

## 回报（`send_message` 给 `main`）
① "同族"假设**证实/证伪**及证据；② 改了哪些文件 + 每处一句话；③ 修前/修后近景路径；④ 抽查结论（别的贴图有没有被改坏）；⑤ 没做到的逐条 BLOCKED。⛔ 不要长解释。
