
# ★ AI 临时目录（skill §1.8）：一次性产物 / 探针 / 驱动 / 判据脚本 / 取证截图
#   .ai-tmp/screenshots/ = 验收取证图（一次性，不进交付）
.ai-tmp/
# ★ 原版资源（skill §1.9）：下载 / 解包素材的唯一来源，不进版本库
原版资源/

# ★ Unity 工程生成物（scaffold/new-project.md §3 检查清单）
client/Library/
client/Temp/
client/Logs/
client/obj/
client/Build/
client/Builds/
client/UserSettings/
client/setting/
client/MemoryCaptures/
client/Recordings/
client/.vsconfig
client/.vs/
client/.idea/
*.csproj
*.sln
*.slnx
*.unityproj
*.userprefs

# ★ 取证截图不许进 client/Assets/**（skill §1.8 第 6 条）
#   历史目录 client/Assets/Screenshots/ 已迁 .ai-tmp/screenshots/，这里拦住它被误提交回来
client/Assets/Screenshots/

# ★ 本机引擎联调覆盖（只在本机存在，不进仓库）：
#   client/Packages/ 下的 embedded 包指向工作区里的 clover-client-unity-engine，
#   优先级高于 manifest.json 里的 git 依赖 ⇒ 本机改引擎源码即时生效、且不用联网克隆。
#   删掉这个链接 = 回到「按 manifest 从 GitHub 拉取引擎」，外人 clone 后的行为不受影响。
client/Packages/com.clover.unity-engine/

# ★ Python 字节码缓存与本地 .bak 备份（不该进版本库；探针跑一次就会生成 __pycache__）
__pycache__/
*.pyc
*.bak

# ★ 历史取证目录已删除（skill §1.8 第 6 条：证据图只许落 .ai-tmp/screenshots/）
#   2026-09-24 sink4-hygiene：client/Assets/_shots/（空目录）+ _shots.meta + 孤儿 Temp.meta 已清掉；
#   原先只拦了 Screenshots/，这里补上 _shots，拦住它被误建 / 误提交回来
client/Assets/_shots/
client/Assets/_shots.meta
