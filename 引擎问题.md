# 引擎问题（clover-client-unity-engine）

格式：`现象 | 最小复现 | 影响 | 我们的绕法 | 建议`

## 引擎缺口

- 地图格式解码器不可在编辑器侧脚本里复用 | 在编辑器里跑 `eval_file` 脚本调 `CloverMapFormat.TryDecode(bytes, out map, out err)` 与 `CloverMapData.Markers` ⇒ 编译报 `is inaccessible due to its protection level`（`CloverMapFormat` / `CloverMapData` / `CloverMapMarker` 均 `internal`） | 想"用引擎自己的解码器离线校验一份 `Resources/MapData/*.bytes`（标记点段 / 位图 / 碰撞体段）"时只能改走运行时 `Game.Map`（需要先加载地图、且必须进 Play），或按格式规范自己离线复解（判据强度低一档，且与引擎实现有漂移风险） | 本片：按 `Runtime/Presentation/MapFormat.cs` 的段布局写离线复解脚本（`.ai-tmp/test/astar-markers/marker-bake.py`），并在实机上用 `[Map] 逻辑地图已加载：… 标记点=117 名字数=13` 与 `标记点已加载（引擎 FlagMarkers 段）：13 类 / 117 点` 兜底确认 | 可加一个 `public static bool TryDecodeForTooling(byte[] data, out CloverMapData map, out string error)`（或把三个类型改 public），让 Editor 侧/工具链能用**同一个**解码器做离线自检，避免"格式改了但离线判据没跟上"

## 引擎 bug

- 本轮无

## skill 问题

- 本轮无
