# 原版对照表（CS 1.6 → clover-project-cs16）

> 本表是 skill §0.5「复刻 = 解析 + 搬运」与 §2「交付闸门」的执行形态：
> **每行 = 一个元素**，`元素 | 原版值（含出处） | 我们的值（含出处） | 差值`。
> 原版值写不出出处的行 = 该元素未验证（本表内不得出现这种行；查不到原版值的元素一律进文末 **§6 BLOCKED**）。
>
> 生成时间：2026-02-19（agent-09）；**§0 口径 / §4 HUD 块 / §5 数值块 / §6 BLOCKED-1·2 / §8 统计由 agent-10b 更新**；
> **本片（agent-14）收口**：把前几棒（agent-11 数值+HUD / agent-12 动画 / agent-13 素材）**已改完的行**的差值改成 `0`、并把"我们的值"更新成**代码现值 + 出处**；未对齐的行写清现状（雷达 U-06 / BLOCKED-1·2 同步实测结论）；
> 差值 ≠ 0 的行注明验收表「允许的差异」#n；并对整表重做了一遍 `文件:行` / `文件:偏移` 可达性复核（见 §8）。
> ⛔ **本片未改 `client/**`**（代码只读，用来取现值）。
> **agent-16 复核**：逐条核对上面这一轮收口的四项 —— ① 已改行的差值是否真为 `0` 且带出处；② 未对齐行的现状措辞；③ 差值 ≠ 0 行是否指到验收表「允许的差异」#n；④ 全表引用可达性。
> ④ 的结果：**305 条引用 = 301 条 `文件:行` / `文件:偏移`（逐个对文件总行数 / 文件大小做上界检查）+ 4 条表内已注明为 `VA` 的记法（`client.dll:0x1a21948` 等，本身不是文件偏移），0 条悬空**；§8 的行数与差值分布用同一机械口径重数，与表内数字逐格一致。
> ⚠️ **一处实测更正**：§2 的「表现类证据状态」原文（"仍是 🔄 待实机取证"）已被 agent-16 的实测推翻 —— 生成器在盘、但**运行期状态机驱动不了**，该段已改写、`策划/验收表.md` 的 **R5 / R6 已改判 ❌**。
> **所有"原版值"都由本机脚本/命令从项目内载体实测读出**，
> 复现命令见各行出处与 §7；没有任何一行是"看起来合理"填进去的。
>
> **本表由 agent-10 / agent-10b 增补**（数值与公式块 + HUD 块 + BLOCKED-1/2）：
> - 数值侧新载体与逐条对照 → `原版资源/解包产物/原版数值表.md`（cvar 出厂默认 / 武器表 `WeaponInfo[]`）；
> - HUD 侧新载体与逐条对照 → `原版资源/解包产物/原版HUD布局.md`（`client.dll` cvar / 屏幕锚点 / 截图像素量化）。

## 0. 路径简写、口径与算法

| 简写 | 实际路径（相对项目根 `clover-project-cs16`） | `Test-Path` |
|---|---|---|
| `HLSDK` | `原版资源/cs16src/hlsdk/halflife-master`（GoldSrc 官方 SDK 源码；CS 1.6 运行在同一引擎 pmove 上） | True |
| `MAP` | `原版资源/cs109/maps/de_dust2.bsp`（2,057,288 字节；与 `原版资源/cs16src/cs16game/app/cstrike/maps/de_dust2.bsp` 逐字节同源，同尺寸） | True |
| `CL` | `client/Assets` | True |

> ### ⛔ `SRC` 简写**已废除**（agent-10b 消歧，理由 = 它曾指向两份不同的 `server.cfg`）
>
> 旧写法 `SRC/...` 的**真实相对路径 = `原版资源/cs16src/cs16game/app/cstrike/...`**
> （= 官方 CS 1.6 客户端 ISO 解包产物，见 `原版资源/清单.md`）。本表已把 `SRC/` **逐处替换成真实相对路径**，
> 表内**不再存在**该简写（复核命令见 §7-H）。
>
> 项目内共有 **2 份 `server.cfg`**，身份与采信情况：
>
> | 真实相对路径 | 大小 (B) | 身份 | 是否采信为"原版" |
> |---|---|---|---|
> | `原版资源/cs16src/cs16game/app/cstrike/server.cfg` | 1,102 | **官方 CS 1.6 客户端随包附带的 dedicated `server.cfg`** | ✅ **采信**（本表 §5 的"随包覆盖值"一律引它） |
> | `原版资源/cs-docker/files/server.cfg` | 1,388 | **社区 docker 镜像配置**（`coderocker/counter-strike-docker`，见 `策划/素材调研.md` §2 第 8 行） | ⛔ **不采信**（不是原版随包；它的 `mp_freezetime "3"`（第 19 行）**不得**当原版值） |
>
> ⚠️ 另有一处**必须知道**的事实（影响"玩家实际生效"的判断）：
> `原版资源/cs16src/cs16game/app/cstrike/listenserver.cfg`（778 B，每次换图执行）里**没有** `mp_freezetime`
> / `mp_roundtime` / `mp_c4timer` / `mp_buytime`（只覆盖了 `sv_maxspeed`/`sv_accelerate`/`sv_airaccelerate`/
> `sv_wateraccelerate`/`sv_friction`）⇒ **只听服/单人开图时，这 4 个 cvar 实际生效的是 `mp.dll` 出厂默认**。
> 本表按主 agent 决策**统一以「随包 `server.cfg`」为口径**（见 §5.2），同时把两套口径逐条登记。

**单位换算**：GoldSrc 1 unit = 1 inch = **0.0254 m**（出处：`原版资源/cs16src/cs16_build.py:40` `HL_UNIT = 0.0254`）。
下表凡写「折算」= 原版 unit 值 × 0.0254。

**差值列口径**（三选一，不许留空）：
- `0` = 完全一致；
- `差 <量>` = 两者可比的同一量之差（单位不同时先折算，并标注）；
- `未实现` = 原版有、我方没有该系统/表现；
- `差（表征不同）` = 两边语义不同（例：原版是 BSP 凸体树、我方是 1 m 位图），**不可逐值相减，但必须知道它们不是同一个东西**。

**类别列**：`数值类` = 用运行时日志/脚本输出即可判定；`表现类` = 交付前必须进联络图（见 `策划/验收表.md`）。

---

## 1. 几何与碰撞（含通行性）

| # | 元素 | 原版值（出处） | 我们的值（出处） | 差值 | 类别 |
|---|---|---|---|---|---|
| G-01 | BSP 格式版本 | `30`（GoldSource BSP v30）· 出处 `MAP` 偏移 0（int32） | 同源：`CL/ThirdParty/Dust2/de_dust2.bsp`（2,057,288 字节，转成 `de_dust2_geo.bin`）· `CL/Editor/MapGen/Dust2Layout.cs:35` | 0 | 数值类 |
| G-02 | 世界包围盒 X 跨度 | `[-2816, 2048]` ⇒ 跨度 **4864 units** · 出处 `MAP` lump3 VERTICES @偏移 232,504（6,555 顶点逐点取 min/max） | `[-61.7728, 61.7728] m` ⇒ 跨度 **123.5456 m = 4864 units** · `CL/ThirdParty/Dust2/de_dust2_geo.bin` 头 worldMin/Max | **0**（跨度逐 unit 一致；仅把原点从 `-384` 平移到了地图中心 `0`） | 数值类 |
| G-03 | 世界包围盒 Z 跨度 | `[-1664, 3904]` ⇒ 跨度 **5568 units** · 同上 lump3 | `[-70.7136, 70.7136] m` ⇒ 跨度 **141.4272 m = 5568 units** · `de_dust2_geo.bin` | **0**（同上，原点平移） | 数值类 |
| G-04 | 世界高度跨度 Y | `[-192, 640]` ⇒ 跨度 **832 units = 21.1328 m** · 同上 lump3 | `[-4.8768, 16.256] m` ⇒ 跨度 **21.1328 m = 832 units** · `de_dust2_geo.bin` | **0** | 数值类 |
| G-05 | 地图平面尺寸（位图口径） | `123.5456 × 141.4272 m` 的真实包围盒（同上） | 位图 **127 × 145 格 @ 1 m** = 127 × 145 m · `CL/MapData/de_dust2.bytes` 头（width/depth） | 差 +3.45 m / +3.57 m（1 m 格向上取整所致） | 数值类 |
| G-06 | 渲染面数 | `5383` 面（BSP 多边形面，含四边与多边）· `MAP` lump7 FACES（len 107,660 ÷ 20 B/face） | 我方上场景 **9,932 三角面**（21 材质组，四边形已拆三角）· `CL/ThirdParty/Dust2/de_dust2_geo.bin`（组内 idxCount/3 累加） | 差（表征不同：原版 n 边面 / 我方纯三角，且丢掉 `sky`/`clip`/`aaatrigger` 等非实体面） | 数值类 |
| G-07 | 顶点数 | `6555` · `MAP` lump3（len 78,660 ÷ 12 B/顶点） | 21 组共 19,062 顶点（每组独立顶点，未做全局去重）· 同上 geo.bin | 差（表征不同：原版共享顶点表，我方按材质组拆分） | 数值类 |
| G-08 | 碰撞凸体数 | `8321` clipnodes（len 66,568 ÷ 8 B/node）· `MAP` lump9 CLIPNODES @510,728 | **218 个轴对齐 BoxCollider**（`Level/Blockers`）· `CL/MapData/de_dust2.bytes` 头 colliders=218 | 差（表征不同：原版是 BSP 凸体树，我方是从"近垂直且高>0.6 m 的墙面"逐格化的矩形盒） | 数值类 |
| G-09 | 碰撞平面数 | `9582` planes（len 191,640 ÷ 20 B）· `MAP` lump1 @124 | 无平面概念（用 BoxCollider + MeshCollider）· `CL/Editor/MapGen/Dust2Builder.cs:210-239` | 差（表征不同） | 数值类 |
| G-10 | 场景结构节点数（BSP 树） | `2766` nodes / `1455` leaves · `MAP` lump5 @311,164 / lump10 @191,764 | 无（我方用位图 + Physics.Raycast）· `CL/Editor/MapGen/MapBakeRunner.cs` | 差（表征不同） | 数值类 |
| G-11 | 实体块总数 | `101` 个 entity block（11 类 classname）· `MAP` lump0 ENTITIES @1,315,884（9,802 字节） | 标记点 **117 个空物体 / 13 类** · `CL/Scenes/StageDust2.unity` 的 `Level/Markers`；导出为 `CL/Resources/MapData/de_dust2_markers.bytes`（3,809 字节） | 差（原版实体≠标记点；见 G-12~G-16 逐类对账） | 数值类 |
| G-12 | T 出生点数量 | `info_player_deathmatch ×20` · `MAP` lump0 classname 统计 | `Spawn_T ×20` · geo.bin 标记表 | **0** | 数值类 |
| G-13 | CT 出生点数量 | `info_player_start ×20` · `MAP` lump0 | `Spawn_CT ×20` · geo.bin 标记表 | **0** | 数值类 |
| G-14 | 炸弹点数量 | `func_bomb_target ×2`（`target=tgt_a` / `tgt_b`）· `MAP` lump0 | `Bombsite_A` 6 点 + `Bombsite_B` 9 点（判定半径 7 m）· geo.bin + `CL/Scripts/Module/Map/ICsMap.cs:63-65` | 差（我方用点云+半径代替触发体） | 数值类 |
| G-15 | 买枪区数量 | `func_buyzone ×2`（`team=1`=T / `team=2`=CT；出处 `MAP` lump0 实体键值） | `BuyZone_T` 8 点 + `BuyZone_CT` 12 点 · geo.bin | 差（同上，点位来自该触发体覆盖的可走格） | 数值类 |
| G-16 | 可破坏木箱数 | `func_breakable ×10`（`gibmodel=models/mil_crategibs.mdl`, `health=1`）· `MAP` lump0 | 10 个木箱面并入 `box.png`/`box_x.png` 两个渲染组；**不可破坏**（无破坏逻辑） | 未实现（破坏行为） | 数值类 |
| G-17 | 光照（世界光） | `light_environment`：`_light "255 255 128 70"`、`pitch -60`、`angle 43`、`origin "-1648 -432 320"` · `MAP` lump0 | 方向光 `Quaternion.Euler(60, 43, 0)`、color `(255,255,128)`、intensity **1.75**（片X 定案，原 1.15）、Soft 阴影 0.75 · `CL/Editor/MapGen/Dust2Builder.cs:321-331` | 差（原版亮度 70×? 亮度系数与 Unity intensity 不可直比；角度 60/43 = pitch/angle 一致；**亮度这一半改为以基线图像素数字为量化出处**，见 §X-X1） | 表现类 |
| G-18 | 环境雾/环境色 | 原版无雾（BSP `MaxRange 8192`、无 fog key）· `MAP` lump0 worldspawn | 我方**新增**沙漠雾 `fogDensity 0.010`（片X 定案，原 0.0025）、`fogColor (214,197,155)` + Trilight 环境色 + `ambientIntensity 1.55`（片X 新增，原默认 1）· `Dust2Builder.cs:334-346` | 差（本项目新增，非原版；调参依据 = 基线图像素数字，见 §X-X2） | 表现类 |
| G-19 | 天空 | `worldspawn skyname "des"` · `MAP` lump0 | 6 面 `CL/ThirdParty/Dust2/Skybox/sky_{ft,bk,lf,rt,up,dn}.png` → `Skybox/6 Sided` · `CL/Editor/MapGen/Dust2Builder.cs:315-356` | **0**（来源已由 agent-13 升级为**原版本体** `原版资源/cs16src/cs16game/app/cstrike/gfx/env/des{ft,bk,lf,rt,up,dn}.tga` → PNG；见 `CL/ThirdParty/Dust2/SOURCES.txt:22-30`） | 表现类 |
| G-20 | 可走格 / 阻挡格 | 原版无位图（碰撞由 8321 clipnode 实时判定） | 位图 127×145 = 18,415 格：**可走 6,661 / 阻挡 11,754（63.8%）** · `CL/MapData/de_dust2.bytes` 位图段；与 `策划/验收表.md` P2 的 `阻挡 11754 格` 一致 | 差（表征不同） | 数值类 |
| G-21 | 通行性（T 出生点 → A 点） | 原版由 BSP 碰撞树保证连通（无 BFS 数据可引） | **可达，101 格**（BFS 最短路径）· `策划/验收表.md:95` P3 日志 | 差（表征不同；我方为自证数据） | 数值类 |
| G-22 | 通行性（T 出生点 → B 点） | 同上 | **可达，127 格** · `策划/验收表.md:95` | 差（表征不同） | 数值类 |
| G-23 | 通行性（T 出生点 → CT 出生点） | 同上 | **可达，87 格** · `策划/验收表.md:95` | 差（表征不同） | 数值类 |
| G-24 | 世界刷/木箱刷材质映射 | 44 个 miptex 名（`MAP` lump2 @1,325,688） | 归到 21 个材质组（组名 = 贴图文件名）· `de_dust2_geo.bin` | 差（见 §3 贴图对账） | 数值类 |
| G-25 | 雷达总览参数 | `overviews/de_dust2.txt:5-7`：`ZOOM 1.50`、`ORIGIN -223 1097 -192`、`ROTATED 0` | 我方雷达由 `Game.Map` 的 `Origin/Width/Depth/CellSize` 等比映射 · `CL/Scripts/UI/InGame/CsRadarWidget.cs:136-138` | 差（我方按位图等比缩放，未用原版 ZOOM/ORIGIN） | 表现类 |

---

## 2. 模型与动画数据

> 原版全部数值由本机脚本直接读 GoldSrc `studiohdr_t`（244 B 头；`boneindex/seqindex` 为**文件绝对偏移**；
> `mstudiobone_t`=112 B、`mstudioseqdesc_t`=176 B，字段偏移见 `原版资源/cs16src/cs16_build.py:61-67,195-205`），
> 复现命令见 §7-A。
>
> **agent-12/14：§2 各行的"我们的值"共用同一条链路**（逐行只写该行自己的实测值，出处不再重复整条链）：
> `原版资源/cs16src/cs16_anim.py`（从原版 mdl 导出；`PLAYER_CLIPS` / `VIEW_CLIPS` 给出"状态 → 原版 label"映射）
> → `client/Assets/Editor/Views/ModelData/*.cs16anim`（`C16A` 中间格式，读取实现 `CL/Editor/Views/Cs16AnimData.cs:117-238`）
> → `client/Assets/Editor/Views/AnimSetup.cs`（生成 `AnimationClip` + `AnimatorController`，写进 prefab）
> → 运行期 `CL/Scripts/Module/View/{ActorView.cs, ViewModelRig.cs, CsViewTuning.cs}`（`Game.Anim` 切状态）。
> ⚠️ **`.cs16anim` 的键数 = 原版帧数 + 1 个收尾键**（`cs16_anim.py:630-635`：循环回第 0 帧 / 一次性停在末帧）⇒ clip 时长 = 帧数 ÷ fps，与本表"原版值"的帧数**逐条可比**。
> ⚠️ **表现类证据状态（agent-16 实测更正）**：生成器已跑过 ⇒ `.anim` 615 / `.controller` 38 / `Art/**` 预制体 67 都在盘；
> 但**运行期状态机驱动不了**：控制器状态名带皮肤前缀（角色 `player_CT_gsg9_idle1` 等 46 条、视模型 `vm_glock18_idle` 等 7 条），
> 而 `ActorView.ResolveState` / `ViewModelRig.ResolveState` 取的是 `CsViewTuning.PState*` / `VmState*` 的**无前缀**候选
> ⇒ `Animator.HasState` 恒 False ⇒ 只有控制器的 `defaultState` 在自动循环（实测断言：`HasState('idle1')=False`、
> `cur==hash('player_CT_gsg9_idle1')? True normTime=432.8 len=4.067`）。
> ⇒ `策划/验收表.md` 的 **R5 / R6 已改判 ❌**（不是"待取证"）。
> 本表此处的 `0` = **数值/数据层已对齐**，⛔ **不等于"实机已看到播放"**。

| # | 元素 | 原版值（出处） | 我们的值（出处） | 差值 | 类别 |
|---|---|---|---|---|---|
| M-01 | 角色模型文件数 | **9** 个 `原版资源/cs16src/cs16game/app/cstrike/models/player/*/*.mdl`（terror/leet/arctic/guerilla/urban/gsg9/sas/gign/vip） | 工程内 9 个角色 prefab（`player/leet/arctic/guerilla/gsg9/sas/gign/vip`，见 `CL/Resources/Art/{T,CT}/`）+ `viewmodel_*` 29×2；`策划/验收表.md:110` R1（角色模型：原版 `player/*.mdl` 9 皮肤 → 工程 prefab） | **0**（数量一致） | 数值类 |
| M-02 | 第一人称武器模型数 | **31** 个 `原版资源/cs16src/cs16game/app/cstrike/models/v_*.mdl` | 29 个可用（`v_shield_r`/`v_knife_r` 是盾牌/右手附件，未做）· `CsWeapons` 29 把武器 → `viewmodel_<id>.prefab`；`策划/验收表.md:112` R2（原版 `v_*.mdl` 29 把 + 手臂） | 差 **−2**（原版额外有盾牌与右手附件；盾牌本身也属排除项，见 P-32 / W-25；⚠️ 本条未进验收表「允许的差异」） | 数值类 |
| M-03 | 角色骨骼数（terror.mdl） | **23** · `原版资源/cs16src/cs16game/app/cstrike/models/player/terror/terror.mdl` 头 `numbones`（偏移 140+0×4；**本片重读 = 23**） | **23** 根 Transform 层级（`Skeleton` 下逐骨骼，局部平移/旋转 = 原版绑定姿态值）· `CL/Editor/Views/AnimSetup.cs:163-180`；数据 `CL/Editor/Views/ModelData/player_T.cs16anim`（`bones=23`） | **0** | 数值类 |
| M-04 | 角色序列数（terror.mdl） | **111** 条 `mstudioseqdesc_t` · 同上 `numseq`（**本片重读 = 111**） | **46** 条 `AnimationClip` = 运行期状态机用到的全部序列（位移 6 / 死亡 3 / 站姿开枪 11 / 站姿换弹 8 / 蹲姿开枪 11 / 蹲姿换弹 7）· 导出表 `原版资源/cs16src/cs16_anim.py:86-139`（`PLAYER_CLIPS`）→ `.cs16anim` → `CL/Editor/Views/AnimSetup.cs:320-425` 生成 `AnimatorController` | 差 **−65**（原版 111 条含 9-blend 瞄准变体、双持 2 组、盾牌组、游泳/长跳与占位序列；我方只搬运行期会播的状态，blend 只取正中一路 ⇒ 差异 #10） | 数值类 |
| M-05 | `idle1` 序列节奏 | `fps 15.0 / 61 帧` · terror.mdl seq[1]（`seqindex`+1×176，`fps` @+32、`numframes` @+56；**本片重读 = fps 15.0 / 61 帧 / loop=1**） | **播 `idle1` clip**：`fps 15.0`、61 帧（clip 时长 61/15 = 4.067 s）· 导出 `原版资源/cs16src/cs16_anim.py:88` → `ModelData/player_T.cs16anim` 的 `idle1`（fps=15.0、23 轨、每轨 62 键 = 61 帧 + 1 收尾键）→ `CL/Editor/Views/AnimSetup.cs:340`（`frameRate = clipData.Fps`）→ 运行期 `CL/Scripts/Module/View/ActorView.cs:250-290` + `CL/Scripts/Module/View/CsViewTuning.cs:174` | **0** | 表现类 |
| M-06 | `walk` 序列节奏 | `fps 30.0 / 33 帧` · terror.mdl seq[3]（**本片重读一致**） | **播 `walk` clip**：`fps 30.0`、33 帧 · `.cs16anim` 的 `walk`（23 轨 × 34 键）→ 运行期状态候选 `PStateWalk` · `CL/Scripts/Module/View/CsViewTuning.cs:177` | **0** | 表现类 |
| M-07 | `run` 序列节奏 | `fps 60.0 / 37 帧` · terror.mdl seq[4]（**本片重读一致**） | **播 `run` clip**：`fps 60.0`、37 帧 · `.cs16anim` 的 `run` → 运行期状态候选 `PStateRun` · `CL/Scripts/Module/View/CsViewTuning.cs:180` | **0** | 表现类 |
| M-08 | `crouch_idle` 序列节奏 | `fps 10.0 / 31 帧` · terror.mdl seq[2]（**本片重读一致**） | **播 `crouch_idle` clip**：`fps 10.0`、31 帧（**蹲姿不再靠整体压扁** —— `CrouchScale = 1f`，见 `CL/Scripts/Module/View/CsViewTuning.cs:79`）→ 运行期状态候选 `PStateCrouchIdle` · `CL/Scripts/Module/View/CsViewTuning.cs:183` | **0** | 表现类 |
| M-09 | `jump` 序列节奏 | `fps 36.0 / 61 帧` · terror.mdl seq[6]（**本片重读：fps 36.0 / 61 帧 / 非循环**） | **播 `jump` clip**：`fps 36.0`、61 帧 → 运行期状态候选 `PStateJump` · `CL/Scripts/Module/View/CsViewTuning.cs:189` | **0** | 表现类 |
| M-10 | 死亡序列节奏 | `death1 fps30/36帧`、`death2 fps30/41帧`、`death3 fps30/106帧`、`crouch_die fps30/31帧` · terror.mdl seq[101..103,110]（**本片重读：三条 death 均 fps 30、36/41/106 帧、非循环**） | 播 `death1`/`death2`/`death3` 三条 clip（按 `actorId` 稳定取一条，播完再隐藏）· `CL/Scripts/Module/View/CsViewTuning.cs:194`（`PStateDeath`）+ `CL/Scripts/Module/View/ActorView.cs:293-312` | 差（原版 `crouch_die`（蹲姿倒地）**未进状态机**；death1..3 的 fps/帧数逐条一致；3 选 1 的取法见差异 #12） | 表现类 |
| M-11 | 射击（角色）序列节奏 | `crouch_shoot_carbine fps15/5帧`、`ref_shoot_carbine fps15/5帧`、`ref_shoot_onehanded fps15/7帧`、`head_flinch fps30/2帧`、`gut_flinch fps30/2帧` · terror.mdl seq[11,14,20,99,100]（**本片重读逐条一致**） | 播 `ref_shoot_<武器动画类>` / `crouch_shoot_<武器动画类>`（类名映射 `CL/Scripts/Module/View/CsViewTuning.cs:228-286`，类名集合取自 mdl 自身）· `CL/Scripts/Module/View/ActorView.cs:274-280` | 差 **−2**（`head_flinch` / `gut_flinch` 两条受击抖动序列**未进状态机**；射击序列本身 fps/帧数逐条一致；9-blend 只取正中一路 ⇒ 差异 #10） | 表现类 |
| M-12 | 视模型骨骼数 | `v_ak47.mdl` **42** / `v_knife.mdl` **39** · `原版资源/cs16src/cs16game/app/cstrike/models/` 头 `numbones`（**本片重读 = 42 / 39**） | **42 / 39** 根 Transform + `SkinnedMeshRenderer`（顶点 = 绑定姿态世界坐标、单骨骼权重 1.0、`bindposes` = 绑定世界矩阵的逆）· `CL/Editor/Views/AnimSetup.cs:186-236`；数据 `CL/Editor/Views/ModelData/vm_ak47.cs16anim`（`bones=42`）/ `CL/Editor/Views/ModelData/vm_knife.cs16anim`（`bones=39`） | **0** | 数值类 |
| M-13 | 视模型序列名（v_ak47） | `idle1 / reload / draw / shoot1 / shoot2 / shoot3`（6 条）· v_ak47.mdl（**本片重读：`numseq=6`，label 逐条一致**） | 7 条状态 clip：`idle←idle1`、`fire1←shoot1`、`fire2←shoot2`、`fire3←shoot3`、`firelast←shoot3`、`reload←reload`、`draw←draw`（候选表 `原版资源/cs16src/cs16_anim.py:145-162`）· `CL/Editor/Views/ModelData/vm_ak47.cs16anim` | 差 **+1**（状态数 7 vs 原版序列 6：`firelast`（弹匣打空那一发）在 AK 上**复用原版 `shoot3`**；6 条序列本身一致） | 数值类 |
| M-14 | v_ak47 序列节奏 | `idle1 fps30/17帧`、`reload fps37/91帧`、`draw fps30/31帧`、`shoot1..3 fps20/17帧` · v_ak47.mdl seq[0..5]（**本片重读逐条一致**） | **播原版 v_ 序列**：`idle fps30`、`reload fps37`、`draw fps30`、`fire1..3 fps20`、`firelast fps20`（帧数同原版；`.cs16anim` 键数 = 帧数 + 1 收尾键）· `CL/Editor/Views/ModelData/vm_ak47.cs16anim` → 运行期 `CL/Scripts/Module/View/ViewModelRig.cs`（**程序化后坐 / 换弹下沉 / 切枪抬起已删**） | **0** | 表现类 |
| M-15 | v_knife 序列节奏 | `idle fps12/151帧`、`slash1 fps30/2帧`、`slash2 fps30/2帧`、`draw fps45/46帧`、`stab fps35/47帧`、`stab_miss fps40/61帧`、`midslash1/2 fps55/66帧` · v_knife.mdl seq[0..7]（**本片重读逐条一致**） | 6 条状态 clip：`idle←idle(fps12)`、`fire1←midslash1(fps55)`、`fire2←midslash2(fps55)`、`fire3←midslash2(fps55)`、`firelast←stab(fps35)`、`draw←draw(fps45)` · `CL/Editor/Views/ModelData/vm_knife.cs16anim` | 差（原版 8 条序列里 `slash1`/`slash2`（各 2 帧横扫）与 `stab_miss`（收招）**未被状态机使用** ⇒ 这 3 条原版序列没进工程；已用的 5 条 fps/帧数逐条一致；⚠️ 本条未进验收表「允许的差异」） | 表现类 |
| M-16 | 模型内嵌贴图数 | `terror.mdl` 2 张 / `v_ak47.mdl` 11 张 / `v_knife.mdl` 4 张 · 各 mdl 头 `numtextures`（偏移 140+10×4） | 解到 `CL/Resources/Art/Tex/*.png`（242 张模型贴图，见 T-04）· `cs16_build.py:253-279` | 差（我方按 md5 去重合并，不再与 mdl 一一对应） | 数值类 |
| M-17 | 模型数据中间文件 | 原版直接是 `.mdl` 二进制 | **38** 份 `CL/Editor/Views/ModelData/*.cs16mdl`（几何/蒙皮：9 角色 + 29 视模型）+ **38** 份 `*.cs16anim`（骨骼动画，agent-12 新增；`C16A` 容器，读取实现 `CL/Editor/Views/Cs16AnimData.cs:117-238`）· 目录实测计数（38 + 38） | 差（表征不同：可读中间格式，且**几何与动画分成两份**） | 数值类 |
| M-18 | 模型空间→世界坐标换算 | GoldSrc 模型空间 前=+X / 左=+Y / 上=+Z（右手系） | `Unity = (HL.y, HL.z, HL.x)`，行列式 −1 · `cs16_build.py:131-142`（`to_unity`，含实测依据） | 0（方向依据已实测记录） | 数值类 |

---

## 3. 贴图

| # | 元素 | 原版值（出处） | 我们的值（出处） | 差值 | 类别 |
|---|---|---|---|---|---|
| T-01 | BSP 内嵌 miptex 数 | **44** 个 · `MAP` lump2 TEXTURES @1,325,688（`lump[2]` 头 u32 = 44，随后 44 个 u32 偏移表） | 解码落盘为 `CL/ThirdParty/Dust2/Textures/*.png`（26 张）· `de_dust2_geo.bin` 的 21 个材质组名 | 差（只搬运实际被面引用的贴图，未引用的 20 余张未导出） | 数值类 |
| T-02 | 主沙墙贴图尺寸（csSandWallDJ1） | `256 × 240` · `MAP` lump2 该 miptex 头（name[16] 后 u32 w,h） | `CL/ThirdParty/Dust2/Textures/csSandWallDJ1.png` = `256 × 240`（本机读 PNG IHDR 实测） | **0** | 数值类 |
| T-03 | 沙岩贴图尺寸（-0SandRock2） | `128 × 256` · `MAP` lump2 | `_0SandRock2.png` = `128 × 256`（MINUS 在文件名里改写为下划线） | **0** | 数值类 |
| T-04 | 模型内嵌贴图总数 | 38 个 mdl 内嵌贴图合计（例 terror 2 / v_ak47 11 / v_knife 4） | `CL/Resources/Art/Tex/*.png` = **243** 张 = **242 张模型内嵌贴图** + `Art/Tex` 下的 ui_white（程序化 1×1 白图，非原版）· 目录实测计数 + `client/资源欠缺清单.md:57`（`242 张` 口径 = 不含 ui_white） | 0（242 与清单口径一致） | 数值类 |
| T-05 | 地图贴图目录 | 原版编译期引用 `cs_dust.wad`（`MAP` lump0 worldspawn `wad` 键含 `cs_dust.wad`）—— ⚠️ **agent-14 实测更正：该 WAD 就在本体里**（`原版资源/cs16src/cs16game/app/cstrike/cs_dust.wad`，1,055,884 字节 / `WAD3` / 28 lumps，见 §6 BLOCKED-3） | `CL/ThirdParty/Dust2/Textures/*.png` **26 张**（11 张来自 BSP 内嵌、15 张按角色回落到同族沙漠贴图）· `CL/ThirdParty/Dust2/SOURCES.txt:14-20`（⛔ 该段"下不到 WAD"是**旧结论**，本片不许改 `client/**`） | 差（回落贴图非逐像素同源；载体其实可得 ⇒ 缺的是**搬运**不是**载体**，见 §6 BLOCKED-3/4；⚠️ **本条未进验收表「允许的差异」** ⇒ 见回报） | 表现类 |
| T-06 | 天空盒贴图 | `worldspawn skyname "des"` → `gfx/env/des{up,dn,ft,bk,rt,lf}.tga`（**原版 TGA 就在本体里**：`原版资源/cs16src/cs16game/app/cstrike/gfx/env/`，6 张各 262,188 字节、256×256×32bpp） | `CL/ThirdParty/Dust2/Skybox/sky_*.png`（6 面，源 = **原版本体那 6 张 TGA**）· `CL/ThirdParty/Dust2/SOURCES.txt:22-30` + `原版资源/清单.md` §8.1（逐张 SHA256） | **0**（TGA→PNG 转码为 RGB；且**本体那 6 张与社区镜像的同名 6 张逐字节相同** —— 6/6 SHA256 一致 ⇒ 像素不变，只是来源口径统一到本体） | 表现类 |
| T-07 | 解包留痕（可直接对账的原版贴图） | 原版 BSP 内嵌像素 | `原版资源/解包产物/tex_*.png` **10 张**（-0SandRock2 / csSandWallBTA / csSandWallDJ1 / csSandWallGH1a / djCredit1 / djCredit2 / SandCCrete2 / SandWllDoorBTA / SandWllDoorDJ1 / SandWllDoorDJ2）· 目录实测 | 差（BSP 内嵌共 44，仅 10 张留了独立解包产物） | 数值类 |
| T-08 | 材质球参数 | 原版贴图无"材质球"（GoldSrc 用 miptex 名 + lightmap） | 我方 `Standard` 材质：`color (0.79,0.65,0.42)`=#C9A66B、`_Glossiness 0.05`、`_Metallic 0` · `CL/Editor/MapGen/Dust2Builder.cs:194-198` | 差（本项目新增） | 表现类 |
| T-09 | 缺贴图兜底 | 原版缺 wad 会显示 purple/checker | 我方缺 PNG 回落到沙色并 `Debug.LogError` · `Dust2Builder.cs:188-192`；`策划/验收表.md:93` 实测"缺贴图回落到沙色材质 **0 个**" | 差（我方不静默） | 数值类 |

---

## 4. 界面元素坐标 / 尺寸 / 颜色

> **原版 HUD 数据在哪（实测确认）**：
> ① **HUD 精灵源矩形**在 `原版资源/cs16src/cs16game/app/cstrike/sprites/hud.txt`（7218 字节，文本，首行 `215` = 版本；格式 `名字 分辨率 精灵文件 x y w h`），
>    外加每把武器的 `原版资源/cs16src/cs16game/app/cstrike/sprites/weapon_<id>.txt` 与 `原版资源/cs16src/cs16game/app/cstrike/sprites/observer.txt`；
> ② **菜单/面板像素坐标**在 `原版资源/cs16src/cs16game/app/cstrike/resource/ui/*.res`（**文本** KeyValues，含 `xpos/ypos/wide/tall/cost`）与 `原版资源/cs16src/cs16game/app/cstrike/resource/clientscheme.res`（配色/字体）；
> ③ **HUD 元素在屏幕上的绝对坐标**：既不在 ① 也不在 ② —— 它由 `原版资源/cs16src/cs16game/app/cstrike/cl_dlls/client.dll` 在**运行期按屏幕尺寸**算出来
>    （屏幕宽/高全局 = `client.dll:0x1a21948` / `client.dll:0x1a2194c`（**VA；`.data` BSS 尾部，文件里读不到初值**，见 `原版资源/解包产物/原版HUD布局.md` §0.2/§2.1）；逐条证据见 `原版资源/解包产物/原版HUD布局.md` §2）。
>    `HLSDK/cl_dll/` 只有 `hud.cpp`/`hud_redraw.cpp` 等 Half-Life 客户端文件，**没有** CS 的 `hud_health/hud_ammo`。
> ④ **HUD 在屏幕上的实际像素**可由**原版实机截图**量化：`原版资源/cs16-maps/screenshots_to_conv/*.bmp`
>    （**31 张，1920×1080**，掩膜 = HUD 文字色 `(255,176,0)`）⇒ 右上角常显 HUD（比分两行 / 回合计时 / 秒表图标 / 竖线）**已解除**，见 **U-31~U-37**。
>    血量/护甲/金钱/弹药那一排**仍进 BLOCKED-1**（那 31 张是旁观机位，画面里没有这些元素）。
> ⑤ **`client.dll` 里的 HUD 侧 cvar 默认值**已解出（读法：name 串 → `push 默认值串` → `push name` → call），见 `原版资源/解包产物/原版HUD布局.md` §1。

| # | 元素 | 原版值（出处） | 我们的值（出处） | 差值 | 类别 |
|---|---|---|---|---|---|
| U-01 | HUD 数字字形尺寸（`number_0..9`） | `20 × 25` px @640 基准 · `原版资源/cs16src/cs16game/app/cstrike/sprites/hud.txt:110`（`number_0 640 640hud7 0 0 20 25`） | 我方血量字号 36 / 金钱 32 / 弹药 38（1920×1080 基准，用**字体**而非位图字形）· `CL/Scripts/UI/InGame/HudPanel.cs:110,126,132`；比分/计时字号 **16**（按原版 ink 高 12 px 反推）· `CL/Scripts/UI/InGame/CsHudTheme.cs:183`（`ScoreFontSize`） | 差（口径不同：原版 640×480 位图字形 20×25，我方 1080p 字体；**比分那一行**见 U-31） | 表现类 |
| U-02 | HUD 美元符号尺寸（`dollar`） | `18 × 25` px · `hud.txt:122` | 用普通文本 `$` + 金钱字号 32 · `CL/Scripts/UI/InGame/HudPanel.cs:126,569` | 差（同上口径；原版是位图字形 `dollar`） | 表现类 |
| U-03 | HUD 护甲图标尺寸（`suit_full`） | `24 × 24` px · `hud.txt:135` | 字形 `\U0001F6E1` 字号 26 · `CL/Scripts/UI/InGame/CsHudTheme.cs:93`、`CL/Scripts/UI/InGame/HudPanel.cs:118,555-557` | 差（同上口径；且缺字时会成方框，见 `CsHudTheme.cs` 该行注释） | 表现类 |
| U-04 | HUD 头盔图标尺寸（`suithelmet_full`） | `24 × 24` px · `hud.txt:137` | 无独立图标，用后缀 `+` · `CL/Scripts/UI/InGame/CsHudTheme.cs:95`（`HelmetMark`）、`CL/Scripts/UI/InGame/HudPanel.cs:555-557` | 差（原版是位图图标，我方用文本后缀） | 表现类 |
| U-05 | HUD 血量图标（`cross` = 医疗十字） | `24 × 24` px · `hud.txt:121` | 字形 `\u2665`（♥）· `CL/Scripts/UI/InGame/CsHudTheme.cs:91`（`HealthIcon`） | 差（我方用 ♥ 而非原版十字） | 表现类 |
| U-06 | 雷达精灵尺寸 | `128 × 128` px @640 · `hud.txt:183`（`radar 640 radar640 0 0 128 128`） | `RadarSize = 128` px，位置左上 `(24, -14)`，内层再收 3 px（内区 122） · `CL/Scripts/UI/InGame/CsHudTheme.cs`（RadarSize）、`CsRadarWidget.cs` | 差 **0**（**2026-09-21 重出**：生成器重生成 `Resources/UI/HudPanel.prefab`，Radar 节点 `m_SizeDelta` 200→128；实机 `Radar sizeDelta=128.0x128.0 screenRect x[24.0..152.0] y[938.0..1066.0]`、`canvas[[UI]] scaleFactor=1.0000 refRes=1920x1080` ⇒ 1080p 下 1:1。旧理由「640→1080p 换比率反推不出」被 `hud.txt:183` 的**分辨率档判据**取代：屏幕宽>640 即选 640 档精灵、该档精灵 1:1 不缩放 ⇒ 1080p 取 128×128） | 表现类 |
| U-07 | 准星精灵尺寸 / 绘制机制 | 精灵 `24 × 24` px @320 · `原版资源/cs16src/cs16game/app/cstrike/sprites/weapon_ak47.txt:4`（`crosshair 320 crosshairs 24 0 24 24`）；**绘制机制**（`client.dll:0x043160-0x043240`）：中心 = 屏幕宽/2、高/2（`client.dll:0x0431db-0x0431de` / `client.dll:0x0431c3-0x0431d2`），矩形高（线粗）= **1 px**（`client.dll:0x0431d0` `push 1`），臂长 = `ScreenWidth × [obj+0x44] ÷ [obj+0x6c]`（`client.dll:0x043170-0x043198`）⇒ **随分辨率缩放**；`cl_crosshair_size` 默认 `0`(auto) · `client.dll:0x0e6df8`，auto 分档判据 = `ScreenWidth > 640`（`client.dll:0x0423f6`） | 程序化 4 线 + 中心点：线长 10 / 粗 2 / 静止间隙 3 / 最大间隙 34 / 中心点 2 px · `CL/Scripts/UI/InGame/CsCrosshairWidget.cs:26-34` | 差（**不是同一机制**：原版臂长随屏幕宽比例变化，我方 10 px 定长；原版实际像素值仍 BLOCKED-D3） | 表现类 |
| U-08 | 准星颜色 | **`cl_crosshair_color` 默认 `50 250 50`**（R/G/B 十进制）· `原版资源/cs16src/cs16game/app/cstrike/cl_dlls/client.dll:0x0e6e3c`（名串 `client.dll:0x0e6e28`、注册点 `client.dll:0x041174`；读法与输出见 `原版资源/解包产物/原版HUD布局.md` §0.3/§0.6） | `Color32(0x32, 0xFA, 0x32)` = R50/G250/B50 · `CL/Scripts/UI/InGame/CsHudTheme.cs:83`（`Crosshair`） | **0**（R/G/B 逐通道一致；⛔ 原表写的 `50 255 50` 是估的，实测 `50 250 50`，agent-11 已改成 250） | 表现类 |
| U-09 | 命中标记形态 | 原版 `d_headshot` 精灵 `36 × 16` px · `hud.txt:179` | 4 条 45° 斜线拼 X：长 12 / 粗 2 / 距心 9 px；爆头红 `FF4433` / 普通白 · `CL/Scripts/UI/InGame/CsCrosshairWidget.cs:36-38` | 差（我方不用位图） | 表现类 |
| U-10 | 买枪主菜单：分类按钮排布 | 每个 `MouseOverPanelButton` `xpos 76`、`wide 148`、`tall 20`，`ypos` = 116/148/180/212/244/276/308/340/380 · `原版资源/cs16src/cs16game/app/cstrike/resource/ui/mainbuymenu.res:57-63`（pistols）等 | 我方 8 分类页签（1920×1080 基准，`CsHudTheme.CreateButton`）· `CL/Scripts/UI/InGame/BuyMenuPanel.cs`、`策划/验收表.md:58`（图 `07_buymenu.png` 8 分类页签） | 差（我方按 1080p 重排版，未逐像素对齐 640×480 版） | 表现类 |
| U-11 | 买枪主菜单：标题 | `Title xpos 76 ypos 22 wide 500 tall 48`，字体 `Title` · `mainbuymenu.res:20-31` | 面板标题（字号 22~24，`CsUiStyle`）· `BuyMenuPanel.cs` | 差（未逐像素对齐） | 表现类 |
| U-12 | 买枪子面板：条目行 | `xpos 76`、`wide 148`、`tall 20`，行距 32 px（116→148→180→212）· `原版资源/cs16src/cs16game/app/cstrike/resource/ui/buypistols_ct.res:57-58,78-79,99-100,120-121,141-142` | 我方列表行高 `CsUiStyle.RowHeight = 44` px · `CL/Scripts/UI/Flow/CsUiStyle.cs:51` | 差 +24 / 行 | 表现类 |
| U-13 | 记分板外框 | `xpos "c-260"`（居中，半宽 260）、`ypos 48`、`wide 520`、`tall 340` · `原版资源/cs16src/cs16game/app/cstrike/resource/ui/scoreboard.res:7-10` | 我方记分板（1080p 基准，自定尺寸）· `CL/Scripts/UI/InGame/ScoreboardPanel.cs` | 差（未逐像素对齐） | 表现类 |
| U-14 | 记分板行距 | `linespacing 13` px · `scoreboard.res:48` | 未声明独立行距（用统一 RowHeight 44） | 差（我方行距显著更大） | 表现类 |
| U-15 | 记分板服务器名位置 | `xpos 3 ypos 2 wide 250 tall 24` · `scoreboard.res:21-24` | 我方有玩家名列、无服务器名（单机版）· `ScoreboardPanel.cs` | 差 | 表现类 |
| U-16 | 阵营选择面板外框 | `TeamMenu xpos 76 ypos 0 wide 552 tall 448` · `原版资源/cs16src/cs16game/app/cstrike/resource/ui/teammenu.res:7-10` | `TeamSelectPanel`（1080p 基准，自定尺寸）· `CL/Scripts/UI/Flow/TeamSelectPanel.cs` | 差 | 表现类 |
| U-17 | 阵营选择按钮排布 | 6 个按钮 `xpos 0`、`wide 148`、`tall 20`，`ypos` = 116/148/180/212/244/276（T/CT/VIP/Auto/Spec/Cancel）· `原版资源/cs16src/cs16game/app/cstrike/resource/ui/teammenu.res:85-186` | 我方 5 项（CT / T / 随机 / 观察者 / 返回主菜单）· `策划/验收表.md:26`（M6） | 差（原版第 3 项是 **VIP**，我方未做）⚠️ 原表写「规格文件第 89 行已明示排除人质/VIP 模式」**与文件不符**：该文件的排除项在 `:150-156`，其中只写了「人质模式 / as_ / cs_ 地图」，**未点名 VIP**（agent-14 更正） | 表现类 |
| U-18 | 主菜单/菜单文字色 | `BaseText "255 176 0 255"`（橙 `#FFB000`）· `原版资源/cs16src/cs16game/app/cstrike/resource/clientscheme.res:24` | `CsUiStyle.Accent = #E8A33D` / `CsUiStyle.Text = #FFFFFF` · `CsUiStyle.cs:38,42` | 差（`#E8A33D` vs `#FFB000`：R −23 / G −21 / B +61） | 表现类 |
| U-19 | 菜单选中底/边框色 | `SelectionBG "255 176 0 100"`（:43）、`BorderBright/Dark "188 112 0 128"`（:76-77）、`DisabledText1 "80 48 0 255"`（:31） | 选中态 `AccentDim #6E4C18`、禁用 `(0.22 灰)` · `CL/Scripts/UI/Flow/CsUiStyle.cs:40`、`CL/Scripts/UI/InGame/CsHudTheme.cs:376`（按钮禁用色） | 差（口径不同：原版是 VGUI scheme 的背景/边框色，我方是按钮 `ColorBlock`）⚠️ 原表此处写的「分隔线 `#444444` · `CsHudTheme` 第 53 行」在 `client/Assets/Scripts` 全量 grep `444444` = **0 命中** ⇒ agent-14 按代码实况改写 | 表现类 |
| U-20 | 记分板阵营色 | `team0 "204 204 204"`（观察者）、`team1 "255 64 64"`（文件注释写 CT）、`team2 "153 204 255"`（文件注释写 T）· `clientscheme.res:81-83` | `TeamSpec #B0B0B0`、`TeamCT #6CA8FF`、`TeamT #FFA850` · `CL/Scripts/UI/InGame/CsHudTheme.cs:67-69` | 差（**注意方向相反**：原版文件把 CT 标成红 `255 64 64`、T 标成蓝 `153 204 255`；我方 CT 蓝 / T 橙） | 表现类 |
| U-21 | HUD 文字色（**已解除**） | **`(255,176,0)` = `#FFB000`** · 原版截图 `原版资源/cs16-maps/screenshots_to_conv/*.bmp` 逐像素实测（31/31 一致），并与 `原版资源/cs16src/cs16game/app/cstrike/resource/clientscheme.res:24` `BaseText "255 176 0 255"` 逐通道一致 | `TextHud = #FFB000` · `CL/Scripts/UI/InGame/CsHudTheme.cs:45`（agent-11 改；旧值 `#ECECE0` 无出处） | **0**（逐通道一致）｜⚠️ 同文件另两支色仍无原版出处：`Message #FFE080`（`:62`）/ `Danger #FF4433`（`:59`）⇒ 见 U-22 | 表现类 |
| U-22 | 血量/金钱/护甲状态色 | 只有**比分/计时**那一处能在原版截图里采到色（= `#FFB000`，见 U-21）；血量/金钱/护甲**无截图载体** ⇒ 仍 BLOCKED（见 §6 BLOCKED-1 与 `原版资源/解包产物/原版HUD布局.md` BLOCKED-D2） | 血量 `>60` 绿 `#9CFF9C` / `25~60` 黄 `#FFE164` / `<25` 红 `#FF4B3C`；护甲 `#9CD8FF`；金钱 `#86D886`（可买 `#7CFF64`）· `CL/Scripts/UI/InGame/CsHudTheme.cs:48-64`（定义）、`:481-486`（`HealthColor` 取色） | 差（原版无出处，本项目自行设计 ⇒ §6 BLOCKED-1 与 `原版HUD布局.md` BLOCKED-D2；**"色值"这一半在验收表里没有独立条目**：差异 #16 只覆盖"那排的绝对坐标"，见回报） | 表现类 |
| U-23 | 面板底色（**已复刻原版背景拼图**） | 原版菜单/读条底 = 12 块 TGA 拼图：`resource/background/800_{1,2,3}_{a,b,c,d}_loading.tga`，坐标/尺寸逐行取自 `原版资源/cs16src/cs16game/app/valve/resource/backgroundlayout.txt:3-16`（`resolution 800 600`；4 列 × 3 行；`scaled`） | 我方 = **同一套 12 张原版 TGA**（逐字节复制进 `Resources/Background/`，SHA256 与源文件一致），逐块按 `.txt` 原坐标摆放 · `UI/Flow/CsMenuBackground.cs` + `Core/ResPaths.cs` + `Editor/Flow/FlowSetup.cs`（生成期绑 sprite）；纯色 `#1B1B1B` 只作为**贴图缺失时的兜底层**保留 · `CsUiStyle.cs:38`；内容框 `#222222` / 输入底 `#2C2C2C` 不变 | **0**（12 块的设计坐标/尺寸与 `.txt` 逐行一致，运行时回算 `mismatch=0`；1920×1080 画布下 x×2.4 / y×1.8） | 表现类 |
| U-24 | 字体 | `DefaultSmall: Verdana tall 15`、`Title: Verdana Bold tall 18` · `clientscheme.res:381-394` | 引擎内置字体 `UIFactory.DefaultFont()`（Unity 6 = `LegacyRuntime.ttf`，回退系统中文字体）· `client/资源欠缺清单.md:43`（第 17 项） | 差（字体族不同：Verdana vs LegacyRuntime/系统字体） | 表现类 |
| U-25 | 面板按钮字号 | 原版菜单按钮文字走 VGUI `DefaultSmall` 15 px @640 | 流程面板按钮 24 px / 标签 22 px / 输入 22 px @1080 · `CsUiStyle.cs:149,168,359` | 差（基准不同） | 表现类 |
| U-26 | 弹药图标尺寸 | `ammo 24 × 24` px · `原版资源/cs16src/cs16game/app/cstrike/sprites/weapon_ak47.txt:3` | 我方用文本 `30 / 90`（字号 38）· `CL/Scripts/UI/InGame/HudPanel.cs:132`、`CL/Scripts/UI/InGame/CsHudTheme.cs:537`（`AmmoText`） | 差（原版是位图图标 + 位图数字，我方纯文本） | 表现类 |
| U-27 | 观战面板 | `原版资源/cs16src/cs16game/app/cstrike/resource/ui/spectator.res`、`bottomspectator.res`（VGUI 文本布局） | `SpectatorWidget`（显示"观战中"+ 目标名）· `CL/Scripts/UI/InGame/CsSpectatorWidget.cs`、`策划/验收表.md:46` | 差（未按原版 .res 对齐） | 表现类 |
| U-28 | 比赛进度条 | `原版资源/cs16src/cs16game/app/cstrike/resource/ui/csprogressbar.res` | 我方读条 `LoadingPanel`（进度由 `Game.Scene.Load` 驱动）· `策划/验收表.md:27` | 差 | 表现类 |
| U-29 | HUD 数字位数排布（`divider`/`minus`/`plus`） | `divider 2×40`、`minus 13×23`、`plus 17×23` · `hud.txt:120,123,124` | 我方金钱/血量按普通文本渲染，无独立字形 | 差 | 表现类 |
| U-30 | 买枪区提示尺寸 | `buyzone 32 × 32` 图标 · `hud.txt:131` | 底部中央文字 `Buy Zone`，字号 24，位置 `(0, 200)` · `CL/Scripts/UI/InGame/HudPanel.cs:206-209` | 差（我方用文字不用图标） | 表现类 |
| U-31 | **常显比分第 1 行**（`Counter-Terrorists : 0`） | 文本 bbox `x[1543..1750] y[38..49]` ⇒ **208 × 12 px** @1920×1080 原生 · `原版资源/cs16-maps/screenshots_to_conv/*.bmp`（31/31 完全一致；掩膜判据见 `原版资源/解包产物/原版HUD布局.md` §0.4） | **右上角、右对齐**：文案逐字 `Counter-Terrorists : N`，字号 16（ink 高按 12 px 反推），右边缘距屏右 **170** px（= 原版 ink 右缘 x=1750）、ink 上缘 y=**38**，框高 12 · `CL/Scripts/UI/InGame/HudPanel.cs:146-149,246-253`、`CL/Scripts/UI/InGame/CsHudTheme.cs:154,158,160,170,183`（agent-11 改） | **0**（位置/尺寸/文案/颜色按原版截图量化值落位；字形族见 U-24） | 表现类 |
| U-32 | **常显比分第 2 行**（`Terrorists : 0`） | `x[1626..1750] y[65..76]` ⇒ 125 × 12 px · 同上 | **右上角、右对齐**：文案 `Terrorists : N`，字号 16，右边缘距屏右 **170** px、ink 上缘 y=**65** · `CL/Scripts/UI/InGame/HudPanel.cs:148-149,256-263`、`CL/Scripts/UI/InGame/CsHudTheme.cs:156` | **0**（同上；两行数字右缘都对齐 1750 ⇒ 都用右对齐） | 表现类 |
| U-33 | **回合剩余时间文字**（`4:42`） | `x[1827..1869] y[65..76]` ⇒ **43~44 × 12 px**（宽度随数字变，5/5 暗背景图一致）· 同上 | **右上角、第 2 行右侧**：`M:SS`，字号 16，右边缘距屏右 **51** px（= 1920 − 1869）、与第 2 行同一条 y（65）· `CL/Scripts/UI/InGame/HudPanel.cs:150-151,266-273`、`CL/Scripts/UI/InGame/CsHudTheme.cs:162` | **0**（布局/字号/颜色对齐；字形族见 U-24） | 表现类 |
| U-34 | **回合计时秒表图标** | `x[1796..1816] y[58..82]` ⇒ **21 × 25 px**（彩色像素 bbox）· 同上；精灵源矩形 `stopwatch 640hud7 144 72 24 24` @640 · `hud.txt:127` | **已实现**：从原版 `640hud7.spr` 的 `144,72,24,24` 解出 → `CL/Resources/UI/Art/stopwatch.png`（24×24 RGBA），按 24×24 绘制、框右上角钉在（距屏右 **102**、上缘 y **58**）· `CL/Scripts/UI/InGame/CsHudTheme.cs:227,230,233,236,266`、`CL/Scripts/UI/InGame/HudPanel.cs:284-291,298-342`；解包脚本 `原版资源/解包产物/cs16_asset_extract.py`（agent-13） | 差（**位置已对齐、图标已落地**；但两张载体不是同一版贴图 —— 本体精灵 ink 17×22 vs 截图 ink 23×25、中心行内圈一空一实 ⇒ 逐像素不可能一致，见 `策划/验收表.md` **差异 #15**） | 表现类 |
| U-35 | **比分/计时之间的竖分隔线** | `x[1776..1777] y[27..93]` ⇒ **2 × 67 px** · 同上（5/5 一致） | **已实现**：宽 2 / 高 67 的纯色块，块右边缘距屏右 **142** px（⇒ 覆盖 x=1776..1777）、上缘 y=**27**，色 = 截图实测 `(96,58,2)` · `CL/Scripts/UI/InGame/CsHudTheme.cs:168,186,188,190,197`、`CL/Scripts/UI/InGame/HudPanel.cs:275-282,399-417`（agent-11 新增） | **0**（位置/尺寸/颜色按原版截图量化值） | 表现类 |
| U-36 | **击杀条显示时长** | `hud_deathnotice_time` 默认 **`6` 秒** · `原版资源/cs16src/cs16game/app/cstrike/cl_dlls/client.dll:0x0e77f8`（名串 `client.dll:0x0e77e0`） | `MessageLifetime = 6f` · `CL/Scripts/UI/InGame/CsHudTheme.cs:110`（agent-11 改；旧值 4f 无出处） | **0** | 数值类 |
| U-37 | **HUD 总开关** | `hud_draw` 默认 **`1`**（开）· `client.dll:0x0e3710`（名串 `client.dll:0x0e7cdc`） | **未实现**（无 `hud_draw` 开关，只有整组 `_hudRoot` 显隐）· `CL/Scripts/UI/InGame/HudPanel.cs:97-99` | 未实现（可开关性） | 数值类 |
| U-38 | **雷达类型开关** | `cl_radartype` 默认 **`0`** · `client.dll:0x0e3714`（名串 `client.dll:0x0e7c2c`） | **未实现**（只有一种雷达）· `CsRadarWidget.cs` | 未实现 | 数值类 |

> **U-31~U-35 的类别口径**：它们本质是"元素在屏幕上的位置/尺寸/颜色"= `表现类`（判据是画面），
> 但**原版那一侧的数值是脚本量出来的**（不是人眼估的），复跑命令见 `原版资源/解包产物/原版HUD布局.md` §0.5 / §7。
> **U-31~U-35 的已知限制**：那 31 张截图是**旁观机位**且**采集条件未随仓库记录**（`hud_draw` / 视频模式未知），
> 所以它们证明的是"**该采集环境下的原版 HUD 几何**"，⛔ 不能声称"等于出厂默认布局"（见 BLOCKED-1 第 3 条）。

---

## 5. 数值与公式

> 原版可引的**四类**载体（agent-10 新增 ③④）：
> **① 官方随包 cvar 配置** —— `原版资源/cs16src/cs16game/app/cstrike/server.cfg`（dedicated，**玩家实际生效**）、
> `原版资源/cs16src/cs16game/app/cstrike/listenserver.cfg`（**每次换图执行，但不含 freeze/round/c4/buy 四个 cvar**）、
> `原版资源/cs16src/cs16game/app/cstrike/settings.scr`（**Game Rules 对话框的 min/max/默认值**，文本）；
> **② GoldSrc 官方 SDK 源码**（`HLSDK/pm_shared/pm_shared.c` —— 玩家移动物理 `PM_*` 的实现，CS 1.6 跑在同一段 pmove 上）；
> **③ `mp.dll` 反汇编**（agent-10）：cvar **出厂默认**（`.data` 里静态 `cvar_t`，360 条）、武器表 `WeaponInfo[]`（25 条）；
> **④ `hw.dll.orig`/`sw.dll`/`swds.dll` 反汇编**（agent-10）：引擎 cvar 出厂默认（`sv_gravity` 等）。
> 逐条读法与偏移 → `原版资源/解包产物/原版数值表.md`。
>
> ⚠️ **两套口径**（`mp.dll` 出厂默认 vs 随包 `server.cfg` 覆盖值）**都要登记**；
> **判"我方该对齐哪套"以「玩家实际体验」为准 = 随包 `server.cfg`**（主 agent 决策，见 §5.2）。
> ⚠️ **只剩"伤害/射速/护甲穿透/部位倍率/击杀奖励/每武器 maxspeed"仍在 `mp.dll` 里写死为代码浮点常量**（不是数据表），
> 需逐武器反汇编 ⇒ 见 §6 **BLOCKED-2**（agent-10 已实测"不是一张表"）。

| # | 元素 | 原版值（出处） | 我们的值（出处） | 差值 | 类别 |
|---|---|---|---|---|---|
| N-01 | 起始金钱 `mp_startmoney` | **随包** `800` · `原版资源/cs16src/cs16game/app/cstrike/server.cfg:52`；`settings.scr:107` 默认 `800.000000`（range 800~16000）；`mp.dll` 出厂 `800` · `mp.dll:0x11bb40` | `CsConst.StartMoney = 800` · `CL/Scripts/Core/CsConst.cs:22` | **0**（三套口径同值） | 数值类 |
| N-02 | 冻结时间 `mp_freezetime` | **随包** `4` s · `原版资源/cs16src/cs16game/app/cstrike/server.cfg:51`；`settings.scr:93` 默认 `4.000000`（range 0~60）；`mp.dll` 出厂 **`6`** · `mp.dll:0x11b9c0` | `CsConst.FreezeTime = 4f` s · `CL/Scripts/Core/CsConst.cs:39`（agent-11 改，注释含两套口径） | **0**（对随包 `server.cfg` / `settings.scr`）；差 **−2**（对 `mp.dll` 出厂 6） | 数值类 |
| N-03 | 回合时长 `mp_roundtime` | **随包** `1.75` min = **105 s** · `原版资源/cs16src/cs16game/app/cstrike/server.cfg:37`；`settings.scr:86` 默认 `1.750000`（range 0~15）；`mp.dll` 出厂 **`5`** min = 300 s · `mp.dll:0x11b990` | `CsConst.RoundTime = 105f` s · `CL/Scripts/Core/CsConst.cs:55`（agent-11 改） | **0**（对随包/对话框）；差 **−195**（对出厂） | 数值类 |
| N-04 | C4 计时 `mp_c4timer` | **随包** `35` s · `原版资源/cs16src/cs16game/app/cstrike/server.cfg:43`（`settings.scr` **未收录**该 cvar）；`mp.dll` 出厂 **`45`** s · `mp.dll:0x11b9d8` | `CsConst.BombTimer = 35f` s · `CL/Scripts/Core/CsConst.cs:66`（agent-11 改） | **0**（对随包）；差 **−10**（对出厂） | 数值类 |
| N-05 | 买枪时间 `mp_buytime` | **随包** `0.25` min = **15 s** · `原版资源/cs16src/cs16game/app/cstrike/server.cfg:42`；`settings.scr:100` 默认 `0.250000`（range 0.25~）；`mp.dll` 出厂 **`1.5`** min = 90 s · `mp.dll:0x11b9a8` | **独立买枪期** `CsConst.BuyTime = 15f` s（与冻结期解耦：冻结结束后 15 s 内 + 身处 `func_buyzone` + 活着才可买）· `CL/Scripts/Core/CsConst.cs:49`（agent-11 新增）；判定 `CsRound.BuyTimeLeft` / `CsMatch.CanBuyTime` | **0**（对随包/对话框）；差 **−75 s**（对出厂） | 数值类 |
| N-06 | 友军伤害 `mp_friendlyfire` | `0`（关）· `原版资源/cs16src/cs16game/app/cstrike/server.cfg:46`；`settings.scr:140` 默认 `0`；`mp.dll` 出厂 `0` · `mp.dll:0x11b930` | `CsMatchConfig.FriendlyFire = false`（New Game 面板可改）· `CL/Scripts/Core/CsMatchConfig.cs:21` | **0** | 数值类 |
| N-07 | 脚步声 `mp_footsteps` | `1`（开）· `原版资源/cs16src/cs16game/app/cstrike/server.cfg:47`；`settings.scr:114` 默认 `1` | 有脚步/跳跃/落地音（`TickFootsteps`）· `client/资源欠缺清单.md:32`（第 6 项） | 0 | 数值类 |
| N-08 | 自动平衡 `mp_autoteambalance` | `1` · `原版资源/cs16src/cs16game/app/cstrike/server.cfg:40`；`mp.dll` 出厂 `1` · `mp.dll:0x11ba68` | 有阵营自动平衡 · `策划/验收表.md:56` G1 日志 `BalanceTeams 后 T=5 CT=5` | 0 | 数值类 |
| N-09 | 坠落伤害 `mp_falldamage` | `1`（开）· `原版资源/cs16src/cs16game/app/cstrike/server.cfg:36` | **未实现**（无坠落伤害逻辑）· `CsConst.cs:110` 只有 `MaxFallSpeed`（速度钳制，不是伤害阈值） | 未实现 | 数值类 |
| N-10 | 重力 `sv_gravity` | **随包** `800`（units/s²）· `原版资源/cs16src/cs16game/app/cstrike/server.cfg:16`；**引擎出厂** `800` · `hw.dll.orig:0x189e90`（`sw.dll`/`swds.dll` 交叉验证同值）；应用方式 `velocity[2] -= gravity*0.5*frametime`（每帧两次：`HLSDK/pm_shared/pm_shared.c:761` 与 `:782`）⇒ 等效 800 units/s² | `CsConst.Gravity = 20.32f` m/s² · `CL/Scripts/Core/CsConst.cs:109`（agent-11 改；800 × 0.0254 = 20.32） | **0**（折算后一致） | 数值类 |
| N-11 | 最大速度钳制 `sv_maxspeed` | **随包** `320` units/s · `原版资源/cs16src/cs16game/app/cstrike/server.cfg:15`（`listenserver.cfg:15` 同值）；**引擎出厂** `320` · `hw.dll.orig:0x18b208` | 无全局钳制；每武器速度 3.6~5.4 m/s · `CL/Scripts/Core/CsConst.cs:91-94`（`SpeedKnife/Pistol/Rifle/AWP`） | 差（原版全局钳制 = 8.128 m/s 折算；**每武器 `maxspeed` 仍 BLOCKED-2**；⚠️ 本条未进验收表「允许的差异」） | 数值类 |
| N-12 | 地面摩擦 `sv_friction` | **随包** `4` · `原版资源/cs16src/cs16game/app/cstrike/server.cfg:33`（`listenserver.cfg:31` 同值）；**引擎出厂** `4` · `hw.dll.orig:0x189e60`；实现见 `HLSDK/pm_shared/pm_shared.c:1202` `PM_Friction` | **未实现**（无对应常量） | 未实现 | 数值类 |
| N-13 | 地面加速度 `sv_accelerate` | **随包** `5` · `原版资源/cs16src/cs16game/app/cstrike/server.cfg:30`（`listenserver.cfg:28` 同值）；**引擎出厂 `10`** · `hw.dll.orig:0x18b220`；实现 `HLSDK/pm_shared/pm_shared.c:990` `PM_Accelerate` | **未实现** | 未实现（⛔ 注意两套口径本就不同：5 vs 10） | 数值类 |
| N-14 | 空中加速度 `sv_airaccelerate` | **随包** `10` · `原版资源/cs16src/cs16game/app/cstrike/server.cfg:31`；**引擎出厂** `10` · `hw.dll.orig:0x189f38` | **未实现** | 未实现 | 数值类 |
| N-15 | 水中加速度 `sv_wateraccelerate` | **随包** `10` · `原版资源/cs16src/cs16game/app/cstrike/server.cfg:32` | **未实现** | 未实现 | 数值类 |
| N-16 | 跳跃初速度 | `sqrt(2 * 800 * 45.0)` = **268.328 units/s** = 6.82 m/s · `HLSDK/pm_shared/pm_shared.c:2596` 与 `:2601` | `CsConst.JumpSpeed = 6.82f` m/s · `CL/Scripts/Core/CsConst.cs:102`（agent-11 改，注释写出算式与出处） | **0**（折算后一致） | 数值类 |
| N-17 | 下蹲速度倍率 | `PLAYER_DUCKING_MULTIPLIER 0.333` · `HLSDK/pm_shared/pm_shared.c:123` | `CsConst.SpeedCrouchMultiplier = 0.34` · `CL/Scripts/Core/CsConst.cs:96` | 差 **+0.007**（未对齐，⚠️ 本条未进验收表「允许的差异」） | 数值类 |
| N-18 | 长跳水平速度 | `PLAYER_LONGJUMP_SPEED 350` ×1.6 = **560 units/s** · `HLSDK/pm_shared/pm_shared.c:121` 与 `:2589` | **未实现**（无长跳模块，单机版不需要） | 未实现 | 数值类 |
| N-19 | 安全坠落速度 | `PLAYER_MAX_SAFE_FALL_SPEED 580` units/s = 14.73 m/s · `HLSDK/pm_shared/pm_shared.c:116` | `CsConst.MaxFallSpeed = -30` m/s · `CL/Scripts/Core/CsConst.cs:110` | 差（语义不同：我方是速度钳制、原版是伤害阈值；且无坠落伤害 N-09；⚠️ 本条未进验收表「允许的差异」） | 数值类 |
| N-20 | 致死坠落速度 | `PLAYER_FATAL_FALL_SPEED 1024` units/s = 26.01 m/s · `HLSDK/pm_shared/pm_shared.c:115` | **未实现** | 未实现 | 数值类 |
| N-21 | 坠落伤害系数 | `100 / (1024 - 580)` = 0.2252 /（unit/s） · `HLSDK/pm_shared/pm_shared.c:117` | **未实现** | 未实现 | 数值类 |
| N-22 | 起飞屏抖阈值 | `PLAYER_FALL_PUNCH_THRESHHOLD 350` units/s · `HLSDK/pm_shared/pm_shared.c:119` | **未实现** | 未实现 | 数值类 |
| N-23 | 弹跳最小速度 | `PLAYER_MIN_BOUNCE_SPEED 200` units/s · `HLSDK/pm_shared/pm_shared.c:118` | **未实现** | 未实现 | 数值类 |
| N-24 | 台阶高度 | `pmove->movevars->stepsize`（引擎 cvar `sv_stepsize`，默认 18 units = 0.457 m） | `CsConst.StepUpHeight = 0.45` m · `CL/Scripts/Core/CsConst.cs:111` | 差 **−0.007**（折算 18×0.0254=0.4572；⚠️ 本条未进验收表「允许的差异」） | 数值类 |
| N-25 | 蹲下高度倍率（引擎侧） | `PLAYER_DUCKING_MULTIPLIER 0.333` ⇒ 站高 72 units = 1.829 m、蹲高 36 units = 0.914 m | 站高 1.80 m、蹲高 1.25 m、眼高 1.62 m、半径 0.36 m · `CL/Scripts/Core/CsConst.cs:87-90` | 差（我方站高对齐 1.8 m 归一化口径；原版 72×0.0254=1.829；**蹲高差最大**：0.914 vs 1.25 —— 蹲姿现由原版 `crouch_idle` 序列表达，见 M-08 / `CL/Scripts/Module/View/CsViewTuning.cs:79`；⚠️ 本条未进验收表「允许的差异」） | 数值类 |

### 5.1 武器/装备价格（原版出处 = `原版资源/cs16src/cs16game/app/cstrike/resource/ui/buy*.res` 的 `cost` 字段）

| # | 元素 | 原版值（文件:行） | 我们的值（`CsWeapons.cs`） | 差值 |
|---|---|---|---|---|
| P-01 | Glock 18 | `400` · `buypistols_ct.res:72` | `400` · `CsWeapons.cs:97` | **0** |
| P-02 | USP .45 | `500` · `buypistols_ct.res:93` | `500` · `CsWeapons.cs:98` | **0** |
| P-03 | P228 | `600` · `buypistols_ct.res:114` | `600` · `CsWeapons.cs:99` | **0** |
| P-04 | Desert Eagle | `650` · `buypistols_ct.res:135` | `650` · `CsWeapons.cs:100` | **0** |
| P-05 | Five-SeveN | `750` · `buypistols_ct.res:156` | `750` · `CsWeapons.cs:101` | **0** |
| P-06 | Dual Berettas (Elite) | `800` · `mp.dll:0x10f7e8` `iCost`（旁证 `buypistols_ter.res:155`） | `800` · `CL/Scripts/Core/CsWeapons.cs:103` | **0**（agent-11 已把 `1000` 改成 `800`） |
| P-07 | TMP | `1250` · `buysubmachineguns_ct.res:72` | `1250` · `CsWeapons.cs:106` | **0** |
| P-08 | MAC-10 | `1400` · `buysubmachineguns_ter.res:72` | `1400` · `CsWeapons.cs:107` | **0** |
| P-09 | MP5 Navy | `1500` · `buysubmachineguns_ct.res:93` | `1500` · `CsWeapons.cs:105` | **0** |
| P-10 | UMP-45 | `1700` · `buysubmachineguns_ct.res:114` | `1700` · `CsWeapons.cs:108` | **0** |
| P-11 | P90 | `2350` · `buysubmachineguns_ct.res:135` | `2350` · `CsWeapons.cs:109` | **0** |
| P-12 | Galil | `2000` · `buyrifles_ter.res:72` | `2000` · `CsWeapons.cs:111` | **0** |
| P-13 | FAMAS | `2250` · `buyrifles_ct.res:72` | `2250` · `CsWeapons.cs:112` | **0** |
| P-14 | AK-47 | `2500` · `buyrifles_ter.res:93` | `2500` · `CsWeapons.cs:113` | **0** |
| P-15 | M4A1 | `3100` · `buyrifles_ct.res:115` | `3100` · `CsWeapons.cs:114` | **0** |
| P-16 | SG-552 | `3500` · `buyrifles_ter.res:136` | `3500` · `CsWeapons.cs:115` | **0** |
| P-17 | AUG | `3500` · `buyrifles_ct.res:136` | `3500` · `CsWeapons.cs:116` | **0** |
| P-18 | Scout | `2750` · `buyrifles_ct.res:93` / `buyrifles_ter.res:114` | `2750` · `CsWeapons.cs:118` | **0** |
| P-19 | AWP | `4750` · `buyrifles_ct.res:178` / `buyrifles_ter.res:158` | `4750` · `CsWeapons.cs:119` | **0** |
| P-20 | G3SG1 | `5000` · `buyrifles_ter.res:179` | `5000` · `CsWeapons.cs:120` | **0** |
| P-21 | SG-550 | `4200` · `buyrifles_ct.res:157` | `4200` · `CsWeapons.cs:121` | **0** |
| P-22 | M3 Super 90 | `1700` · `buyshotguns_ct.res:72` | `1700` · `CsWeapons.cs:123` | **0** |
| P-23 | XM1014 | `3000` · `buyshotguns_ct.res:92` | `3000` · `CsWeapons.cs:124` | **0** |
| P-24 | M249 | `5750` · `buymachineguns_ct.res:72` | `5750` · `CsWeapons.cs:126` | **0** |
| P-25 | Kevlar Vest | `650` · `buyequipment_ct.res:72` | `650` · `CsWeapons.cs:132` | **0** |
| P-26 | Kevlar + Helmet | `1000` · `buyequipment_ct.res:93` | `1000` · `CsWeapons.cs:133` | **0** |
| P-27 | Flashbang | `200` · `buyequipment_ct.res:114` | `200` · `CsWeapons.cs:129` | **0** |
| P-28 | HE Grenade | `300` · `buyequipment_ct.res:135` | `300` · `CsWeapons.cs:128` | **0** |
| P-29 | Smoke Grenade | `300` · `buyequipment_ct.res:156` | `300` · `CsWeapons.cs:130` | **0** |
| P-30 | Defusal Kit | `200` · `buyequipment_ct.res:176` | `200` · `CsWeapons.cs:134` | **0** |
| P-31 | Night Vision | `1250` · `buyequipment_ct.res:197` | **未实现**（原版有 NVG，我方 `CsWeapons` 未收录；`mp.dll` `WeaponInfo[]` 也不含它） | 未实现（⚠️ 未进验收表「允许的差异」） |
| P-32 | Tactical Shield | `2200` · `buyequipment_ct.res:218` | **未实现**（`mp.dll` `WeaponInfo[]` idx25 有它，价格 2200）· 范围依据：`策划/策划案/CS1.6单机参考规格.md:150-156`（排除项节）——⚠️ **该节实际只列了「人质模式 / as_ / cs_ 地图」等，未点名战术盾**；本条依据实为「`CsWeapons` 未收录」（见 W-25） | 未实现 |

**价格小计**（agent-14 重算）：32 行中 **30 行差值 = 0**（含 agent-11 已改的 P-06 Elite `800`）、**0 行有差**、2 行未实现（P-31 NVG / P-32 战术盾 —— ⛔ 两条都**未**进验收表「允许的差异」）。

### 5.2 两套口径总表（`mp.dll` 出厂默认 vs 随包 `server.cfg` / `settings.scr`）

> **主 agent 决策**：原版有**两套口径**，两套都登记；**判"我方该对齐哪套"以「玩家实际体验」为准 = 随包 `server.cfg` 的覆盖值**。
> 本表同时给出 `settings.scr`（Game Rules 对话框默认值）作为**第二条独立文本旁证**（它与 `server.cfg` 在 4/1.75/0.25/800 上一致）。
> 逐条偏移与读法：`原版资源/解包产物/原版数值表.md` §1（mp.dll cvar）、§1.1（与 server.cfg 的关系）、§2（引擎 cvar）。

| # | cvar | ① `mp.dll` 出厂默认 | ② 随包 `server.cfg`（**判据口径**） | ③ `settings.scr` 对话框默认 | 我们的值（出处） | 差值 | 类别 |
|---|---|---|---|---|---|---|---|
| N-26 | `mp_freezetime` | `6` s · `mp.dll:0x11b9c0` | `4` s · `server.cfg:51` | `4.000000` · `settings.scr:93` | `4` s · `CL/Scripts/Core/CsConst.cs:39` | **0**（对 ②）／差 **−2**（对 ①） | 数值类 |
| N-27 | `mp_roundtime` | `5` min = 300 s · `mp.dll:0x11b990` | `1.75` min = 105 s · `server.cfg:37` | `1.750000` · `settings.scr:86` | `105` s · `CL/Scripts/Core/CsConst.cs:55` | **0**（对 ②）／差 **−195**（对 ①） | 数值类 |
| N-28 | `mp_c4timer` | `45` s · `mp.dll:0x11b9d8` | `35` s · `server.cfg:43` | （未收录） | `35` s · `CL/Scripts/Core/CsConst.cs:66` | **0**（对 ②）／差 **−10**（对 ①） | 数值类 |
| N-29 | `mp_buytime` | `1.5` min = 90 s · `mp.dll:0x11b9a8` | `0.25` min = 15 s · `server.cfg:42` | `0.250000` · `settings.scr:100` | 独立买枪期 `15` s · `CL/Scripts/Core/CsConst.cs:49`（agent-11 新增） | **0**（对 ②）／差 **−75 s**（对 ①） | 数值类 |
| N-30 | `mp_startmoney` | `800` · `mp.dll:0x11bb40` | `800` · `server.cfg:52` | `800.000000` · `settings.scr:107` | `800` · `CL/Scripts/Core/CsConst.cs:22` | **0**（三套同值） | 数值类 |
| N-31 | `mp_limitteams` | `2` · `mp.dll:0x11ba50` | `0`（不限）· `server.cfg:41` | （未收录） | 未实现 | 未实现 | 数值类 |
| N-32 | `mp_maxrounds` | `0`（不限）· `mp.dll:0x11bb58` | `0`（不限）· `server.cfg:55` | `0.000000` · `settings.scr:79` | `CsConst.MaxRounds = 30` · `CL/Scripts/Core/CsConst.cs:59` | 差（原版不限，我方固定 30 且未接 cvar；⚠️ 本条未进验收表「允许的差异」） | 数值类 |
| N-33 | `mp_winlimit` | `0`（不限）· `mp.dll:0x11bb70` | `0`（不限）· `server.cfg:56` | `0.000000` · `settings.scr:72` | 未实现 | 未实现 | 数值类 |
| N-34 | `mp_timelimit` | `0`（不限）· `mp.dll:0x11b918` | `0`（不限）· `server.cfg:38` | `0.000000` · `settings.scr:65` | 未实现 | 未实现 | 数值类 |
| N-35 | `mp_autokick` | **`1`（开）** · `mp.dll:0x11ba08` | `0`（关）· `server.cfg:39` | **`0`** · `settings.scr:154` | 未实现 | 未实现（⚠️ ①② 冲突：按 ② 取 `0`） | 数值类 |
| N-36 | `mp_flashlight` | **`0`（关）** · `mp.dll:0x11b948` | `1`（开）· `server.cfg:45` | **`1`** · `settings.scr:168` | 未实现 | 未实现（⚠️ ①② 冲突：按 ② 取 `1`） | 数值类 |
| N-37 | `mp_tkpunish` | `0` · `mp.dll:0x11ba80` | `0` · `server.cfg:48` | `0` · `settings.scr:147` | 未实现 | 未实现 | 数值类 |
| N-38 | `mp_forcecamera` | `0` · `mp.dll:0x11bae0` | `0` · `server.cfg:50` | `0.000000` · `settings.scr:121` | 未实现 | 未实现 | 数值类 |
| N-39 | `mp_fadetoblack` | `0` · `mp.dll:0x11bba0` | `0`（`server.cfg` 未列，出厂生效） | `0` · `settings.scr:133` | 未实现 | 未实现 | 数值类 |
| N-40 | `mp_ghostfrequency` | `0.1` · `mp.dll:0x11b9f0` | （未列） | （未收录） | 未实现 | 未实现 | 数值类 |
| N-41 | `mp_mapvoteratio` | `0.66` · `mp.dll:0x11bb10` | （未列） | （未收录） | 未实现（无投票系统） | 未实现 | 数值类 |
| N-42 | `decalfrequency` | `30` · `mp.dll:0x11b960` | `60` · `server.cfg:4` | （未收录） | 未实现（无贴花系统） | 未实现 | 数值类 |
| N-43 | `sv_restartround` | `0` · `mp.dll:0x11ba20` | （未列） | （未收录） | 未实现 | 未实现 | 数值类 |

### 5.3 武器弹匣 / 备弹 / 武器 id（**agent-10 新解除**，出处 = `mp.dll` `WeaponInfo[]`）

> 出处：`原版资源/cs16src/cs16game/app/cstrike/dlls/mp.dll`，数组基址 `mp.dll:0x10f708`，步长 `0x20`。
> `iCost` = `+0x04`、`iMaxClip` = `+0x10`、`iMaxAmmo` = `+0x14`、`pszName` = `+0x1c`（字段语义的**铁证**见 `原版资源/解包产物/原版数值表.md` §0.3/§3）。
> 我方值 = `CL/Scripts/Core/CsWeapons.cs`。**24 把里 23 把的"弹匣 / 备弹"与 `mp.dll` 完全一致**（agent-11 已把 Elite 价格 `1000` → `800`：见 §5.1 P-06 / §5.3 W-07 ⇒ 现已无差值）。

| # | 元素 | 原版值（`mp.dll` `文件:偏移`） | 我方值（`CsWeapons.cs`） | 差值 | 类别 |
|---|---|---|---|---|---|
| W-01 | P228 | iId 1 / iCost 600 / iMaxClip 13 / iMaxAmmo 52 · `mp.dll:0x10f708` | 600 / 13 / 52 · `CsWeapons.cs:99` | **0** | 数值类 |
| W-02 | Glock 18 | 2 / 400 / 20 / 120 · `mp.dll:0x10f728` | 400 / 20 / 120 · `CsWeapons.cs:97` | **0** | 数值类 |
| W-03 | Scout | 3 / 2750 / 10 / 90 · `mp.dll:0x10f768` | 2750 / 10 / 90 · `CsWeapons.cs:118` | **0** | 数值类 |
| W-04 | XM1014 | 5 / 3000 / 7 / 32 · `mp.dll:0x10f788` | 3000 / 7 / 32 · `CsWeapons.cs:124` | **0** | 数值类 |
| W-05 | MAC-10 | 7 / 1400 / 30 / 100 · `mp.dll:0x10f7a8` | 1400 / 30 / 100 · `CsWeapons.cs:107` | **0** | 数值类 |
| W-06 | AUG | 8 / 3500 / 30 / 90 · `mp.dll:0x10f7c8` | 3500 / 30 / 90 · `CsWeapons.cs:116` | **0** | 数值类 |
| W-07 | Dual Berettas (Elite) | 10 / **800** / 30 / 120 · `mp.dll:0x10f7e8` | **800** / 30 / 120 · `CL/Scripts/Core/CsWeapons.cs:103` | **0**（agent-11 已把价格改成 800；弹匣/备弹本就一致） | 数值类 |
| W-08 | Five-SeveN | 11 / 750 / 20 / 100 · `mp.dll:0x10f808` | 750 / 20 / 100 · `CsWeapons.cs:101` | **0** | 数值类 |
| W-09 | UMP-45 | 12 / 1700 / 25 / 100 · `mp.dll:0x10f828` | 1700 / 25 / 100 · `CsWeapons.cs:108` | **0** | 数值类 |
| W-10 | SG-550 | 13 / 4200 / 30 / 90 · `mp.dll:0x10f848` | 4200 / 30 / 90 · `CsWeapons.cs:121` | **0** | 数值类 |
| W-11 | Galil | 14 / 2000 / 35 / 90 · `mp.dll:0x10f868` | 2000 / 35 / 90 · `CsWeapons.cs:111` | **0** | 数值类 |
| W-12 | FAMAS | 15 / 2250 / 25 / 90 · `mp.dll:0x10f888` | 2250 / 25 / 90 · `CsWeapons.cs:112` | **0** | 数值类 |
| W-13 | USP .45 | 16 / 500 / 12 / 100 · `mp.dll:0x10f8a8` | 500 / 12 / 100 · `CsWeapons.cs:98` | **0** | 数值类 |
| W-14 | AWP | 18 / 4750 / 10 / 30 · `mp.dll:0x10f8c8` | 4750 / 10 / 30 · `CsWeapons.cs:119` | **0** | 数值类 |
| W-15 | MP5 Navy | 19 / 1500 / 30 / 120 · `mp.dll:0x10f8e8` | 1500 / 30 / 120 · `CsWeapons.cs:105` | **0** | 数值类 |
| W-16 | M249 | 20 / 5750 / 100 / 200 · `mp.dll:0x10f908` | 5750 / 100 / 200 · `CsWeapons.cs:126` | **0** | 数值类 |
| W-17 | M3 Super 90 | 21 / 1700 / 8 / 32 · `mp.dll:0x10f928` | 1700 / 8 / 32 · `CsWeapons.cs:123` | **0** | 数值类 |
| W-18 | M4A1 | 22 / 3100 / 30 / 90 · `mp.dll:0x10f948` | 3100 / 30 / 90 · `CsWeapons.cs:114` | **0** | 数值类 |
| W-19 | TMP | 23 / 1250 / 30 / 120 · `mp.dll:0x10f968` | 1250 / 30 / 120 · `CsWeapons.cs:106` | **0** | 数值类 |
| W-20 | G3SG1 | 24 / 5000 / 20 / 90 · `mp.dll:0x10f988` | 5000 / 20 / 90 · `CsWeapons.cs:120` | **0** | 数值类 |
| W-21 | Desert Eagle | 26 / 650 / 7 / 35 · `mp.dll:0x10f9a8` | 650 / 7 / 35 · `CsWeapons.cs:100` | **0** | 数值类 |
| W-22 | SG-552 | 27 / 3500 / 30 / 90 · `mp.dll:0x10f9c8` | 3500 / 30 / 90 · `CsWeapons.cs:115` | **0** | 数值类 |
| W-23 | AK-47 | 28 / 2500 / 30 / 90 · `mp.dll:0x10f9e8` | 2500 / 30 / 90 · `CsWeapons.cs:113` | **0** | 数值类 |
| W-24 | P90 | 30 / 2350 / 50 / 100 · `mp.dll:0x10fa08` | 2350 / 50 / 100 · `CsWeapons.cs:109` | **0** | 数值类 |
| W-25 | 战术盾 | 99 / 2200 / — · `mp.dll:0x10fa28` | **未实现**（`mp.dll` `WeaponInfo[]` idx25 有它；范围依据 `策划/策划案/CS1.6单机参考规格.md:150-156` 排除项节 —— ⚠️ **该节实际未点名战术盾**，本条依据实为「`CsWeapons` 未收录」） | 未实现（⚠️ 未进验收表「允许的差异」） | 数值类 |
| W-26 | 同一 `pszName` 重复条目（`weapon_glock18` ×2，iId 17 与 2） | `mp.dll:0x10f748` | —（我方无此结构） | 差（表征不同；原版实测如此，未做归因） | 数值类 |
| W-27 | 买枪命令别名表（34 条） | 基址 `mp.dll:0x10f564`，步长 12 · 出处 `原版资源/解包产物/原版数值表.md` §3.1 | `CsWeapons` 用类名直呼，无别名层 | 未实现（别名层） | 数值类 |

**§5.3 小结**（agent-14 重算）：24 把里 **24 把的"价格/弹匣/备弹"三项与 `mp.dll` 完全一致**（= 每项都有 `文件:偏移` 级出处；agent-11 只改过 Elite 价格，现已归零）；
W 块 27 行里差值 ≠ 0 的只剩 **W-25**（战术盾 · 未实现）、**W-26**（原版 `pszName` 重复条目 · 表征不同）、**W-27**（买枪别名表 · 未实现）。

---

## 6. BLOCKED（原版值拿不到，**不许编**）

> 每行写清：**缺什么 / 为什么拿不到 / 谁能给 / 我方现在用的是什么**。

### BLOCKED-1（**已部分解除**）：CS 1.6 屏幕 HUD 的绝对像素坐标与 HUD 文字色

**已解除（agent-10b，出处见 `原版资源/解包产物/原版HUD布局.md`）**：
- **HUD 文字色** = `(255,176,0)` = `#FFB000`（原版截图逐像素实测，31/31 一致；与 `原版资源/cs16src/cs16game/app/cstrike/resource/clientscheme.res:24` `BaseText "255 176 0 255"` 逐通道一致）⇒ 见 **U-21**；
- **右上角常显 HUD 的绝对像素**（比分两行 `x[1543..1750] y[38..49]` / `x[1626..1750] y[65..76]`、回合计时 `x[1827..1869] y[65..76]`、秒表图标 `x[1796..1816] y[58..82]`、竖分隔线 `x[1776..1777] y[27..93]`，均 1920×1080 原生）⇒ 见 **U-31~U-35**；
- **准星颜色默认值** `cl_crosshair_color = 50 250 50`（`client.dll:0x0e6e3c`）⇒ 见 **U-08**；
- **击杀条显示时长** `hud_deathnotice_time = 6`（`client.dll:0x0e77f8`）⇒ 见 **U-36**；
- **HUD 元素的屏幕锚点机制**：屏幕宽/高全局 = `client.dll:0x1a21948` / `client.dll:0x1a2194c`（**VA；`.data` BSS 尾部，文件里读不到初值**，见 `原版资源/解包产物/原版HUD布局.md` §0.2/§2.1），元素位置在**运行期**由它们算出 ⇒ `原版资源/解包产物/原版HUD布局.md` §2.1。

**仍然 BLOCKED（缺什么 / 本次试了什么、卡在哪 / 谁能给）**：
- **缺什么**：**血量 / 护甲 / 金钱 / 弹药 / 武器名** 这 5 项在屏幕上的 `x/y`，以及它们的字号与颜色（含血量 `<25` 变红的阈值/色值）。
- **本次试过什么、卡在哪（逐条，都不是"没找"）**：
  1. 把 `client.dll` 里**屏幕宽/高全局的全部真实引用点**枚举出来（`.text` 内 116 处 `mov`，先用字节模式粗筛 78+38 处、
     再用 `A1` / `8B 05` 操作码过滤掉"立即数里恰好含这几个字节"的假阳性），逐处反汇编 ±0x30 窗口 ——
     只认出**准星绘制**（`client.dll:0x043160-0x0435xx`）与**字符串/控制台渲染**（`client.dll:0x064900-0x065100`），
     **没有任何一处呈现「`ScreenWidth − 常量` → 写入某对象字段」的自洽簇** ⇒ 未能定位这 5 项的布局函数（时间盒到点）。
  2. 用 `mp.dll` 那套静态 `cvar_t` 表读法扫 `client.dll` ⇒ **不适用**（`client.dll` 的 cvar 是函数内注册，默认值是 `.text` 立即数），
     改用「name 串 → `push` 引用 → 前一条 `push`」才解出 HUD 侧 cvar（`原版资源/解包产物/原版HUD布局.md` §0.3）。
  3. 全仓搜 VGUI `*HudLayout*` ⇒ **0 命中**（该机制是 Source / CS:GO 的，GoldSrc CS 1.6 没有）。
  4. 找原版带 HUD 的截图：`原版资源/cs16src` 内 > 100 KB 图片全是天空盒 TGA / 雷达 BMP / 主菜单背景（**无游戏内截图**）；
     `原版资源/cs16-maps/screenshots/*.jpg`（452 张 1280×1024）逐张黄字掩膜 **0 命中**（`hud_draw` 关闭）；
     唯一有 HUD 的 `原版资源/cs16-maps/screenshots_to_conv/*.bmp`（31 张 1920×1080）是**旁观机位**，
     **画面里就没有**血量/护甲/金钱/弹药 ⇒ 这 5 项**无像素可量**。
- **谁能给**：① 继续反汇编 `client.dll`（找这几排元素的 `Init`，需要新的时间盒）；
  ② **由人给一张第一人称、HUD 打开的原版截图**（任何分辨率；量化流程已在 `原版资源/解包产物/原版HUD布局.md` §0.5 就绪，可直接复用）。
- **本片（agent-14）同步的结论**（把"试过什么、卡在哪"与验收表的登记对齐）：这 5 项的坐标**在原版载体里拿不到** ——
  ① 反汇编枚举了 `client.dll` 里屏幕宽/高全局的**全部 116 处真实 `mov` 引用**并逐处反汇编 ±0x30，**没有一处呈现**「`ScreenWidth − 常量` → 写入某对象字段」的自洽簇；
  ② `hud.txt` 只给精灵源矩形、不给屏幕坐标；VGUI `*HudLayout*` 全仓 0 命中（那是 Source / CS:GO 的机制）；
  ③ 项目内唯一带 HUD 的截图（31 张 1920×1080）是**旁观机位**，画面里没有这 5 项 ⇒ **无像素可量**。
  ⇒ 结论与 `策划/验收表.md` 的 **差异 #16** 一致；逐条实测过程见 `原版资源/解包产物/原版HUD布局.md` §6「步骤 0（6 轮尝试）」与 BLOCKED-D1/D2。
- **我方实际依据**：HUD 布局是**本项目自行设计**的（`CL/Scripts/UI/InGame/HudPanel.cs:30-35` 的一整套 `Margin/HealthY/ArmorY/MoneyY`
  与各控件锚点），颜色见 `CsHudTheme.cs`。**它是"本项目新增"，不是"原版搬运"**，不许当 1:1 对齐证据。
- ⚠️ **另一条必须记住的限制**：那 31 张截图的**采集条件（`hud_draw` / 视频模式 / 是否 spectator / 版本号）未随仓库记录**
  ⇒ 它们证明的是"**该采集环境下的原版 HUD 几何**"，⛔ **不能**声称"等于 CS 1.6 出厂默认布局"。

### BLOCKED-2（**已部分解除**）：CS 1.6 武器数值（伤害 / 射速 / 护甲穿透 / 距离衰减 / 换弹时长 / 击杀奖励 / 每武器 maxspeed）

**已解除（agent-10）**：`iId` / `iCost` / `iMaxClip` / `iMaxAmmo` —— 全部有 `mp.dll:偏移`，见 **§5.3**（W-01~W-27）
与 `原版资源/解包产物/原版数值表.md` §3。
**agent-11 已按这张表逐把核对并改正 Elite 价格** ⇒ §5.3 的 W-01~W-24 现在**全部差值 = 0**（小结见 §5.3）。
**agent-14 同步**：仍缺的那 9 组字段（`Damage / ArmorPenetration / Rpm / ReloadTime / Range / FalloffPerMeter / KillReward / Spread / Recoil*`）与**每武器 `maxspeed`** 的实测结论，与 `原版资源/解包产物/原版数值表.md` **§6 BLOCKED-A / BLOCKED-B** 逐条一致 —— **它们不是数据表，而是写死在每把武器类 `PrimaryAttack` 里的浮点立即数**（已试过：全文件搜伤害序列 0 命中；`WeaponInfo[]` 只有 1 处使用者且不读伤害字段；同包 AMXX 头无对应 native），要取出必须逐把反汇编 29 把武器。
`WeaponInfo[]` 实测：基址 `mp.dll:0x10f708`、步长 `0x20`、25 条（`iCost` = `+0x04`、`iMaxClip` = `+0x10`、`iMaxAmmo` = `+0x14`、`pszName` = `+0x1c`）。

**仍然 BLOCKED（缺什么 / 本次试了什么、卡在哪 / 谁能给）**：
- **缺什么**：`CsWeapons.cs:29-42`（字段声明）与 `:95-137`（逐把赋值）里 `Damage / ArmorPenetration / Rpm / ReloadTime / Range / FalloffPerMeter /
  KillReward / Spread / Recoil*` 这 9 组的原版出处，以及每把武器的 `maxspeed`。
- **本次试过什么、卡在哪（逐条）**：
  1. **逐字节扫 `mp.dll` 找"伤害表"**：把社区口径的 24 把武器伤害序列
     `[25,32,75,20,29,32,38,20,30,70,30,30,34,115,26,32,22,32,20,80,54,33,36,21]` 以 **int32 与 float32 两种编码**、
     前缀长度 6/5/4/3 在**整个文件**里搜 ⇒ **0 命中**。
  2. **枚举 `mp.dll` 内所有结构体化数据表**：只找到 `WeaponInfo[]`（步长 0x20）、买枪别名表（`mp.dll:0x10f564`，步长 12）、
     HL 武器限制表（`mp.dll:0x11ad80`（原文写作 VA `0x1011ad80`）起）、`mp.dll:0x10f0b0` 起的 8 字节对表 ⇒ **没有任何一张表含伤害/射速/护甲穿透**。
  3. **反汇编 `WeaponInfo` 的全部使用者**：全文件搜 `call 0x100012b0` 的字节模式 ⇒ 只有 **1 处**（`mp.dll:0x6ad3f`，买枪函数），
     它只读 `+0x04`（价）与 `+0x1c`（类名），**不读任何伤害类字段**。
  4. **交叉核对同包 AMXX 头**：`原版资源/cs16src/cs16game/app/cstrike/addons/amxmodx/scripting/include/cstrike.inc` 全文读过，
     只有 `cs_get_weapon_ammo / cs_get_weapon_id / cs_get_user_bpammo` 之类，**没有"取武器伤害"的 native**
     ⇒ 说明伤害不是一张可被外部读出的表。
  5. **机制结论**：这些量在 CS 1.6 里**不是数据表，而是写死在每把武器类的 `PrimaryAttack/Fire` 里的浮点立即数**
     （同族实现 `原版资源/cs16src/hlsdk/halflife-master/dlls/weapons.cpp` 就是这个形状）。
     要取出来必须逐把定位 `LINK_ENTITY_TO_CLASS(weapon_ak47, CAK47)` → `Create` → vtable → `PrimaryAttack` → 读其中浮点常量 ——
     **29 把逐个做**，量级远超本片范围。
  6. `HLSDK/dlls/weapons.cpp` 是 **Half-Life** 的武器实现（crowbar / glock(HL) / mp5(HL) / rpg / gauss / egon / crossbow…），
     **不是 CS 的**；`HLSDK/dlls/` 下实测**不存在** `weapon_*.cpp`（`Get-ChildItem -Filter 'weapon_*.cpp'` = 0 命中）。
  7. **每武器 `maxspeed`**：`WeaponInfo` 的未识别字段 `+0x08` / `+0x0c` / `+0x18` 里**没有** 210/221/230/235/240/250/260
     这类 CS 1.6 已知 maxspeed 值 ⇒ **不在该表里**。
- **谁能给**：需要**专门开一片做逐武器反汇编**（或以 `原版资源/cs16src/hlsdk/halflife-master/dlls/weapons.cpp` 作同族参照 + 逐把核验），
  ⛔ **不允许直接拿社区表格当"原版出处"**。
- **我方实际依据**：`CL/Scripts/Core/CsWeapons.cs:95-137` 的 32 条定义，注释自称「数值对齐官方 CS 1.6 默认参数」，
  但**本表无法为这 9 组字段中的任何一条给出 `文件:偏移` 级出处** ⇒ 全部按"未验证"处理，**不许当成已搬运**。

### BLOCKED-3：编译期 WAD（`cs_dust.wad` / `halflife.wad`）里的未内嵌贴图
- **缺什么**：de_dust2 未内嵌进 BSP 的那部分墙面/地面贴图的**原始像素**。
- ⚠️ **agent-14 实测更正：本条的前提（"本项目内也没有"）不成立** —— 那 4 个 WAD **就在解包出的客户端本体里**
  （原版 `worldspawn` 的 `wad` 键指向 `\sierra\half-life\valve\{halflife.wad, decals.wad, cs_dust.wad}`，出处 `MAP` lump0）：
  - `原版资源/cs16src/cs16game/app/cstrike/cs_dust.wad` —— **1,055,884 字节 / `WAD3` / 28 lumps**
  - `原版资源/cs16src/cs16game/app/cstrike/decals.wad` —— 960,012 字节 / `WAD3` / 225 lumps
  - `原版资源/cs16src/cs16game/app/valve/halflife.wad` —— 37,914,096 字节 / `WAD3` / 3,116 lumps
  - `原版资源/cs16src/cs16game/app/valve/decals.wad` —— 924,840 字节 / `WAD3` / 222 lumps

  （读法：文件头 `'WAD3'` + `int32 numlumps` + `int32 infotableofs`；目录项 32 B、名字 @+16 共 16 B。
  实测 `cs_dust.wad` 的 28 个 lump 里**含** `SandCrtLrgSd` / `SandRoad` / `MltryCrteSd` / `SandWllWndw`
  —— 正是下面 BLOCKED-4 原先点名"未解出"的那几张。）
  `SOURCES.txt:14-20` 那句「本项目下不到该 WAD」是**旧结论，已被本次实测推翻**（该文件属 `client/**`，本片⛔不许改）。
- **本条仍登记，但口径已变**：从"载体不可得"改成"**本片按任务书「保持」不搬运**" ⇒ 我方现在用的仍是**回落贴图**，
  与"逐像素同源"的差距**没有消除**（T-05 仍是"差"）。
- **谁能给**：下一个执行者（实现动作，不在本片范围）：解析上面那 4 个 WAD3 的 miptex（768 B 调色板 + 4 级 mip，
  与 BSP 内嵌 miptex 同一格式），按 `MAP` lump2 的名字把回落贴图换成原版像素，再重跑 `dust2_build.py`。
- **我方实际依据**：按角色回落到同族沙漠贴图（`CL/ThirdParty/Dust2/Textures/*.png`，来源 `github.com/1832051637/de_dust2_scene`），
  **不是逐像素同源**。这条同时是 §4 闸门 3 的「级 ③ 兜底」登记项 —— ⚠️ **更正后"级 ① 载体可得"这一条已满足**，
  是否立刻换成级 ① 素材、以及 `SOURCES.txt` 那段旧结论怎么改（属 `client/**`）**由主 agent 定**。

### BLOCKED-4：被 `de_dust2.bsp` 引用但未能解出的 miptex 像素
- **缺什么**：`MAP` lump2 里 44 个 miptex 中，除已导出的以外还有若干张（如 `SandCrtLrgSd/SandRoad/MltryCrteSd/SandWllWndw` 等）的像素。
- **为什么拿不到（本片限制）**：本片是**纯文档**，不许改 `client/**`、也不重跑 `dust2_build.py`；
  导出这些需要重跑转换脚本并核对面的引用关系，属实现动作，不在本片范围。
  ⚠️ **agent-14 更正**：这几张（`SandCrtLrgSd` / `SandRoad` / `MltryCrteSd` / `SandWllWndw`）**已经定位到载体** ——
  就在 `原版资源/cs16src/cs16game/app/cstrike/cs_dust.wad`（`WAD3`、28 lumps、1,055,884 字节）里
  ⇒ 缺的**不是载体**，是**本片没做这次搬运**（见 BLOCKED-3 的更正表）。
- **谁能给**：下一个执行者：解析 `cs_dust.wad` / `valve/halflife.wad` / 两个 `decals.wad` 的 miptex，
  并重跑 `原版资源/解包产物/dust2_build.py`（BSP→geo.bin）即可补齐（脚本已实现 miptex 解码）。
- **我方实际依据**：现在只搬了**场景里实际被面引用**的 21 组（`de_dust2_geo.bin` 组名清单）。

### BLOCKED-5（**本片已大部分解除，仅剩一处不可证**）：`原版资源/` 各子目录的**版本身份**
- **原先以为缺什么**：`cs16-maps` / `cs_16_maps` / `de_dust2` / `de_dust2_scene` 的原始下载 URL
  （`SOURCES.txt` 只写了两个仓库名，没写 URL）。
- **本片实测结果 —— 已解除**：这 4 个目录**都是 git 克隆**，`.git/config` 里留着 `remote.origin.url`、
  `.git/logs/HEAD` 里留着 commit hash 与克隆时间 ⇒ **URL 与版本都拿到了**（4/4）。逐条见 `策划/素材调研.md` §2。
  `cs-docker` 同理（第 5 个）。
- **仍然不可证的（真 BLOCKED）**：① **历史检索的搜索关键词与搜索页 URL** —— 仓库内无任何留痕，**不可核验**；
  ② `cs16-maps` 是 **shallow + git-lfs** 克隆，**无法证明本地文件等于上游 commit 的完整快照**（需 `git lfs pull`）；
  ③ `cs-docker/maps/*.bsp` 那 9 张社区图是 docker 构建时从 Steam 拉的，**无哈希清单**。
- **谁能给**：① 当时执行的那个 agent，或由主 agent 追认；②/③ 需网络 + `git lfs pull` / 上游核对（本片不写盘，未做）。

---

## 7. 复现命令（本表所有原版值的读法）

**A. BSP / geo.bin / .bytes / mdl 的原版值**（本片一次性脚本，`<项目根>/tools/probes/probe_ref.py`，已用后删除）：

```powershell
cd clover-project-cs16
$env:PYTHONIOENCODING='utf-8'
python .ai-tmp\test\probe_ref.py bsp     # → §1 全部原版值（version/lump 偏移/entity classname/texture 尺寸/bbox/各 lump 计数）
python .ai-tmp\test\probe_ref.py geo     # → §1 我方值（cellSize/width/depth/21 组/218 盒/13 类 117 标记）
python .ai-tmp\test\probe_ref.py bytes   # → §1 我方 .bytes（CLVM v1 / 127×145 / colliders 218 / 可走 6661 / 阻挡 11754）
python .ai-tmp\test\probe_ref.py mdl     # → §2 全部原版值（骨骼数/序列数/各序列 fps 与帧数）
```

判读脚本口径（供复核）：BSP lump 计数 = `lump 长度 ÷ 结构体字节数`（planes 20 / vertices 12 / nodes 24 / faces 20 /
clipnodes 8 / leaves 28 / marksurfaces 2 / edges 4 / surfedges 4）；mdl 头字段 = `偏移 140 + i×4`，
序列字段 = `seqindex + i×176` 的 `+32(fps) / +56(numframes)`（依 `原版资源/cs16src/cs16_build.py:61-67,195-205`）。

**B. 原版 cvar / 物理常量 / 菜单坐标 / 配色**（全部 `Select-String` 直读，命令见回报"自检"段）：

```powershell
$R = 'clover-project-cs16'
$CSTRIKE = "$R\原版资源\cs16src\cs16game\app\cstrike"
Select-String "$CSTRIKE\server.cfg" -Pattern 'sv_gravity|sv_maxspeed|mp_roundtime|mp_c4timer|mp_freezetime|mp_startmoney|mp_buytime|mp_friendlyfire'
Select-String "$R\原版资源\cs16src\hlsdk\halflife-master\pm_shared\pm_shared.c" -Pattern 'PLAYER_DUCKING_MULTIPLIER|sqrt\(2 \* 800|PLAYER_MAX_SAFE_FALL_SPEED'
Select-String "$CSTRIKE\resource\ui\buy*.res" -Pattern '"fieldName"|"cost"'
Select-String "$CSTRIKE\resource\clientscheme.res" -Pattern 'BaseText|SelectionBG|team[012]'
Get-Content  "$CSTRIKE\sprites\hud.txt"
```

**C. `mp.dll` / `hw.dll.orig` 的原版值**（agent-10 产物一；代码见 `原版资源/解包产物/原版数值表.md` §0.4，已内联、不依赖已删脚本）：

```powershell
cd clover-project-cs16
$env:PYTHONIOENCODING='utf-8'
# 把 `原版资源/解包产物/原版数值表.md` §0.4 的代码另存为 ref_extract.py 后：
python ref_extract.py
# 抽查任意一行（§1 / §5.3 的偏移）：
python -c "d=open(r'原版资源\cs16src\cs16game\app\cstrike\dlls\mp.dll','rb').read();print(repr(d[0x11e974:0x11e978]), repr(d[0x1010fc3c:0x1010fc45]))"
#   → 期望输出: b'6\x00\x00\x00'  b'weapon_ak47'
```

**D. `client.dll` 的 HUD 侧原版值**（agent-10b 产物二；代码见 `原版资源/解包产物/原版HUD布局.md` §0.5）：

```powershell
cd clover-project-cs16
$env:PYTHONIOENCODING='utf-8'
# 把 `原版资源/解包产物/原版HUD布局.md` §0.5 的代码另存为 hud_verify.py 后：
python hud_verify.py
# 抽查偏移：
python -c "d=open(r'原版资源\cs16src\cs16game\app\cstrike\cl_dlls\client.dll','rb').read();print(d[0x0e6e3c:0x0e6e48], d[0x0e6e28:0x0e6e3c])"
#   → 期望输出: b'50 250 50\x00\x00\x00'  b'cl_crosshair_color\x00\x00'
```

**E. `settings.scr`（Game Rules 默认值）与两份 `server.cfg` 的区分**：

```powershell
$R = 'clover-project-cs16'
Select-String "$R\原版资源\cs16src\cs16game\app\cstrike\settings.scr" -Pattern 'mp_roundtime|mp_freezetime|mp_buytime|mp_startmoney|mp_autokick|mp_flashlight' -Context 0,5
Select-String "$R\原版资源\cs16src\cs16game\app\cstrike\server.cfg"  -Pattern 'mp_freezetime|mp_roundtime|mp_c4timer|mp_buytime|mp_startmoney'
Select-String "$R\原版资源\cs-docker\files\server.cfg"                -Pattern 'mp_freezetime'
#   → 采信前两者（官方随包）；cs-docker 那份是社区 docker 配置，⛔ 不采信
```

**F. 原版 HUD 截图的像素量化**（agent-10b 产物二；代码见 `原版资源/解包产物/原版HUD布局.md` §0.5 的 `[C]` 段）：

```powershell
$R = 'clover-project-cs16'
# 掩膜 = 精确色 (255,176,0)±2；对 31 张 1920x1080 BMP 全量跑（脚本见上）
# 人眼复核：把 `.ai-tmp/test/hud_zoom_*.png`（放大 3 倍的右上角裁切）打开对比
```

**G. 本表 §1~§3 的原版值**：见 §7-A（`probe_ref.py`，已删；口径写在 §7-A 下方）。

**H. 「表内已无 `SRC/` 简写」的复核命令**（判据 4）：

```powershell
cd clover-project-cs16
(Select-String -Path '策划\对照表.md' -Pattern 'SRC' -CaseSensitive).Count      # 期望 0
Select-String -Path '策划\对照表.md' -Pattern 'SRC' -CaseSensitive             # 期望无输出
Test-Path '原版资源\cs16src\cs16game\app\cstrike\server.cfg'                     # True
Test-Path '原版资源\cs-docker\files\server.cfg'                                  # True（社区配置，不采信）
Test-Path '原版资源\cs16src\cs16game\app\cstrike\listenserver.cfg'               # True
Test-Path '原版资源\cs16src\cs16game\app\cstrike\settings.scr'                   # True
Test-Path '原版资源\cs16src\cs16game\app\cstrike\cl_dlls\client.dll'             # True
Test-Path '原版资源\cs16src\cs16game\app\cstrike\dlls\mp.dll'                    # True
```

---

## 8. 本表统计

> **重算说明**：本表由 agent-10b 用 `原版资源/解包产物/原版HUD布局.md` §0.5 同源的解析口径逐行重新数过
> （不靠人眼估）；行数 = 以 `行首 id` 为准（`G-`/`M-`/`T-`/`U-`/`N-`/`P-`/`W-`），
> 差值分类 = 「含`未实现`⇒未实现 / 含`差`⇒差 x / 其余⇒0」（**agent-14 沿用同一条机械口径重数**，见下表）。
> **agent-16 复核用同一条机械口径（脚本按「行首 id」+ 表头定位 `差值` 列）重数：192 行 = 89 `0` / 74 `差` / 29 `未实现`，
> 分块 25 / 18 / 9 / 38 / 25 / 32 / 18 / 27 —— 与下表逐格一致；差值 ≠ 0 且**没有**「差异 #n」指引的 97 行已在回报里逐条列出（⛔ 未自行往验收表加条目）。**
>
> ⚠️ **口径提醒（agent-14 加）**：判据口径 = **随包 `server.cfg`**（§5.2 的 ② 列）。有 **8 行**是
> "**判据口径已经是 `0`、但行内同时登记了另一口径（`mp.dll` 出厂/对话框）的差**"
> （N-02 / N-03 / N-04 / N-05 / N-26 / N-27 / N-28 / N-29 —— 这 8 行的"我们的值"现在就是随包值），
> 按上面的机械口径它们被计入 **"差"列**。另有 2 行是"判据口径下 `0`、另两个口径同值"（N-01 / N-30）。
> ⇒ **真正"判据口径下仍不为 0"的差行**见本节末的处置清单，**不是**上表"差 x"那一列的全部。

| 块 | 行数 | 差值 = 0 | 差 x | 未实现 |
|---|---|---|---|---|
| §1 几何与碰撞（含通行性） | 25 | 7 | 17 | 1 |
| §2 模型与动画数据 | 18 | 10 | 8 | 0 |
| §3 贴图 | 9 | 4 | 5 | 0 |
| §4 界面元素坐标/尺寸/颜色（U-01~U-38 共 38 行） | 38 | 7 | 29 | 2 |
| §5 数值与公式（N-01~N-25 共 25 行） | 25 | 6 | 9 | 10 |
| §5.1 武器/装备价格（P-01~P-32 共 32 行） | 32 | 30 | 0 | 2 |
| §5.2 两套口径（N-26~N-43 共 18 行） | 18 | 1 | 5 | 12 |
| §5.3 武器弹匣/备弹（W-01~W-27 共 27 行） | 27 | 24 | 1 | 2 |
| **合计** | **192** | **89** | **74** | **29** |

**BLOCKED 小节 5 条**：
BLOCKED-1 HUD 屏幕绝对坐标与 HUD 文字色（**已部分解除**：文字色 + 右上角常显 HUD 的像素 + 准星色 + 击杀条时长 + 屏幕锚点机制；
仍缺血量/护甲/金钱/弹药等 5 项的坐标与色）· BLOCKED-2 CS 武器数值表（`mp.dll` 二进制；
**已部分解除**：`iId/iCost/iMaxClip/iMaxAmmo`；仍缺伤害/射速/护甲穿透/射程/换弹/击杀奖励/每武器 maxspeed）·
BLOCKED-3 编译期 WAD 里的未内嵌贴图（⚠️ **agent-14 实测更正前提**：`cs_dust.wad` / `halflife.wad` / 两个 `decals.wad`
**都在解包出的客户端本体里** ⇒ 不再算"载体不可得"，本片按任务书"保持"、**只是没搬运**；依据脚本 `原版资源/解包产物/dust2_build.py` 仍可达）·
BLOCKED-4 未解出的 miptex 像素（那 4 张已定位在 `cs_dust.wad`；`dust2_build.py` 在项目内、可重跑）·
BLOCKED-5 已**大部分解除**（5 个子目录的 URL/commit 已从 `.git/config` 拿到），
仅剩"历史检索关键词"与"shallow+LFS 的完整性"两处不可证。

> 说明：**"差"不等于"错"** —— 大量"差（表征不同）"两行是**两边语义本身就不同**
> （BSP 凸体树 vs 1 m 位图、原版 640×480 位图字形 vs 我方 1080p 字体）。
> **agent-14 收口结果**：上面这一批"同量可比却对不上"的，**在判据口径（随包 `server.cfg`）下已全部归零** ——
> N-02/N-26 冻结 4 s、N-03/N-27 回合时长 105 s、N-04/N-28 C4 计时 35 s、N-05/N-29 新增独立买枪期 15 s、
> N-10 重力 20.32、N-16 跳跃初速 6.82、P-06 / W-07 Elite 价格 800、U-08 准星 `50 250 50`、
> U-21 HUD 文字色 `#FFB000`、U-31~U-33 比分/计时（已搬到右上角）、U-34 秒表图标（已实现）、
> U-35 竖分隔线（已实现）、U-36 击杀条 6 s。
> **现在"差"里仍需处置的只剩这些**（逐条已注明是否进验收表）：
> N-11 `sv_maxspeed`、N-17 蹲速倍率、N-19 坠落速度（语义不同）、N-24 台阶高、N-25 站/蹲高口径、N-32 `mp_maxrounds`；
> M-02（盾牌/右手附件未做）、M-10（`crouch_die` 未用）、M-11（`head/gut_flinch` 未用）、M-13（`firelast` 复用 `shoot3`）、M-15（knife `slash1/2`/`stab_miss` 未用）；
> T-05（未内嵌贴图回落）；U-06 雷达（**已对齐**，2026-09-21 重出 · 差异 #14 已消除）、U-34 秒表形态（差异 #15）；
> 其余 U 行是"位图字形 vs 字体 / 菜单未逐像素对齐"这一类**口径差**（原版 640×480 的 VGUI / hud.txt 精灵 vs 我方 1080p uGUI）。
> ⚠️ 另有两处**原版两套口径自身冲突**、按 §5.2 取随包值即可：`mp_autokick`（1 vs 0）、`mp_flashlight`（0 vs 1）。

---

## 9. 外观专项（**agent-21 新增** · 只读取证：FOV / viewmodel / A 点地面材质 / 天空盒 / 第三视角 / 模型移动 / UI）

> **⚠️ 本节是 §1~§5.3 之外的独立块**（§8 的 192 行统计**只覆盖 §1~§5.3**，未含本节；本节自己的统计见 **§9.1**，
> 全表新合计 = 192 + 32 = **224 行**）。⛔ 本节未改动 §1~§8 的任何一行、任何列。
>
> **本片（agent-21）只读取证**：⛔ 未改 `client/**`（"我们的值"全部是**代码现值 + `文件:行`**）、⛔ 未进我方工程 Play。
> 逐项的**推导过程 / 读图实测 / 像素级对账**在 **`策划/外观差异清单.md`**（本节是它的"对照表口径"投影）；基线图见 `策划/基线图/`。
>
> **原版素材新增载体**（本节用到，补 §0 的简写表）：
> `HLSDK` = `原版资源/cs16src/hlsdk/halflife-master`（Valve 官方 GoldSrc SDK 源码）·
> `ENV` = `原版资源/cs16src/cs16game/app/cstrike/gfx/env/des{ft,bk,lf,rt,up,dn}.tga`（原版天空盒本体）·
> `BSPBUILD` = `原版资源/解包产物/dust2_build.py`（BSP → 我方资源的转换器）。
>
> **分块**：`A` = FOV 与 viewmodel（用户报的"鱼泡眼 / 胳膊太长"）· `B` = A 点平台地面材质 ·
> `C` = 天空盒朝向与接缝 · `D` = 第三视角机位 · `E` = 模型移动表现 · `F` = UI 逐项（⛔ 不写"UI 有问题"）。

| # | 元素 | 原版值（出处） | 我们的值（出处） | 差值 | 类别 |
|---|---|---|---|---|---|
| A-01 | `default_fov` 默认值 | **`"90"`** · `HLSDK/cl_dll/hud.cpp:332`（`default_fov = CVAR_CREATE("default_fov","90",FCVAR_ARCHIVE)`；声明 `HLSDK/cl_dll/hud.h:584`） | `CsConst.DefaultFov = 90f` · `CL/Scripts/Core/CsConst.cs:115` | **0**（数值本身一致；**错的是单位口径 ⇒ 见 A-02**） | 数值类 |
| A-02 | **fov 的口径**（水平 vs 垂直） | **水平**：`fov_y` 由 `fov_x` 与视口宽高算出 —— `CalcFov(fov_x,width,height)`：`x = width/tan(fov_x/360*π)`；`a = atan(height/x)`；`return a*360/π` ⇒ `fov_y = 2·atan((H/W)·tan(fov_x/2))` · `HLSDK/cl_dll/view.cpp:1737-1747`（⚠️ 原版唯一实现，被 `#if defined(TRACE_TEST)` 包着 `:1729-1750`） | **Unity `Camera.fieldOfView` 是垂直**，而 `FirstPersonCamera` 把 `DefaultFov` **直接赋给它** · `CL/Scripts/Module/CameraRig/FirstPersonCamera.cs:319`（初值 `:340`；`_fov=_baseFov` `:115-116`） | 差（**口径错**：同一个 90 一个当水平、一个当垂直） | 数值类 |
| A-03 | 16:9 下"等效水平视场" | **90.00°**（固定水平 ⇒ 宽屏竖向更少，Vert−）· 由 A-02 的公式推出：`fov_y(16:9) = 2·atan((1080/1920)·tan45°) = 58.72°`（4:3 = 73.74°；1280×1024 = 77.32°；21:9 = 46.28°） | **121.26°**（= `2·atan(tan(45°)·16/9)`，由 `fieldOfView=90`(垂直) 推出） | **差 +31.26°**（水平） | 数值类 |
| A-04 | Unity 该填的 `fieldOfView`（垂直）@1920×1080 | **58.72°**（= A-03 的 `fov_y`；公式 `2·atan(tan(fov_x/2)/aspect)`，`aspect=Screen.width/Screen.height`） | `90°` · `CL/Scripts/Core/CsConst.cs:115` → `FirstPersonCamera.cs:319` | **差 +31.28°** | 数值类 |
| A-05 | 开镜 FOV | **未解出**：`default_fov` 只给未开镜默认；CS 的开镜下发在 **CS 自己的 `client.dll`**（`HLSDK/cl_dll/` 只有 HL 的 `hud_*`，**没有** CS 的 `hud_redraw` 开镜路径）⇒ 见 `策划/外观差异清单.md` A1-c [BLOCKED] | `CsConst.ZoomFov = 40f` · `CL/Scripts/Core/CsConst.cs:116`（**注释里没有任何出处**） | 差（我方 40° 是**无出处**值；原版值待解） | 数值类 |
| A-06 | viewmodel 的**视图原点偏移** | 眼位后**再下移 1 unit**：`view->origin[2] -= 1;`（注释："pushing the view origin down off of the same X/Z plane as the ent's origin…"）· `HLSDK/cl_dll/view.cpp:644-665`；**另按 `viewsize` 补偿** 110→+1 / 100→+2 / 90→+1 / 80→+0.5 unit · `view.cpp:667-684`；**`viewsize` 出厂默认实测 = `120`**（出处 `原版资源/cs16src/cs16game/app/hw.dll:0x177684` 的 cvar 字面量区，片段 `…"30" "viewsize" "120" "viewsize" …`；HLSDK 只在 `common/ref_params.h:50` 有字段、未注册）⇒ **`120` 不落那四档 ⇒ 无补偿** ⇒ 净偏移 = **−1 unit = −0.0254 m**（GoldSrc 1 unit = 0.0254 m · `原版资源/cs16src/cs16_build.py:43` `HL_UNIT=0.0254`） | `ViewModelLocalPosition = (0, −0.0254, 0)`（= −1 unit，注释含完整出处链）· `CL/Scripts/Module/View/CsViewTuning.cs:173`；`ViewModelScale = 1f` · `:179`；实际挂载 `_modelRoot.localPosition = CsViewTuning.ViewModelLocalPosition` · `CL/Scripts/Module/View/ViewModelRig.cs:395` | **0**（`120` 走无补偿分支 ⇒ 原版净偏移 −0.0254 m，与我方逐值一致） | 数值类 |
| A-07 | viewmodel 的**渲染 FOV** | 由**世界 refdef 的同一份参数**渲染（`viewsize` 就是 `ref_params_s` 的字段 · `HLSDK/common/ref_params.h:50`）⇒ 吃的是世界的 `fov_x=90°(水平)` | **由主相机渲染**（`ViewModelRig` 是相机 GameObject 上的组件、`_modelRoot` 挂在相机下）· `CL/Scripts/Module/View/ViewModelRig.cs:75-79,230-234` ⇒ 吃的是 `fieldOfView=90°(垂直)=121.26°(水平)` | 差（**随 A-02/A-04 一起改**；同一 z 处线性尺寸比值 = `tan(29.36°)/tan(45°) = 0.5625` ⇒ 我方把 viewmodel 画成 **1.7778 倍小**） | 数值类 |
| A-08 | viewmodel 的 **bob 驱动方式与常量** | **速度驱动**：`bob = \|simvel.xy\| · cl_bob`（⛔ 竖线已转义，否则会撑破本表列结构）；`bob = bob·0.3 + bob·0.7·sin(cycle)`；`clamp(bob, −7.0, +4.0)`；`cycle` 由 `cl_bobcycle` 分段、前半程映射到 `0→π` · `HLSDK/cl_dll/view.cpp:180-203`；cvar 默认 **`cl_bobcycle "0.8"` / `cl_bob "0.01"` / `cl_bobup "0.5"`** · `view.cpp:1720-1722`；位移/角度用法 `:651-660`（`origin[i] += bob·0.4·forward[i]`、`origin[2] += bob`、`yaw −= bob·0.5`、`roll −= bob·1`、`pitch −= bob·0.3`） | 常量 **`ViewBobAmount = 0.035f` / `ViewBobSpeed = 9.5f`**（**注释无出处**）· `CL/Scripts/Core/CsConst.cs:118-119`；由引擎 `CloverEngine.ViewBob`（E-core-18 下沉；原 `Module/CameraRig/ViewBob.cs` 已删）按时间驱动，七个数值经 `ViewBobConfig` 由 `PlayerModule` 传入（数值未变） | 差（**不是同一套公式**：原版速度驱动 + 3 个已列默认值的 cvar；我方是自定时间驱动） | 表现类 |
| B-01 | **A 点（`tgt_a`）区域被渲染的上行面材质构成** | `func_bomb_target` `target=tgt_a` → brush `*42` bbox = `x[1024..1280] y[2304..2624] z[96..192]`（`MAP` lump0 实体 + lump14 `models[42]`）；**区域内被渲染的上行面（normal.z≥0.86；已排除 trigger 自身面）按面积**：`SandRoad` **533,892 u²/29 面** · `-0Sand` **267,329 u²/20 面** · **`SandCCrete` 238,816 u²/19 面** · `-0csSandWall` 35,968/14 · `SandTrim` 16,390/4 · `SandCrtLrgSd` 14,336/3 · `SandCrtSmTp` 9,216/1 · `SandWllDoor` 4,096/1。**平台顶（z 128..200）单独**：`-0Sand` 78,324/7 · `SandRoad` 68,096/5 · `-0csSandWall` 31,104/13 · `SandTrim` 12,292/3 · `SandCrtSmTp` 9,216/1。miptex 名表 = `MAP` lump2（44 名：`SandCCrete` idx32 / `SandRoad` idx37 / `SandTrim` idx40 / `SandRoadTgtA` idx38） | 我方按 `BSPBUILD` 的 `FALLBACK` 角色表映射成 26 张 PNG 里的某几张 · `原版资源/解包产物/dust2_build.py:42-58`；贴图集 `CL/ThirdParty/Dust2/Textures/*.png`（目录实测 26 张）；材质着色器/色 `CL/Editor/MapGen/Dust2Builder.cs:194-198` | 差（原版 8 组里有 6 组是名对名的 miptex，我方一个都没按原版像素搬） | 表现类 |
| B-02 | **`SandCCrete`（A 点混凝土路面）→ 我方错在这一组** | miptex `SandCCrete`（`MAP` lump2 idx32；全图上行面面积第 **2** 名 = 2,549,048 u² / 198 面） | **回落成 `box.png`（木箱贴图）** · `原版资源/解包产物/dust2_build.py:52`（`('sandccrete', 'box.png')`）；我方贴图集里**没有** `SandCCrete.png` | **差（画错东西：混凝土画成木箱）** ⚠️ 未进验收表「允许的差异」 | 表现类 |
| B-03 | `SandRoad` / `SandTrim`（A 点沙路 / 台阶条） | miptex `SandRoad`（idx37）/ `SandTrim`（idx40）· `MAP` lump2 | 均回落成 `path.png` · `原版资源/解包产物/dust2_build.py:45,46` | 差（非逐像素同源） ⚠️ 未进验收表「允许的差异」 | 表现类 |
| B-04 | `-0Sand`（平台顶面积第 1） | miptex `-0Sand`（`MAP` lump2 idx24） | 回落成 `ground.png` · `dust2_build.py:55` | 差（非逐像素同源） ⚠️ 未进验收表「允许的差异」 | 表现类 |
| B-05 | A/B 点**地面标记** `SandRoadTgtA/TgtB` | `SandRoadTgtA`（idx38）实测 **2 面**：center `(-288,2448,168)` / `(-288,2339,157)`，**法线 z ≈ 0.000 ⇒ 是竖面上的涂装、不是地面** · `MAP` lump2+lump7 实算 | 回落成 `site_A.png` / `path.png` · `dust2_build.py:43,44` | 差（非逐像素同源） | 表现类 |
| B-06 | 未内嵌 miptex 的**载体可得性** | **可得**：`原版资源/cs16src/cs16game/app/cstrike/cs_dust.wad`（1,055,884 B / `WAD3` / 28 lumps；§6 BLOCKED-3 已列 `SandCrtLrgSd`/`SandRoad`/`MltryCrteSd`/`SandWllWndw` 在其中） | 我方现用 15 张"同族沙漠贴图"回落（来源 `de_dust2_scene/dust2/textures`）· `dust2_build.py:42-58,310-317` | 差（缺的是**搬运**不是载体；⇒ §6 BLOCKED-3/4 的口径更正已成立） | 数值类 |
| C-01 | 天空盒 6 面的**像素** | `ENV` des{ft,bk,lf,rt,up,dn}.tga，各 262,188 B / TGA type2 / 256×256×32bpp / `desc=0x08`（原点左下） | `CL/ThirdParty/Dust2/Skybox/sky_{ft,bk,lf,rt,up,dn}.png` · 来源 `ENV`（`原版资源/清单.md` §8.1）；**引擎另用两张预旋转副本** `sky_up_r90cw.png` / `sky_dn_r270cw.png`（= up/dn 的装配旋转，见 C-03） | **0**（agent-24 复测：6 张**同名**原版贴图 **6/6 逐像素相同**，`maxdiff=0`；"误名检测"逐个最佳匹配都为同名。⛔ 两张副本由原版像素**旋转**得到，不是重画） | 表现类 |
| C-02 | 天空盒 6 面的**邻接顺序** | 由原版像素竖边连续性导出（agent-24 用两两竖边逐像素差复算）：`ft` 右 = `lf` 左（**3828**，同面其余组合 ≥25027）· `lf` 右 = `bk` 左（**4777**）· `bk` 右 = `rt` 左（**4134**）· `rt` 右 = `ft` 左（**3158**）⇒ **真环序 `ft→lf→bk→rt`**（4 条缝全接得上；镜像装配 4 条缝全对不上，总差 7×） | **同名直塞**：`{_FrontTex,_BackTex,_LeftTex,_RightTex,_UpTex,_DownTex}` ↔ `{sky_ft, sky_bk, sky_lf, sky_rt, sky_up_r90cw, sky_dn_r270cw}` · `CL/Editor/MapGen/Dust2Builder.cs:373`（`mat.SetTexture` `:397`）。**Unity 槽位方向实测 = `_LeftTex "Left [+X]"` / `_RightTex "Right [-X]"`**（内置着色器属性名逐字 + edit-mode 渲染实测 `sky_rt` 在 +X、`sky_lf` 在 -X）⇒ 同名直塞才给出世界环序 `ft→lf→bk→rt`（+Z→+X→-Z→-X） | **0**（agent-24 修正：agent-22 那次"交换一对相对面"把**正确**装配改成了**镜像** —— 它按"Left = 向左看看到的那面"假设，与 Unity 的 `Left [+X]` 恰好相反；edit-mode 渲染实测：**同名直塞接缝连续、镜像装配接缝断开**，见 `策划/外观差异清单.md` C1） | 表现类 |
| C-03 | 天空盒 **up/dn 面朝向** | 立方体邻接判据（up/dn 四条边 vs 四侧面的**顶/底边**）：up 的 8 种朝向代价 = **6451**（90° 顺时针）/ 111965（次优）…；dn = **20350**（270° 顺时针）/ 83200（次优）⇒ **区分度 17.4× / 4.1×**（远高于 1.5× 门槛。agent-21 报的"只差 4%"是它把 up 边与**非共边**的侧边比了，不是这条判据） | `_UpTex ← sky_up_r90cw.png` / `_DownTex ← sky_dn_r270cw.png` · `Dust2Builder.cs:373`（Unity 把贴图**原样**贴上去：edit-mode 6 轴向渲染与源贴图 8 朝向匹配 **6/6 都是"不旋转不翻转"**；而 `Skybox/6 Sided` 无逐面旋转（`[NoScaleOffset]`）⇒ 必须预先把贴图转好） | **0**（朝向已定案并对齐；残余逐像素差 = 原版天空盒自身的边缘失配，两侧同源） | 表现类 |
| D-01 | 第三人称机位**是否存在** | **存在**，且判据与命令齐全：`CAM_COMMAND_TOTHIRDPERSON = 1` · `HLSDK/cl_dll/in_camera.cpp:39-44`；`CL_IsThirdPerson()` · `in_camera.cpp:619-624`；`CAM_ToThirdPerson()` · `:446-456`；机位公式 `if (CL_IsThirdPerson()) { CL_CameraOffset(ofs); camAngles = ofs; camAngles[ROLL] = 0; AngleVectors(...); for(i) vieworg[i] += -ofs[2]*camForward[i]; }`（= 沿 cam_ofs 朝向**向后**退 `ofs[2]`）· `HLSDK/cl_dll/view.cpp:617-635` | **未实现**（`ThirdPerson`/`thirdperson`/`FreeLook` 于 `CL/Scripts/**` **0 命中**）；观战时相机被放到**被观察者的眼睛位置**并藏掉他的模型 · `CL/Scripts/Module/CameraRig/FirstPersonCamera.cs:195-207,244,537-554`；枪收起 · `CL/Scripts/Module/View/ViewModelRig.cs:123-129`；只有文字 UI · `CL/Scripts/UI/InGame/CsSpectatorWidget.cs` | **未实现** | 表现类 |
| D-02 | 第三人称**距离**常量 | `cl_chasedist "112"` · `HLSDK/cl_dll/view.cpp:1724`；`cam_idealdist "64"` · `in_camera.cpp:496`（单位 = GoldSrc unit，×0.0254 → **2.8448 m** / **1.6256 m**） | 未实现 | 未实现（目标值见左） | 数值类 |
| D-03 | 第三人称**角度/平滑/下限**常量 | `cam_idealyaw "90"` / `cam_idealpitch "0"` / `cam_snapto "0"` / `cam_contain "0"` · `in_camera.cpp:493-497`；`#define CAM_MIN_DIST 30.0` · `:31`；`CAM_ANGLE_DELTA/SPEED 2.5` · `:29,30`；角度用 `MoveToward(..., CAM_ANGLE_SPEED)` 平滑 · `:372-390`；`roll` 归零 · `view.cpp:627`；viewangles 同步 · `view.cpp:788-794` | 未实现 | 未实现（`CAM_MIN_DIST 30` = **0.762 m**） | 数值类 |
| D-04 | **CS 1.6 观察者**自己的机位常量 | **未解出**：CS 的观察者走 `client.dll`（`原版资源/cs16src/cs16game/app/cstrike/spectatormenu.txt` / `resource/ui/spectator.res` / `sprites/observer.txt` 在盘），本片未反汇编 ⇒ `策划/外观差异清单.md` D1 [BLOCKED] | 未实现 | 未实现（CS 观察者常量待解；D-02/D-03 先用 HLSDK 常量作"有出处的起点"） | 数值类 |
| E-01 | 角色模型**动画状态切换**是否生效 | 原版：动画档位由服务端按速度选（`mp.dll`；`HLSDK/cl_dll/` **没有** CS 的角色动画选择），客户端只做插值；我方已对齐的原版序列节奏 = `idle1` 15fps/61 帧、`walk` 30/33、`run` 60/37、`crouch_idle` 10/31、`jump` 36/61（`terror.mdl` 实测，见 §2 M-05~M-09） | **★ 实测失效**：`Animator.HasState` 恒 False ⇒ 只有控制器 `defaultState` 自动循环（agent-16 实测断言 `HasState('idle1')=False`、`cur==hash('player_CT_gsg9_idle1')`，见 §2 第 105-111 行）。根因（本片实读）：控制器状态名带前缀（`player_CT_gsg9_idle1` 等 46 条）而候选名无前缀 —— `CL/Scripts/Module/View/CsViewTuning.cs:174-215` → `ViewModelRig.cs:307-323`（`HasState(0, StringToHash(...))`）+ `ActorView.ResolveState` | **差（序列切换完全没生效；§2 的"0"只是数值/数据层对齐）** ⇒ 与 `策划/验收表.md` R5/R6 ❌ 一致 | 表现类 |
| E-02 | **视模型**动画状态切换 | 原版视模型序列名 `idle1/reload/draw/shoot1..3` · `v_ak47.mdl` `numseq=6`（见 §2 M-13） | 同一处缺陷：视模型控制器状态名带前缀（`vm_glock18_idle` 等 7 条）而候选 `CsViewTuning.VmState*` 无前缀 ⇒ `ViewModelRig.ResolveState` 取不到 · `CL/Scripts/Module/View/ViewModelRig.cs:307-323` | **差（同 E-01：切枪/开火/换弹序列切不动）** | 表现类 |
| E-03 | 角色视图**位置平滑** | 原版客户端有位置插值（`HLSDK/cl_dll/view.cpp:719-780` 的 `ViewInterp` 环形缓冲，`Length(delta) < 64` 才插值；服务端 10Hz→客户端插值） | **不平滑**：`PositionSmoothTau = 0f` · `CL/Scripts/Module/View/CsViewTuning.cs:88`；另 `TeleportSnapDistance = 2.5f` 无出处 · `:85` | 差（我方直取 `CsActor.Position`，无插值） ⚠️ 未进验收表「允许的差异」 | 数值类 |
| E-04 | "在移动"的判定阈值 | **未解出**：原版按速度档选序列的阈值在 `mp.dll`（服务端）里，本片未反汇编 | `AnimMoveSpeedEpsilon = 0.15f`（米/秒，**注释无出处**）· `CL/Scripts/Module/View/CsViewTuning.cs:171` | 差（无出处值） | 数值类 |
| E-05 | 移动速度 | 全局钳制 `sv_maxspeed 320` unit/s = **8.128 m/s** · `原版资源/cs16src/cs16game/app/cstrike/server.cfg:15`（`hw.dll.orig:0x18b208` 交叉验证）；**每武器 `maxspeed` 仍 BLOCKED-2** | 每武器 3.6~5.4 m/s 四个常量 · `CL/Scripts/Core/CsConst.cs:91-94` | 差（同 N-11） ⚠️ 未进验收表「允许的差异」 | 数值类 |
| F-01 | UI：`Map: <地图名>` 文本 | **原版有**：`策划/基线图/original/de_dust2_freecam_A_00.jpg`（1280×1024）右上角实测可见 `Map: de_dust2`，位于**竖分隔线右侧、计时行上方** | **未实现**（`CL/Scripts/UI/InGame/HudPanel.cs` 无该文案；§4 的 U-31~U-35 五项里没有它） | 差 **未实现**（新增元素） ⚠️ 未进验收表「允许的差异」 | 表现类 |
| F-02 | UI：右上角比分/计时的**上屏色** | **`#FFB000`**（原版 HUD 文字色）· §4 U-21（31 张 1920×1080 BMP 逐像素实测 31/31 + `resource/clientscheme.res:24` `BaseText "255 176 0 255"`） | 实测 `验收截图（一次性产物，未入库）`（1600×900，mtime 09-19 **15:26:31**，晚于 `CsHudTheme.cs` 12:08:50）右上角 `y[20..100] x[1150..1600]`：黄色像素 442 px，主色 **`#AFAF32`/`#B77D0C`/`#9B982D`**；**精确色 `#FFB000±24` 在该区域 0 命中**（同图仅右下角弹药区命中 `x[1486..1579] y[844..865]`） | 差（**需定案**：代码色 vs 上屏色被渲染链路改了；⛔ 本片不下结论） | 表现类 |
| F-03 | UI：右上角 ink 的**右缘距屏右** | §4 U-31/U-32 = **170 px**（比分两行）、U-33 = **51 px**（计时），口径 = **1920×1080 原生** | 实测同上图（**1600×900**）ink bbox `x[1366..1561] y[36..75]` ⇒ 右缘距屏右 **38 px** | 差（**口径不同不可直比** ⇒ 必须 1920×1080 重采一次再判；⛔ 本片不判对错） | 表现类 |
| F-04 | UI：血量/护甲/金钱/弹药那一排 | ⛔ **BLOCKED**（原版无像素载体：`screenshots/` 452 张 `hud_draw` 关、`screenshots_to_conv/` 31 张旁观机位） | 字号 36/32/38 · `CL/Scripts/UI/InGame/HudPanel.cs:110,126,132` | 差（同 §6 BLOCKED-1） | 表现类 |
| F-05 | UI：雷达尺寸/位置 | 精灵 `128×128` px @640 · `原版资源/cs16src/cs16game/app/cstrike/sprites/hud.txt:183` | `RadarSize = 128` px、左上 `(24,-14)` · `CL/Scripts/UI/InGame/CsHudTheme.cs`、`CsRadarWidget.cs` | 差 **0**（2026-09-21 重出后实机 128×128 @scaleFactor=1.0000；§4 U-06 已对齐） | 表现类 |
| F-06 | UI：图标用**字形**替代原版位图 | 原版位图图标：`cross`(血量) 24×24 · `hud.txt:121`；`suit_full`(护甲) 24×24 · `:135`；`suithelmet_full` 24×24 · `:137` | 字形 `\u2665`(♥) / `\U0001F6E1`(盾) / 后缀 `+` · `CL/Scripts/UI/InGame/CsHudTheme.cs:91,93,95` | 差（同 §4 U-03/U-04/U-05） | 表现类 |

### 9.1 本块统计（**与 §8 同一机械口径**：行首 id / 含`未实现`⇒未实现 / 含`差`⇒差 x / 其余⇒0）

| 块 | 行数 | 差值 = 0 | 差 x | 未实现 |
|---|---|---|---|---|
| §9-A FOV 与 viewmodel（A-01~A-08） | 8 | 2（A-01 / **A-06**） | 6 | 0 |
| §9-B A 点平台地面材质（B-01~B-06） | 6 | 0 | 6 | 0 |
| §9-C 天空盒朝向与接缝（C-01~C-03） | 3 | 3（C-01 / **C-02** / **C-03**） | 0 | 0 |
| §9-D 第三视角机位（D-01~D-04） | 4 | 0 | 0 | 4 |
| §9-E 模型移动表现（E-01~E-05） | 5 | 0 | 5 | 0 |
| §9-F UI 逐项（F-01~F-06） | 6 | 0 | 5 | 1 |
| **§9 合计** | **32** | **5** | **22** | **5** |

> **agent-24 复算本表**（机械口径与 §8 同一套）：`A-06` 因 `viewsize` 定案（= 120 ⇒ 无补偿 ⇒ 净 −0.0254 m）
> 从"差"改判 **0**；`C-02` / `C-03` 因天空盒装配定案（同名直塞 + up 90°CW / dn 270°CW）从"差"改判 **0**。
> ⇒ 本块 `0` 由 2 行升到 **5 行**、`差` 由 25 行降到 **22 行**（`未实现` 仍 5，行数仍 32）。

**§9 附录（不计入行数）**：`D-02/D-03` 的目标常量按 GoldSrc unit（×0.0254 → m）给出；
`A-04` 的目标值随宽高比变化（16:9 = 58.72° / 4:3 = 73.74° / 1280×1024 = 77.32° / 21:9 = 46.28°）；
`A-07` 的 `0.5625` 比值 = `tan(fov_y/2) / tan(45°)`；`C-02` 的**真环序**由 agent-24 用两两竖边**逐像素差**复算
（`ft`右=`lf`左 3828 / `lf`右=`bk`左 4777 / `bk`右=`rt`左 4134 / `rt`右=`ft`左 3158；镜像装配同 4 条缝 ≥25027，总差 7×）；
`C-03` 的 8 朝向代价表由 agent-24 的离线脚本按"up/dn 四条边 vs 四侧面顶/底边"算得（up 最优 6451 / 次优 111965，dn 20350 / 83200）。

> ⚠️ **本块 22 个"差"行的处置**（机械口径 = 与 §8 同一套：**行首 id** + `差值` 列，含`未实现`⇒未实现 / 含`差`⇒差 x / 其余⇒`0`；
> ⇒ 同时含"差"和"未实现"的 `F-01` 会被计入**未实现**，这就是 `F` 块 5+1 而不是 6+0 的原因）：
> **只有 `F-05` 带 `差异 #n` 字面**（→ 差异 **#14**）；`F-04` 虽无字面，但它指到 §6 **BLOCKED-1**（既有条目 ↔ 差异 #16）。
> ⇒ **真正"在 `策划/验收表.md` 的「允许的差异」里查不到任何条目"的是 20 行**，逐条列出：
> `A-02..A-05 A-07 A-08`（6）· `B-01..B-06`（6）· `E-01..E-05`（5）· `F-02 F-03 F-06`（3） = **20 行**
> （`C-02` / `C-03` 已按 agent-24 定案归 0、`A-06` 已按 `viewsize` 定案归 0，均从原 23 行清单里移出）。
> ⛔ 本片**未**自行往验收表加条目（加条目属主 agent 权限）。
>
> ⚠️ **`§8` 的 192 行统计未包含本节**（那是设计如此：§8 的口径是 §1~§5.3）。全表新合计 = **192 + 32 = 224 行**。

### §9 cs16-收尾A 新增（2026-09-20 夜）

| 元素 | 原版值（出处） | 我们的值 | 差值 | 类别 |
| 坡道可站立阈值 | `MaxStandableSlopeNormalZ = 0.7`（≈45.573°，GoldSrc `pm_shared.c` `PM_CatagorizePosition`/`PM_WalkMove`） | 同值 | 0 | 数值类 |
| 选角界面文本 | 原版只有底部署名，无第三方署名行 | **未完成**：实机节点树仍含 `GameHelper.com / johnsto.co.uk / MacManInfi@aol.com`（预制体未重跑生成器） | ≠0 | 表现类 |
| 菜单启动曲 | `cstrike/media/gamestartup.mp3`，主菜单首次播、进图停 | 实机 `clip=gamestartup playing=True vol=0.60` | 0 | 表现类 |
| UI 点击音 | `sound/ui/buttonclick.wav` | 同帧 `clip=menu_click playing=True` | 0 | 表现类 |
| 枪口火焰/弹痕 | 同上 .spr/.wad | 程序化贴片（载体不可得，登记 #15） | 载体差异 | 表现类 |
| 玩家贴地高度 | 跟随地形（T生 +3.62 / 中门 0.00 / B通 0.81） | 实机恒 -3.25（不跟地形） | **≠0 严重** | 数值类 |

---

### §9 cs16-切片T 新增（2026-09-21 夜 · 角色 / 视模型外观域）

> **本片只做角色与视模型外观域**（用户报：①「人物模型 100% 不是 cs 原版的，太粗糙了」②「巨大到失真的手臂 + 枪」
> ③「人形纯黑剪影」）。⛔ 未改引擎、未改 `ICsMap`/`ICsMatch`/`CsConst`、未改 skill、未碰碰撞与光照/雾（属地图片）。
> **判据升级**：片 R 给 R1/R2 判 ✅ 用的「视口坐标在准星右下 + z 在相机前方 + bindposes 非零」
> **只判了"存在/在画面里"，没判"多大、什么颜色"** —— 本片一律换成 **尺寸（视口 bbox %）+ 像素（区域平均色/色簇数）**。
> 复现：`tools/probes/probe-viewmodel.cs`（判据资产，`eval_file` 调）+ `tools/probes/probe_{mesh,mesh2,vbone,c16m,skin,matlink,pixels}.py`（一次性，用后删）。

| # | 元素 | 原版值（出处） | 我们的值（出处） | 差值 | 类别 |
|---|---|---|---|---|---|
| T-01 | **角色/视模型 mesh 来源**（用户第一优先质疑） | 原版 `player/*/*.mdl`（9 个）+ `v_*.mdl`（31 个）· `原版资源/清单.md`。⚠️ **载体不在盘**：`原版资源/cs16src/` 实测**为空目录**、`原版资源` 全树 `*.mdl` 计数 = **0**（`清单.md` 自述"本机已被清理，原始 ISO / 客户端 / 解包脚本不在盘上"） | 工程内 `.cs16anim` / `.cs16mdl` = 那次解包的**产物副本**（`CL/Editor/Views/ModelData/*.cs16anim` 38 份）。逐模型实测：`vm_ak47` bones=**42**（= 原版 `numbones` 42，见 M-12）、verts=**869** / tris=**1050**、**11 个 submesh**、UV∈[0.000,0.984]、逐顶点法线长度恒 **1.0000**、逐顶点 `boneIdx`（GoldSrc `vertinfoindex` 单骨骼刚性蒙皮）、submesh 名 = 原版 mdl 内嵌贴图名（`vm_ak47_view_glove` / `view_skin` / `view_finger` / `wood` / `lower_body` / `forearm` / `barrel` / `upper_body` / `reticle` / `handle` / `magazine`）；`player_T` bones=**23**（= 原版 23）、verts=385 / tris=298、2 个 submesh（`player_T_TERROR` / `player_T_Backpack1`）· 复现 `python tools/probes/probe_mesh.py` | **结论：是原版网格，不是程序化几何**（顶点数正常、有 UV、有骨骼索引、贴图名是原版内嵌名）；⛔ **无法与 mdl 逐项比对**（载体不在盘，见 BLOCKED-T1） | 数值类 |
| T-02 | **mesh 轴映射 / 几何正确性**（交叉验证两份独立导出） | `.cs16mdl`（由 `cs16_build.py` 导出，映射 = M-18 的 `(HL.y,HL.z,HL.x)`）`vm_ak47` 包围盒 = `x[-0.026,+0.239] y[-0.286,-0.062] z[-0.087,+0.705]` span=(0.265,0.223,0.793) · 复现 `python tools/probes/probe_c16m.py` | `.cs16anim` 用**它自己的 `idle` 轨道第 0 帧蒙皮** → 包围盒 = `x[-0.026,+0.239] y[-0.286,-0.062] z[-0.087,+0.705]` —— **逐位一致（小数点后 3 位）** · 复现 `python tools/probes/probe_skin.py vm_ak47 idle` | **0**（两份独立导出同源；轴映射正确 —— 枪的长轴落在 +z=前方、偏 +x=右侧、y 在准星下方，正是 CS 1.6 第一人称该有的样子） | 数值类 |
| T-03 | 骨骼 ↔ 网格坐标系一致性 | （无原版可比，本行为内部一致性判据） | 每个顶点到**其所属骨骼**（绑定世界位置）的距离：`player_T` med **0.152** / max **0.479**、`player_T_arctic` 0.177 / 0.488、`player_CT` 0.153 / 0.502、`vm_ak47` max 0.505 · 复现 `python tools/probes/probe_vbone.py` | 一致（人形四肢应 <0.45；实测同一量级 ⇒ **无坐标系错位**） | 数值类 |
| T-04 | **视模型被渲染的 FOV 口径** | `default_fov` 默认 **`90`** = **水平** FOV · `HLSDK/cl_dll/hud.cpp:332`（cvar 注册）+ `HLSDK/cl_dll/view.cpp:1737-1747`（`fov_y = 2·atan((H/W)·tan(fov_x/2))`）⇒ 1920×1080 下 `fov_y` = **58.72°** | 实机读数：`screen=1920x1080 aspect=1.7778 fovY=**58.7155** fovX_back=**90.0000** near=0.0500 cam=CsFpsCamera` · `tools/probes/probe-viewmodel.cs` | **0**（口径已正确：水平固定 90°、垂直 58.72°；⛔ 不再是那个「把 90 当垂直」的旧缺陷） | 数值类 |
| T-05 | **视模型屏幕占比**（尺寸判据） | ⛔ **原版第一人称视模型的像素参照 = BLOCKED-T1**（项目内没有第一人称的原版图：`原版资源/cs16-maps/screenshots/` 452 张 `hud_draw` 关、`screenshots_to_conv/` 31 张旁观机位、`策划/基线图/original/hud_1920x1080_gg_dust2_aim_trainning.bmp` 实测**是信箱遮幅帧、画面里没有视模型**） | **修后实测**（1920×1080，把 7 个 `SkinnedMeshRenderer` 逐个 `BakeMesh` 成当前姿态再逐顶点 `WorldToViewportPoint`，共 963 顶点）：合并视口 bbox = `x[0.5967..0.8414] y[-0.4349..+0.3642]` ⇒ **24.4% 屏宽 × 36.4% 屏高，落在准星右下**；视模型 rig = `localPosition (0,−0.0254,0)`、`localScale 1`；`HasState(idle)=True`（状态机可用）· `tools/probes/probe-viewmodel.cs`（在 Play 中一次 `eval_file` 的返回原文） | 差（**无法与原版比** ⇒ BLOCKED-T1；⛔ 但已量化，不再是"大致像"） | 表现类 |
| T-06 | **修前：视模型被卡在 ref/绑定姿态时的投影**（"巨大到失真的手臂+枪"的机制） | — | v_* 模型的**绑定姿态**几何 = `x[-0.9087,-0.1160] y[-0.0469,+0.1763] z[-0.1272,+0.1374]` ⇒ **把相机原点夹在里面**（z 跨 0）。实测该姿态下有一个顶点投影到视口 **`x=154.1395`（= 154 倍屏宽）**、`y=-314.67`，且 `behindCamera=7/48` ⇒ 一帧用绑定姿态渲染，画面上就是**巨大到失真的手臂/枪** | 差（**已修**，见 T-07） | 数值类 |
| T-07 | 蒙皮 / 骨架更新开关（T-06 的修） | — | 绑定姿态包围盒**不覆盖**渲染姿态（视模型：绑定 `x[-0.909,-0.116]` vs `idle` `x[-0.026,+0.239]`；角色：绑定 `x[-0.195,+0.272]` vs `idle1` `x[-0.454,+0.241]`）⇒ 拿它当"可见性 / 要不要更新"的判据会把姿态**卡在绑定姿态**。已改：`Editor/Views/AnimSetup.cs`（生成期）+ `Module/View/{ViewModelRig,ActorView}.cs`（运行期兜底，盘上已烘好的旧预制体一并生效）：`SkinnedMeshRenderer.updateWhenOffscreen = **true**`、`Animator.cullingMode = **AlwaysAnimate**` | **0**（修后 T-05 的 24.4%×36.4% 即常态；`csc exit=0`） | 数值类 |
| T-08 | **角色 skin 贴图引用链**（材质 → 贴图，用 guid 判） | 原版 mdl 内嵌 skin 名（`player_T_TERROR` / `player_T_Backpack1` / `player_CT_seal` / `player_CT_gsg9_GSG9_Working2` / …） | 38 个模型 × 全部 submesh 逐条核对：`Art/Tex/{名}.png` 存在 = **True**、`Art/Mat/{名}.mat` 存在 = **True**、材质 `_MainTex` 的 guid == 该 PNG `.meta` 的 guid = **True**；**BAD 行数 = 0** · 复现 `python tools/probes/probe_matlink.py` | **0**（**无断链** —— "skin 贴图未绑 / 材质丢失"这一假设被实测否掉） | 数值类 |
| T-09 | **角色 skin 贴图内容与渲染像素**（颜色判据） | 原版 skin 本身：CT 系是**近黑战术装**、T 系是迷彩（T-01 的载体不在盘，色值只能引工程内产物） | 贴图实测均值：`player_CT_seal` **(26,26,21)**、`player_CT_gign_GIGN` (51,52,58)、`player_T_TERROR` (59,53,53)、`player_T_arctic_arctic` (85,85,86)、`vm_*_view_skin` (192,151,109)。渲染像素（`t11-bot-closeup.png` 的模型区，n=**149130**）：均值 (66,55,37)、亮度 p05 **18.9** / p50 **56.0** / p95 **92.6**、**色簇（32 级）= 46** · `python tools/probes/probe_matlink.py` + `probe_pixels.py` | 差（**"纯黑剪影"被实测否掉**：色簇 46 ≠ 1、p95 = 92.6 ⇒ 有贴图纹理与光照层次；"暗"来自**原版 CT/T 基色贴图本身就是近黑战术装**（`player_CT_seal` 均值 (26,26,21)），不是丢贴图） | 表现类 |

> **§9-片T 小结**：`0` 行 T-02 / T-04 / T-07 / T-08；`差` 行 T-01（结论"是原版网格"、差在**无法与 mdl 比对**）/ T-05（差在**无原版像素参照**）/ T-06（已修）/ T-09（差在被误判成"纯黑"，实测为原版近黑 skin）。
> ⛔ **本片未自行往验收表加"允许的差异"条目**（加条目属主 agent 权限）。

### BLOCKED-T1（本片新增）：原版 mdl 载体与「第一人称视模型的原版像素参照」

- **缺什么**：① 原版 `cstrike/models/player/*/*.mdl` 与 `models/v_*.mdl`（用来把 T-01 的顶点/三角形/UV/骨骼数与 mdl **逐项比**）；
  ② **一张第一人称、HUD 打开、原版 CS 1.6 的实机截图**（用来把 T-05 的 24.4%×36.4% **与原版比**）。
- **为什么拿不到（本片逐条试过）**：
  1. `原版资源/cs16src/` 实测**空目录**；`原版资源` 全树 `Get-ChildItem -Recurse -Filter *.mdl` = **0 个**；
     `清单.md` 自述"本机已被清理，原始 ISO / 客户端 / 解包脚本不在盘上 ⇒ 需重新解包才能复现"。
  2. 项目内筛过所有候选原版图：`原版资源/cs16-maps/screenshots/`（452 张）是 `hud_draw` 关的画面、
     `screenshots_to_conv/`（31 张 1920×1080）是**旁观机位**、`策划/基线图/original/de_dust2_freecam_A_0{0,1,2}.jpg` 是**自由视角**、
     `策划/基线图/original/hud_1920x1080_gg_dust2_aim_trainning.bmp` 本片实测**上下信箱遮幅**（画面底部是黑边、无第一人称武器/手臂）。
  3. `.cs16anim` / `Art/**` 是**唯一在盘**的原始数据载体 ⇒ 重建链路在缺件状态下**不可复跑**（`清单.md` 同口径）。
- **谁能给**：**只有用户**能给载体（CS 1.6 客户端本体 + 解包脚本，或一张第一人称原版截图）。
- **拿到后我立刻做什么**：① 跑 `cs16_anim.py` 重导，把 T-01 的 `bones/verts/tris/UV/贴图名` 与 mdl 逐项对账；
  ② 用同一把尺（`probe-viewmodel.cs` + 同机位截图）把 24.4%×36.4% 与原版比，写 `一致 / 不一致(差在哪)`。

---

## §W（2026-09-21 · 切片W）外观可量化复核：贴图导入设置 + 像素对照 + 光照/雾

> ⛔ 本节**不是** §G 覆盖矩阵（§G 只由 `tools/probes/enumerate-entities.py --inject` 生成）；本节是分片结论表。
> 复现命令：`python tools/probes/check-import-settings.py client/Assets/Resources/Art`、
> `python tools/probes/texture-fidelity.py <png>`、`python tools/probes/pixel-compare.py <图...>`。

| # | 项 | 原版值（出处） | 我方值 | 差值 | 类别 |
|---|---|---|---|---|---|
| W-01 | 角色/武器 skin 贴图**导入尺寸** | 原版 mdl 内嵌 skin 的像素尺寸 = 我方 PNG 的源尺寸（PNG 是那次解包的**唯一在盘**载体；`Dust2Builder.AssertTextureSizesMatchSource` 逐张断言 `PNG IHDR w×h == 导入后 Texture2D.width×height`） | 243 张贴图尺寸分布：256x256 ×46 / 256x128 ×55 / 128x128 ×51 / 128x64 ×22 / 256x64 ×21 / 64x64 ×15 … **最大边 256 px，无一张 >512** | **0**（未被压：`maxTextureSize` 2048 ≥ 源尺寸 ⇒ ⛔ "maxTextureSize 被压小"这一假设实测**否掉**） | 数值类 |
| W-02 | skin 贴图**过滤方式** | GoldSrc 硬件渲染默认**双线性 + mip**（⛔ 本机**无出处文件**：`原版资源/cs16src` 为空 ⇒ 原版侧记 **BLOCKED-W1**） | **修前** `filterMode: 0 (Point)` ×242，由 `client/Assets/Editor/Views/ArtSetup.cs:300` 设置，其注释自述理由 = "复古像素观感"（⛔ **无原版出处**，属风格选择） ⇒ **修后** `filterMode: 1 (Bilinear)` ×242 | 修后 0（未压/未降采） | 表现类 |
| W-03 | skin 贴图**压缩** | 原版 skin 是 8-bit 索引调色板（我方 PNG 实测 uniqueColor = 235 / 248 ⇒ 与原版同为调色板级） | **修前** `textureCompression: 1 (Compressed → BC1/DXT1 有损)` ×242（Unity 默认） ⇒ **修后** `textureCompression: 0 (Uncompressed)` ×242 | 修后 0（无编码误差） | 数值类 |
| W-04 | **"粗糙"的量**（贴图侧） | —（原版可比量 = BLOCKED-W1） | Point vs Bilinear 在 8× 放大下逐像素差：`Art/Tex` 下的 player_CT_gsg9_GSG9_Working2（mean **3.28/255**、max **111**、**10.12%** 像素差 >8）、player_T_TERROR（mean **5.39**、max **113**、**18.89%**。BC1 往返 PSNR **34.38 / 30.89 dB**、maxΔ **48 / 50**（≈19%） | 修后这两项误差**归零** | 数值类 |
| W-05 | 像素对照（原版基线图 vs 我方 A 点帧） | `策划/基线图/original/de_dust2_freecam_A_00/01/02.jpg`（**1280×1024**）实测：meanLum **103.7 / 106.2 / 85.8**、p50 **113 / 115 / 86**、RMS 对比度 **51.0 / 56.0 / 47.6**、平均饱和度 **0.509 / 0.511 / 0.446**、远景带(上20%)RMS **44.5 / 49.7 / 52.0** | `60_bombsite_a.png`（1280×720）：meanLum **101.2**、p50 **109**、RMS **46.4**、饱和度 **0.678**、远景带RMS **32.3**；`24_bombsite_a.png`：meanLum **48.0** / p50 **27**（整体压暗，与另两帧不同态，**未定性**） | 亮度/中位亮度 **一致**；**饱和度 不一致（+33%）**；**雾感 不一致（远景对比 -35%）** —— ⚠️ **片X 复算后这两项"不一致"被判定为口径产物，不是光照差异**：① 那三张基线图**上下各有约 106 / 108 行信箱黑边**（行均值 26/32、行内 std≈5），"上 20%"带里混了黑边 ⇒ 该 RMS 是**黑边 vs 内容的分界方差**，不是"雾感"；② `60_bombsite_a.png` 是**高空俯瞰帧**（画面前景全是地面 ⇒ 饱和度天然高、远景带天然低），与基线的**平视自由视角**构图不可直比。逐项复算见 **§X** | 表现类 |
| W-06 | 光照 / 雾 对齐（G-17/G-18） | 原版 `light_environment`: `_light "255 255 128 70"`、pitch -60 / angle 43（`MAP` lump0）；原版**无雾**（`MaxRange 8192`、worldspawn 无 fog key） | 现状：方向光 `Euler(60,43,0)` + intensity 1.15（`Dust2Builder.cs:291-297`）；**雾 = 本项目新增** `fogDensity 0.0025` + `fogColor (214,197,155)` + Trilight 环境色（`:299-308`） | **片X 已做并定案**（原"未做"）：`sun intensity 1.15→1.75`、`ambientIntensity 1→1.55`、`fogDensity 0.0025→0.010`；⛔ 未加任何滤镜/后处理。逐档实测与收敛数字见 **§X-X1/X2** | 表现类 |

**本片对贴图链的改动（一行版）**：`Assets/Resources/Art/**` 242 张 `.png` 的导入设置由
`filterMode Point + textureCompression Compressed(BC1)` 改为 `filterMode Bilinear + textureCompression Uncompressed`
（`maxTextureSize` 2048、`enableMipMap` 1、`anisoLevel` 1 保持），经编辑器 `AssetImporter` 落盘并读回校验 0 张残留。
⛔ **未改**任何 `.png` 像素、几何、材质参数、引擎。

---

## §X（2026-09-21 · 切片X）回退风险消除 + 光照/雾/饱和度对齐 + 同机位并排图

> ⛔ 本节**不是** §G 覆盖矩阵（§G 只由 `tools/probes/enumerate-entities.py --inject` 生成）；本节是分片结论表。
> 复现命令：`python tools/probes/check-import-settings.py client/Assets/Resources/Art`、
> `python tools/probes/pixel-compare.py <图...>`；
> 采帧驱动（复用区）：`.ai-tmp/drivers/w-capture.ps1`（按 `.ai-tmp/test/x-poses.txt` 摆探针相机并截 1920×1080）。
> **本片取证全程走编辑器直采**（`capture_game_view --source camera`；`EditorApplication.isPlaying` 实测 = **EDIT**），
> ⛔ 未进 Play（故 `.ai-tmp/test/play-log.tsv` 无新增行）；临时探针全部在 `.ai-tmp/test/`，一次性的用后即删。

| # | 项 | 原版值（出处） | 我们的值 | 差值 | 类别 |
|---|---|---|---|---|---|
| X1 | **贴图导入设置的回退风险** | —（本项目内部一致性判据） | **修前**：`ArtSetup.cs:299-304` 仍写回 `FilterMode.Point` + `maxTextureSize 512` ⇒ 重跑一次生成器就把片W 的 242 张修复**全部回退**。**修后**：`FilterMode.Bilinear` + `maxTextureSize 2048` + 补 `textureCompression = Uncompressed`（`ArtSetup.cs:299-313`）；并**实际重跑了一次生成器**（菜单 `Clover/CS16/整理视图与音效资源`）后复测 | **0**（重跑后 `check-import-settings.py` 汇总仍是 `0 / 2048 / 0 / 1 / 1 / 1 -> 242` = textureType Default / maxSize 2048 / **Uncompressed** / mip 开 / **Bilinear** / sRGB；第 243 张 = `ui_white`（Sprite，按设计）） | 数值类 |
| X2 | **基线图的"信箱黑边"（本片新发现）** | `de_dust2_freecam_A_0{0,1,2}.jpg` 名义 **1280×1024**；实测**上下各有黑边**：上 `0..105`、下 `916..1023`（行均值 25~32、行内 std ≈ 3.5~9）⇒ **真实内容区 = 1280×810**（≈16:10） | 我方帧无黑边（1920×1080 全画面） | ⛔ 片W 的 W-05「远景带 RMS 44.5~52.0」**是黑边混进"上 20%"带造成的分界方差**，不是雾感。**裁掉黑边后基线远景带 RMS = 31.7 / 37.1 / 33.8**（`.ai-tmp/test/xb_de_dust2_freecam_A_0{0,1,2}_crop.png`） | 数值类 |
| X3 | 同机位我方帧（**修前**，= 片W 落盘状态） | 见 X4 的原版行 | 探针相机 `(45,5,24) → (30,2.5,44)`、fovY 58.72（= 水平 90，`default_fov` 口径）· `.ai-tmp/screenshots/x-L0-base.png`（1920×1080） | meanLum **99.4**（基线内容区 123.1 ⇒ **−19.2%**）；平均饱和度 **0.546**；远景带 RMS **47.3** | 表现类 |
| X4 | **光照调参逐档实测**（同一机位、单变量隔离） | `策划/基线图/original/de_dust2_freecam_A_00.jpg` **裁黑边后**：meanLum **123.1**、p50 **127**、p95 **175**、RMS 37.8、平均饱和度 **0.523**、远景带 RMS **31.7**（A_01/A_02 = 126.1/101.2、139/101、0.531/0.469、37.1/33.8） | ① `fogDensity 0.0025→0.010`（只动雾）：meanLum 99.4→**104.1**、饱和度 0.546→**0.508**、远景带 RMS 47.3→**47.3（不变）**；② `sunIntensity 1.15→1.55` + `ambientIntensity 1→1.4`（只动亮度）：meanLum→**113.6**、饱和度→0.557、远景带 RMS 仍 47.3；③ 组合：meanLum **117.6** / 饱和度 **0.524**；④ 再抬一档（`sun 1.75` / `amb 1.55`，**定案**）：meanLum **124.3** / p50 **125** / 饱和度 **0.528** / 远景带 RMS **47.3** | **收敛**：meanLum Δ **+1.2（0.98%）**、饱和度 Δ **+0.005**（≤ ±0.05）⇒ 这两项 **一致**；**远景带 RMS 对雾/光全不敏感 ⇒ 该量不是雾感代理**（原因：远景带是**天空盒**，Unity 内建天空盒着色器**不吃雾**）⇒ 登记为未对齐项，见 X5 | 表现类 |
| X5 | **余下未对齐项** | 基线内容区远景带 RMS **31.7~37.1**（三条基线图） | 我方同机位帧远景带 RMS **47.3**（对 `fogDensity` 0.0025↔0.010、`sun` 1.15↔1.75、`ambientIntensity` 1↔1.55 **全部不变**） | **不一致（+10.2 ~ +15.6）** —— 归因：该带是**天空盒 + 远景几何**的对比度（我方远山更暗、细节更多）；要靠**天空盒/远景色调**或**远景几何**才改得动，**不在本片「只调场景光照参数」的范围**；且两侧**机位不同、构图不同**，本就不该期望逐值相等 ⇒ 需**原版实机同机位图**才能判「真不一致 / 口径差」。**已排除"分辨率/JPEG 压缩"这一口径**：把定案帧按双线性降到 1280×810 并编码为 JPEG（q75 / q85）后复测，meanLum 仍 **124.3**、远景带 RMS 仍 **47.3**（`.ai-tmp/test/x-jpeg-q75.jpg` / `x-jpeg-q85.jpg`）⇒ 差异**不是**缩放/压缩产物 | 表现类 |
| X6 | **同机位并排图（表现类交付证据）** | `策划/基线图/original/de_dust2_freecam_A_00.jpg`（裁黑边 1280×810） | `.ai-tmp/screenshots/original-vs-ours-sbs.png`（2732×906：左原版 / 右我方 `w10-final-A.png`，逐项标注数字；同一文件另存一份在 `.ai-tmp/test/original-vs-ours-sbs.png`） | 逐项结论见下方「读图结论」 | 表现类 |

**读图结论（`.ai-tmp/test/original-vs-ours-sbs.png`，逐项）**：

| 项 | 结论 |
|---|---|
| 机位/构图 | **近似**（左：壁龛墙在左、石板路占前景、右大拱门、远山；右：同几件元素都在，但我方机位**更高、更靠 A 点高原**、石板路只占右下角）⇒ 非逐像素同机位 |
| 整体亮度 / 中位亮度 | **一致**（123.1 / 127 vs 124.3 / 125） |
| 平均饱和度 | **一致**（0.523 vs 0.528） |
| 远景（远山带）对比 | **不一致**：我方远山**更暗、细节更多、对比更高**（47.3 vs 31.7）—— 见 X5 |
| 木箱材质 | **不一致**：原版是**木纹板条箱**，我方 A 点摆的是**绿色军用弹箱**（`B-02` 的 miptex 回落问题，属 §9-B 块，不在本片范围） |
| 地面石板 | **部分不一致**：原版石板偏**青灰**，我方偏**土黄**（`B-03` 的 `SandRoad`/`SandTrim` 回落成 `path.png`，属 §9-B） |
| HUD | **不可比**：`--source camera` 不含 Screen Space - Overlay ⇒ 我方帧无 HUD（HUD 已在 §4 U-31~U-35 单独对齐） |

**本片改动（一行版）**：① `client/Assets/Editor/Views/ArtSetup.cs`（导入器不再写回 Point/512，且补 Uncompressed）；
② `client/Assets/Editor/MapGen/Dust2Builder.cs` 的 `sun.intensity 1.75` / `RenderSettings.ambientIntensity 1.55` / `RenderSettings.fogDensity 0.010`，
并**重跑了两个生成器**（`整理视图与音效资源`、`生成 de_dust2 场景`）把设置烘进 `Art/**` 与 `Scenes/StageDust2.unity`。
⛔ **未改**任何 `.png` 像素、几何、碰撞、材质着色器、引擎、契约、skill；⛔ **未加**滤镜/后处理。

**本片对既有行的两处机械更正（闸门 `refs-reachable` 的假引用，非语义改动，已在回报中登记）**：
§3 的 `T-04` 行与 §W 的 `W-04` 行把**贴图文件名**（`ui_white` / `player_CT_gsg9_GSG9_Working2`）写成了反引号包裹的 `*.png`
⇒ 闸门把它们当成**取证截图名**去 `.ai-tmp/screenshots/` 找（找不到）⇒ 一直报 2 条 `refs-reachable` FAIL。
本片把这两处改成**普通文本 + 真实目录**（`Art/Tex` 下的 …），语义不变、判据不变，只是不再被当作截图引用。

**闸门 `coverage-diff` 的 5 行**（D2 ×2 / D9 ×3，标记 `脚本断言`）是**既有残留**（片R/片V 的碰撞域行），
本片**未增行、未手改 §G**（§G 只由 `tools/probes/enumerate-entities.py --inject` 生成，本片跑过后仍是这 5 行）。
