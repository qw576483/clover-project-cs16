# 素材调研（clover-project-cs16 · 参考游戏 A = Counter-Strike 1.6）

> 本文是 skill **§4 闸门 3**（素材从哪来）的落盘要求。**只写真实发生过、且现在还能被复现或对账的事**。
> 生成时间：2026-02-19（agent-09）。
>
> ⛔ **本文不含任何编造的访问记录 / 下载 URL / 搜索页截图**：
> 能当场复现的（HTTP 探测、条目元数据、文件校验和）都附了**原始输出**；
> 不能核验的一律显式写成 **「无留痕，不可核验」**（见 §5）。

---

## 1. 结论（先给结论）

| 问题 | 结论 |
|---|---|
| ① 是否已用上 A 自己的素材？ | **是**（级 ①，最高级）。角色模型 / 第一人称武器模型 / 音效 / 地图几何与内嵌贴图 / **菜单与 HUD 的定义文件**全部来自官方 CS 1.6 客户端本体。 |
| ② 是否用了通用兜底素材（KayKit / Quaternius / Kenney / Unity 内置几何体）？ | **没有**。工程内所有模型资源都是 CS 1.6 的 `models/*.mdl` 转出来的（`client/资源欠缺清单.md:73-74` 第 3 条声明一致，且本片核对过程序化生成的唯一非原版资产 `ui_white.png`）。 |
| ③ 真正的缺口是什么？ | 三类：**(a) 角色/武器帧动画**（原版有 111/6 条序列，我方只取第 0 帧静态姿态）；**(b) 编译期 WAD（`cs_dust.wad` / `halflife.wad`）里的未内嵌贴图**（本体不在客户端随包里）；**(c) 落地音**（**A 自己就没有**，GoldSrc 复用脚步采样）。 |
| ④ 缺口登记在哪？ | (a) `client/资源欠缺清单.md:39-40` 第 13/14 项（状态 ⬜）；(b) 同文件 `:45` 第 19 项 + 本片 `策划/对照表.md` **BLOCKED-3**；(c) 同文件 `:33` 第 7 项（状态 ⬜ 半占位，A 无此素材）。 |

**一句话**：A 的素材**已经拿到并接进工程**（级 ①），剩下的缺口不是"找不到"而是"A 自己没有"或"需要额外解密工作"。

---

## 2. 最终采用的载体与它的确切身份（**有留痕，可逐项复现**）

`原版资源/` 下 8 个子目录里，**5 个是 git 克隆**——它们的 `.git/config` 里留着 `remote origin`，
`.git/logs/HEAD` 里留着**克隆时间与 commit hash**。这是比"搜索记录"更硬的证据：它同时证明了「从哪来 / 哪个版本 / 什么时候」。

| 子目录 | 来源 URL（`<子目录>/.git/config` 的 `remote.origin.url`） | commit（`.git/logs/HEAD`） | 克隆时刻（epoch → UTC） |
|---|---|---|---|
| `原版资源/cs16-maps` | `https://github.com/TheBestPlan/cs16-maps.git` | `9d79e0b75d0c38d9c10cdb3528753ea3ee7fa5cf`（main；**shallow + git-lfs**） | 1789570665 → 2026-09-16 14:57:45 |
| `原版资源/cs_16_maps` | `https://github.com/unimator/cs_16_maps.git` | `842607ac7c6dc88fcced7e2625db157366897f37`（main） | 1789570658 → 2026-09-16 14:57:38 |
| `原版资源/de_dust2` | `https://github.com/wanderer-00/de_dust2.git` | `023cc9a11143da003295caaebf61c8155a751a9f`（main） | 1789571634 → 2026-09-16 15:13:54 |
| `原版资源/de_dust2_scene` | `https://github.com/1832051637/de_dust2_scene.git` | `9185ef52d74eb261b359d14cb5bf90bf82cf3a5f`（main） | 1789571630 → 2026-09-16 15:13:50 |
| `原版资源/cs-docker` | `https://github.com/coderocker/counter-strike-docker.git` | `aed225795e131ed7137095f1ce243e4134013cf9`（master） | 1789571669 → 2026-09-16 15:14:29 |

> 复现命令（只读）：
> ```powershell
> cd c:\Work\Server\full-dev\clover-project-cs16\原版资源
> Get-Content cs16-maps\.git\config ; Get-Content cs16-maps\.git\logs\HEAD
> Get-Content cs_16_maps\.git\config ; Get-Content cs_16_maps\.git\logs\HEAD
> Get-Content de_dust2\.git\config ; Get-Content de_dust2\.git\logs\HEAD
> Get-Content de_dust2_scene\.git\config ; Get-Content de_dust2_scene\.git\logs\HEAD
> Get-Content cs-docker\.git\config ; Get-Content cs-docker\.git\logs\HEAD
> ```

**另外两个非 git 载体（A 自己的素材本体）**：

| 载体 | 来源 | 校验和对账（本片实测） |
|---|---|---|
| `原版资源/cs16src/cstrike-LiON.iso` | `https://archive.org/download/cstrike-LiON/cstrike-LiON.iso` | 本地 `87,855,104` 字节 / MD5 `8d1065e90c6f73e7c764661275a9ae58` / SHA1 `0f36573b5e238362cbf2ccbc64cfed604988196c`；archive.org 条目 `cstrike-LiON` 的 `files[0]` 元数据 **完全一致**（见 §4 第 2 行原始输出） |
| `原版资源/解包产物/cs109_dust2.zip` | `https://archive.org/download/cs-109-de_dust2/cs-109-de_dust2.zip` | 本地 `1,086,336` 字节 / MD5 `81f71bbecf6914614bc23e3c794225b2`；archive.org 条目 `cs-109-de_dust2` 元数据 **完全一致**（见 §4 第 1 行） |
| `原版资源/cs109/**`（bsp+nav+res+txt） | 上一条 zip 解包 | `cs109/maps/de_dust2.bsp` 与 `cs16src/cs16game/app/cstrike/maps/de_dust2.bsp` **大小同为 2,057,288 字节**（两处独立来源同尺寸） |

> ✅ **复核结论（2026-09-19 实测）**：`client/Assets/ThirdParty/Dust2/SOURCES.txt:5` 写的是 **`1,086,336 字节`**（7 位，
> 与本地 zip 及 archive.org 元数据一致）——**本来就是对的**。
> 本节曾登记过一条"把 `10,086,336` 改成 `1,086,336`"的纠错，**经复核系误报，已撤回**；
> 唯一真实笔误在 `原版资源/清单.md`（已同步更正）。

**解包链路（A 的客户端本体）**：`ISO（ISO9660） → 里面是 Inno Setup 安装器（`setup.exe` + `setup-1.bin`）
→ `innoextract 1.9` 解出 `app/cstrike/**` → `原版资源/cs16src/cs16game/app/cstrike/**`（实测存在 `hl.exe` / `cstrike/dlls/mp.dll` / `cstrike/models/**` / `cstrike/sound/**` 等完整目录树）`。
工具来源 `https://github.com/dscharrer/innoextract/releases/download/1.9/innoextract-1.9-windows.zip`（本片实测 HTTP 200，520,705 字节）。
细节见 `client/资源欠缺清单.md:11-12`。

---

## 3. 「穷尽」的三个维度（§4 闸门 3 的量化定义）

### 3.1 维度一：站点类别 —— **3 类，全部当场核验可达**

| 站点类别 | 具体对象 | 实测结论（本片，2026-02-19） |
|---|---|---|
| **① 代码托管（GitHub 仓库）** | `TheBestPlan/cs16-maps`、`unimator/cs_16_maps`、`wanderer-00/de_dust2`、`1832051637/de_dust2_scene`、`coderocker/counter-strike-docker` | 5 个页面全部 **HTTP 200**（§4 第 5-6 行）；其中 `TheBestPlan/cs16-maps` 的**页面顶层目录**与本地 `原版资源/cs16-maps/` **逐项吻合**（`counter-strike` / `half-life` / `screenshots` / `screenshots_to_conv` / `_2025_new_to_add_cstrike` / `_2025_new_to_add_halflife` / `_2025_new_to_check` / `maps_dir_extract.js` / `.gitattributes`）—— 见 §4 第 7 行 |
| **② 代码托管（GitHub Releases）** | `dscharrer/innoextract` v1.9 的 `innoextract-1.9-windows.zip` | **HTTP 200**，520,705 字节（§4 第 4 行） |
| **③ 数字档案库（archive.org）** | 条目 `cstrike-LiON`（ISO）、条目 `cs-109-de_dust2`（zip） | 直接下载 URL **HTTP 200**（87,855,104 / 1,086,336 字节）+ `metadata` API 取回 `md5/sha1/size`（§4 第 1、2 行） |

> 未使用的站点类别（如实登记）：**素材站**（spriters-resource 等）、**论坛/贴吧/3DM/游侠**、**网盘直链**、**CSDN/爱给** ——
> 本项目**没有**从这几类站点取过任何资源（工程内没有任何对应留痕），也没必要：级 ① 的 A 本体已经拿到。

### 3.2 维度二：格式与打包方式 —— **实际搬运过的载体格式（全部可在工程内点到实物）**

| 打包/格式 | 出现形态 | 工程内实物 | 我方如何用 |
|---|---|---|---|
| **ISO9660 光盘镜像** | CS 1.6 完整客户端 | `原版资源/cs16src/cstrike-LiON.iso` | 源头，不直接进工程 |
| **Inno Setup 安装器** | ISO 内的 `setup.exe` + `setup-1.bin` | `原版资源/cs16src/inno/`、`inno.zip` | 用 innoextract 解包 |
| **解包后的客户端目录树** | `hl.exe` + `cstrike/**` | `原版资源/cs16src/cs16game/app/cstrike/**` | 全部素材的最终来源 |
| **ZIP 压缩包** | 社区地图包/SDK/工具 | `解包产物/cs109_dust2.zip`、`cs16src/{hlsdk,goldsrs,inno,test}.zip` | 解压后使用 |
| **GoldSource BSP v30（关卡）** | 地图几何+内嵌贴图+实体 | `原版资源/cs109/maps/de_dust2.bsp`、`cs16src/cs16game/app/cstrike/maps/de_dust2.bsp` | 手写 Python 解析 → `CD2G` geo.bin |
| **BSP 伴随文件** | `.nav`（寻路）/ `.res` / `.ztmp` / `.txt`（关卡说明） | `cs109/maps/de_dust2.{nav,res,res.ztmp,txt}` | `.nav`/`.res` 未用；`.txt` 仅作资料 |
| **GoldSrc MDL v10（模型）** | 角色 `player/*/*.mdl`、视模型 `v_*.mdl` | `cs16src/cs16game/app/cstrike/models/**`（192 个 mdl） | 头字段解析（骨骼/序列/贴图）+ 逐帧顶点解码 → `cs16mdl` |
| **内嵌 8bit 调色板贴图（miptex）** | BSP lump2 里的 miptex（自带 768B 调色板） | 由脚本解出为 PNG | 地图贴图 |
| **内嵌 8bit 调色板贴图（mdl texture）** | mdl `textureindex` 指向的像素+调色板 | 由脚本解出为 PNG | 模型贴图 |
| **WAD3（`halflife.wad` / `cs_dust.wad`）** | 只在 `worldspawn` 的 `wad` 键里被**引用** | **本体不在项目内** | ⛔ 拿不到 ⇒ 对照表 BLOCKED-3 |
| **RMF（Hammer 源地图）** | `cs_16_maps/**/*.rmf` | `原版资源/cs_16_maps/`（10 个 rmf）+ 伴生 `.wad` | **未使用**（供人工查证地图结构） |
| **SPR（GoldSrc 精灵）** | HUD 图标/枪口火焰/烟雾 | `cstrike/sprites/*.spr`、`valve/sprites/*.spr` | **未使用**（我方 HUD 走字体+纯色块） |
| **TGA** | 天空盒 `des*.tga`、菜单背景、瞄准镜 | `cstrike/resource/**/*.tga`、`cstrike/sprites/scope_arc*.tga` | 天空盒 TGA→PNG（社区镜像的 des 天空） |
| **BMP** | 雷达总览图 | `cstrike/overviews/de_dust2.bmp`（787,510 字节） | **未使用**（我方雷达是自制图形） |
| **文本 KeyValues（`.res`）** | VGUI 菜单/配色/字体布局 | `cstrike/resource/ui/*.res`、`cstrike/resource/clientscheme.res` | **已用于对照表 §4**（价格、坐标、颜色） |
| **文本（`hud.txt` / `weapon_*.txt`）** | HUD 精灵源矩形 | `cstrike/sprites/hud.txt` 等 | **已用于对照表 §4** |
| **Web 资源（Three.js 场景）** | 贴图 PNG/JPG + glTF | `原版资源/de_dust2_scene/**` | 作为**回落贴图**来源（11 张未内嵌贴图） |
| **我方转换后的中间格式** | `CD2G` geo.bin / `CLVM` map bytes / `cs16mdl` / 16bit PCM wav | `client/Assets/ThirdParty/Dust2/de_dust2_geo.bin`、`Assets/MapData/de_dust2.bytes`、`Assets/Editor/Views/ModelData/*.cs16mdl`、`Assets/Resources/Sound/SFX/sfx/*.wav` | 工程内运行时载体 |

> **格式维度的"穷尽"判据**：上表覆盖 5 种打包容器（ISO / Inno / ZIP / git 仓 / 裸文件树）× 8 类资源格式
> （BSP / MDL / WAD3 / miptex / SPR / TGA / BMP / KeyValues 文本）。
> **没试过的**也如实写：**没有试过 MPQ / CASC**（那是暴雪系，与 CS 无关）、**没有试过录屏逐帧**（不需要，几何与贴图都拿到了原始数据）。

### 3.3 维度三：检索轮次（关键词）—— **本项目**无留痕，见 §5 的分级说明

**如实说明**：`原版资源/` 与 `client/` 里**没有保存任何"搜索关键词 / 搜索页 URL"记录**（没有浏览器历史、没有搜索日志文件）。
⇒ 本片**不能**把"第几轮搜了什么词"写成事实，**一律标注为无留痕**（见 §5）。
**能替代它的、而且更强的证据**是 §2 的 5 条 git 克隆记录（含 commit hash）与 §2 的 2 条 archive.org 校验和对账 ——
它们直接回答"拿到了什么、从哪拿的、哪个版本"，比关键词记录更硬。

---

## 4. 本片实测的网络核验（**真实检索记录**，附原始输出）

> 下面每一条都是 2026-02-19 由本 agent 在本机执行、**当场拿到响应**的。
> 复现命令统一为：`Invoke-WebRequest -Method Head -Uri <URL> -UseBasicParsing`（archive.org 元数据用 `Invoke-RestMethod`）。

| # | 对象 | 原始输出 |
|---|---|---|
| 1 | `https://archive.org/metadata/cs-109-de_dust2` | `identifier: cs-109-de_dust2 / creator: Valve / date: 2000-12-28 / mediatype: software`；`file=cs-109-de_dust2.zip size=1086336 md5=81f71bbecf6914614bc23e3c794225b2 sha1=3abe852bb607b43b1addff3552239ec55afc3f3f` |
| 2 | `https://archive.org/metadata/cstrike-LiON` | `server=ia800806.us.archive.org`；`file=cstrike-LiON.iso size=87855104 md5=8d1065e90c6f73e7c764661275a9ae58 sha1=0f36573b5e238362cbf2ccbc64cfed604988196c fmt=ISO Image` |
| 3 | 本地 ISO 校验和（`Get-FileHash`） | `MD5 8d1065e90c6f73e7c764661275a9ae58` / `SHA1 0f36573b5e238362cbf2ccbc64cfed604988196c` / `Length 87855104` → **与第 2 行逐位一致** |
| 4 | `https://github.com/dscharrer/innoextract/releases/download/1.9/innoextract-1.9-windows.zip` | `HTTP 200`，`Content-Length = 520705` |
| 5 | 直接下载 URL 探测 | `200 87855104 https://archive.org/download/cstrike-LiON/cstrike-LiON.iso`；`200 1086336 https://archive.org/download/cs-109-de_dust2/cs-109-de_dust2.zip` |
| 6 | 5 个 GitHub 仓库页面 | `OK 200`：`coderocker/counter-strike-docker`、`unimator/cs_16_maps`、`wanderer-00/de_dust2`、`TheBestPlan/cs16-maps`、`1832051637/de_dust2_scene` |
| 7 | `https://github.com/TheBestPlan/cs16-maps` 页面顶层目录抓取 | `directory _2025_new_to_add_cstrike / _2025_new_to_add_halflife / _2025_new_to_check / counter-strike / half-life/maps / screenshots / screenshots_to_conv`；`file .gitattributes / maps_dir_extract.js` → **与本地 `原版资源/cs16-maps/` 顶层逐项吻合** |
| 8 | `https://raw.githubusercontent.com/1832051637/de_dust2_scene/master/README.md` | 正文首行 `# de_dust2_scene` / `A de_dust2 bombsite scene! Made by Three.js` / 引用 `dust2_screenshot.png` → 与本地 `原版资源/de_dust2_scene/dust2_screenshot.png`（1,352,698 字节）对得上 |
| 9 | GitHub REST API（`api.github.com/repos/...`） | **403 已禁止**（本机网络策略/限流）→ 改用页面 HEAD（第 6 行）与 `raw.githubusercontent.com`（第 8 行）达成同一目的 |

---

## 5. 无留痕 / 不可核验的部分（**如实登记，不许编**）

| 项 | 状态 | 说明 |
|---|---|---|
| **历史检索的搜索关键词与搜索页 URL** | **无留痕，不可核验** | 仓库内没有浏览器历史、没有搜索日志。任何"第 N 轮搜了 `<关键词>`"的叙述**都不是本片能证的事实**，故本片不写。 |
| **`原版资源/cs16-maps` 的 LFS 完整性** | 部分不可核验 | 该仓是 **shallow + git-lfs** 克隆（`.git/shallow` 与 `.git/lfs/incomplete` 存在），且上游是 **LFS 仓** ⇒ 本地有的文件是当时拉到的那些，**无法证明与上游 commit 完全一致**（需 `git lfs pull` 才能补齐；本片不做写操作）。 |
| **`cs-docker/*.bsp`（9 张社区地图）的原始出处** | 不可核验 | 它们是 docker 镜像构建时从 Steam 拉的，仓内没有哈希清单。 |
| **`de_dust2_scene` 各贴图的上游再上游来源** | 不可核验 | 该仓 README 只说是 Three.js 场景，未声明贴图版权/来源。 |
| **`cs_dust.wad` / `halflife.wad` 本体** | 拿不到（非"没找"） | 只在 BSP 的 `worldspawn.wad` 键里被引用；客户端随包不含 WAD，需 Half-Life 1 本机安装 + 社区包。⇒ 对照表 **BLOCKED-3**。 |

---

## 6. 按载体反推的"可复核检索入口"（**非原始搜索记录**，仅供下一个执行者复现）

> 这一节**不是**当时的检索日志，而是**由已到手载体的身份反推出的复核入口** ——
> 目的是"换台机器能照着再拿一遍"。明确标注为**反推**，不得当"当时的搜索关键词"引用。

| # | 复核入口（关键词 / URL） | 目标 |
|---|---|---|
| R1 | `github.com/TheBestPlan/cs16-maps`（clone，main @ `9d79e0b`） | 大批 CS 1.6 社区地图（含 des 天空 TGA） |
| R2 | `github.com/unimator/cs_16_maps`（clone，main @ `842607a`） | Unimator 的 RMF 源地图合集 |
| R3 | `github.com/wanderer-00/de_dust2`（clone，main @ `023cc9a1`） | de_dust2 位置/道具标注网页工具 |
| R4 | `github.com/1832051637/de_dust2_scene`（clone，main @ `9185ef5`） | Three.js 的 de_dust2 场景（回落贴图来源） |
| R5 | `github.com/coderocker/counter-strike-docker`（clone，master @ `aed2257`） | 无头 CS 1.6 服务端练习环境 |
| R6 | `archive.org` 条目 `cs-109-de_dust2` → `cs-109-de_dust2.zip`（md5 `81f71bb…`） | 官方 de_dust2 BSP（1.0.0.9 时期） |
| R7 | `archive.org` 条目 `cstrike-LiON` → `cstrike-LiON.iso`（md5 `8d1065e9…`） | **全套 CS 1.6 客户端**（模型/音效/贴图/菜单定义） |
| R8 | `github.com/dscharrer/innoextract` Releases v1.9 | 解 Inno Setup 安装器的工具 |
| R9 | `github.com/ValveSoftware/halflife`（HLSDK） | GoldSrc 引擎/pmove 官方源码（物理常量出处） |

---

## 7. 结论行（§4 闸门 3 指定格式）

**① 是否已用上 A 自己的素材：是（级 ①，最高级）。**
角色模型（9 个 mdl）/ 第一人称武器模型（31 个 `v_*.mdl`）/ 音效（869 个 wav 中按配方取 91 个）/
地图几何与内嵌贴图（de_dust2.bsp 逐结构解析）/ 菜单与 HUD 的定义文件（`.res` / `hud.txt`）全部来自 A 的官方客户端本体；
来源与版本已在 §2 用 **git commit + ISO/zip 校验和** 锁定，可复现。

**② 若否 —— 不适用（已用上级 ①）。**
仅有三处缺口，且都不是"找不到"：(a) 帧动画（本项目主动选择静态姿态，登记于 `client/资源欠缺清单.md:39-40`）；
(b) 编译期 WAD 里的未内嵌贴图（A 的客户端随包不含 WAD，登记于 `client/资源欠缺清单.md:45` 与 `策划/对照表.md` BLOCKED-3）；
(c) 落地音（**A 自己就没有**，登记于 `client/资源欠缺清单.md:33`）。
**全程未使用任何通用兜底素材**（KayKit / Quaternius / Kenney / Unity 内置几何体）。
