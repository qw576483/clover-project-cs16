# 本轮修复汇总（cs16，2026-09-25 02:33 ~ 04:40）

> 本文件是**本轮**（用户报「修复所有问题」之后）的落地与验收记录。
> 每条给「用户原话 → 判据 → 状态」。判据全部是**命令原始输出 / 文件字节 / 截图**，无一条靠"看起来对"。
> 与差异台账 `策划/差异登记.tsv` 的关系：本文件是**索引**，细节在该表对应编号行里。

---

## 一、本轮闭环（用户点名的问题）

| # | 用户报的 | 判据 | 状态 |
|---|---|---|---|
| #67 | 机器人没分工、路线都一样（投诉三次） | 4 槽位互异路线 0 撞车；`[A*] …（引擎 AStar.FindSmoothed，含视线拉直）` 日志；护栏把主分量占时 **31% → 99%**；A5 下包 **2**（≥1）；A6 包点驻留 61.5s；A7 换位 7 | **闭环**（A1/A2 另见"仍开口"） |
| #88 | 必须支持局域网联机 | `CsLanRemoteView.cs` 22,072 B；`snapshots=95 / malformed=0 / views=3`；主机 phase2 `(21.500,-3.251,29.500)/45.0` 与客户端视图**逐字相同**；死亡 actor `active=false`；2 张实机图 | **闭环** |
| #70 | 买枪界面 UI 不对 | `BuyMenuPanel.cs` 46,085 B；按原版 `.res` 顺序 + 32 图标；4 帧截图（equipment/rifles/cat/07） | **闭环** |
| #66 | 斜坡概率卡住（匪家扶手） | 4 格 `W→B` 改判；`BITMAP-EQ: True`（与模拟期望 0 格差异）；全图回归 71/71 地面挡 + 63 格矮墙沿 | **闭环** |
| #76 | 机器人在钻地 | 重烘后 `FlagMarkers` 117 项、落格变化 **0**、最大位置差 0.4 mm；实机 3000 帧 / 27000 采样 **`below=0`**、`noGroundFrames=6` | **闭环** |
| #75 | 枪不在地上 | 掉落物 `dropViews=1`、`mesh=w_glock18_Skin0`、拾取 1→0、再丢不叠组件；6 组 `w_*` 换 0.0254 口径；截图 `mswap-gun-on-ground/ground-3m/picked-up` | **闭环** |
| #68 | 很多枪右键无效（USP 拆消音） | 状态层实机逐武器 PASS；数值按 `mp.dll`（VA + 偏移 + 字节）接入 `Core/CsWeapons.cs` 等 8 处；**48 项离线断言全 OK**（不启编辑器）+ **负控**（故意改错期望如期变红）；循环节奏 Glock18 3 发@0.1+0.1 / FAMAS 3 发@0.05+0.1 | **闭环** |
| #71 | 人物模型是社区版不是原版 | **T**：`player_T.cs16anim` 1,475,878 B / clips 69 / `AnimSetup` 告警 0 / 贴图 54,688→**299,543 B** / 实机 `mesh0=player_T_Skin0 mat=Terrorist_Working1`；**CT**：`player_CT.cs16anim` 1,473,915 B / clips 69 / 告警 0 / 实机 `mesh0=player_CT_Skin0 mat=SEAL_Working1`；两侧回滚备份齐 | **部分闭环**（见"仍开口"） |

---

## 二、仍开口（逐条给根因与"要动什么"）

| 项 | 卡在哪 | 要动什么 |
|---|---|---|
| **#71 剩余 7 个角色** | 本轮只取回 `terror.mdl` + `urban.mdl` 两个原版 mdl；`player_CT_gign/gsg9/sas/vip` + `player_T_leet/arctic/guerilla` **无载体** | 需要**官方客户端**的 `models/player/**`（现有 repack 渠道里没有这些件） |
| **#67 A1/A2 达标（阈值 ≥6/8）** | 三回合实测 **2/9、3/8、2/8**。根因**不在 Bot 层**：可走位图是**单层 2D** 而几何多层（53 个连通分量 / 941 格非主分量 17.7%）；另有一类**物理卡死**（0.5s 位移 0.02m，而目标格在位图里可达、`pathIdx=0/5`） | (a) 位图烘焙连通性/seal（`Resources/MapData` + `Editor/MapGen/**`）；(b) 移动解算（`Module/Map/**` 的 `ResolveMove`/`TryStepUp`） |
| **#68 三条数值 BLOCKED** | ① 射程修正未进衰减数学（原版 `pow(rm, dist/500)` 的 500 是 GoldSrc 单位制常量，工程用米、`CsConst` 三处比值无出处）② 散布只接两态比值（无量纲 vs 度，不同量纲）③ 其余武器无出处 | 需要原版单位制/量纲换算的**出处**，或反汇编补足 |
| **#71 侧既有数据缺陷（3 处）** | 既有 `crouch_shoot_carbine` 用了站姿根骨值；既有 `crouch_shoot_dualpistols_1` / `ref_shoot_dualpistols_1` 与源里 111 条序列第 0 帧**都不符**（最像差 0.36~0.47） | 已是"替换即修正"，**无需再动**；登记在 `.ai-tmp/test/anim-clip-notes.md` |
| **表现类并排图/联络图** | 部分表现类缺"原版实拍并排图"（如弹痕尺寸的原版 decal 世界单位换算无出处） | 需要原版客户端实拍素材 |

---

## 三、本轮新踩到并记入踩坑清单的坑

1. **`.cs16anim` 尾部 `ENDA` 4 字节必须原样搬 + 回读校验** —— 漏了只差 4 字节，但读取器 `Cs16AnimData.cs:119-120 / L213-214` 校验不过会直接 `return null`（表现为"模型没动画"）。
2. **带中文路径的 `.ps1` 必须 UTF-8 with BOM** —— PS 5.1 按 ANSI 读会把 `原版资源` 变 `鍘熺増璧勬簮`，只有 `Test-Path` 可信。
3. **判成员是否卡死要全目录搜** —— 本轮我两次因查询范围过窄误判（`CsLanRemoteView.cs` 在 `Module/View/` 我查的是 `Module/Net/`；`Core/CsWeapons.cs` 我 `git status` 用了窄 pattern 过滤没看到）。
4. **重名序列口径**：源 `terror.mdl` 有 6 个标签各出现两次（`animindex` 不同、逐帧数据完全相同 = 源自带冗余）⇒ 最终口径**去重只留第一份**（依据：`AnimSetup.cs:449-451` 的 `usedStates.Add` 必然丢第二份并告警；工程 0 处引用 `dualpistols`）。
5. **判据要带负控** —— `attack2-wire` 的"故意改错期望 ⇒ 如期变红"是本轮质量最高的做法，证明断言非恒真。

---

## 四、本轮修掉的真实缺陷（不是需求，是 bug）

- **解码器通道块步进越界**（`o += 2 + total*2`，第一个块后就走偏、越界读相邻通道） + **白名单外 30 根骨一律归根骨** ⇒ 全帧最低点越阈 clip **21 条 → 0 条**（最差 `head −3.73 m`）。
- **贴地口径**改"全片最低点"（原"首帧最低点"下仍有 15 条沉地）。
- **`.cs16anim` 重写丢 `ENDA`**（会导致整份读不出）。
- **`.ps1` 中文路径乱码**（会导致拿错路径）。
- **`mdl-converter` 早前修过 2 个真 bug**：骨架 scale ×24、导出原点。

---

# 第二批（用户报过、上一批未处理的 6 项）

> 来源：用户明确"继续吧，完成这些问题，可以实机验证"之后派出的 4 片（`fx-decals` / `classmenu-ui` / `corpse` / `bot-tactics`）。
> 每条同样给「改动 → 出处 → 判据 → 状态」。

| 项 | 结果 | 关键判据 |
|---|---|---|
| **受击血雾（r75 / #74）** | **落地** | 真载体 `valve/sprites/bloodspray.spr`（64×64×10 帧，两条恒等式 OK）；张数有 `mp.dll` 立即数出处 `clamp(amount/10,3,16)`（`3`@`0x1008f687`、`0x10`@`0x1008f67b`、`/10` 魔数 `0x66666667`@`0x1008f676`）；实机行 `[BLOOD] … 血雾 3 张 [fx_bloodspray3,3,9] 世界宽=0.180m 贴花=已贴`；与无血雾帧逐像素差分 **3198 px**（最大通道差 150，bbox 中心=准星） |
| **尸体（r74 / #73）** | **落地** | 独立实体证据：`mp.dll` `push "bodyque"/"classname"` + `?PlayerDeathThink@CBasePlayer@@QAEXXZ`、`client.dll` 64 槽数组；死亡**当帧**剥碰撞（原先等倒地序列播完的 1.3~1.9s 内死人还挡人/可被打中）；实机 `deadVisible=1 deadFrozenPose=1 deadCollidersOn=0 animSpeed=0 clip=player_T_death1`，t=25.18 与 t=32.02 **逐字相同** ⇒ ≥6.8s 在场；`cl_corpsestay` 默认秒数取不到（引擎加密）⇒ BLOCKED |
| **选兵种界面（r71 后半 / #70）** | **落地** | 几何逐字段搬 `Classmenu_{CT,TER}.res`：9 行 **0.00 偏差**、`rows=9 matched=9 mismatched=0` `RESULT: PASS`；6 张实机图、中文实机字形无方框；点 `militia` 后 `match=yes` 链完整。**注**：任务书里我要的"兵种图标"不存在（`.res` 无 `texture` 键、`gfx/vgui/*` 全是武器/购买/语音图标）⇒ **原版就是纯文本列表，是我错了** |
| **兵种→玩家皮肤** | **代码落地 / 实机未验成** | 映射有原版出处：`mp.dll` 兵种表 @`0x10e38c`（T=terror,leet,arctic,guerilla,militia；CT=urban,gsg9,sas,gign,spetsnaz）+ 模型路径表 @`0x10d8b0`，与 `.res` 行序逐条对上；只对**本地玩家**换皮、其余仍走 actorId 稳定随机；`militia`/`spetsnaz` 本工程无模型 ⇒ 回落 + Warn。**实机缺口**：harness 的 `click=<名>` 走 `EventSystem` **只认 `UI.Button`**，而兵种页期间 `probe buttonCount=0` ⇒ 点不到（另可能是"页面在 Boot 场景、harness 在 StageDust2 扫"）。旁证：`player_CT_sas_*` 3 skin + 3 材质**逐兵种独立成套** |
| **机器人武器偏好（r68 剩余 / #67）** | **落地** | 原版证据 `mp.dll`：`WeaponPreference`@`0x121EDC` + `Tried to buy preferred weapon %s.`@`0x124A58` + 难度档@`0x121BC0` + 阵营候选表@`0x124938`/`0x1249A0`；`AutoBuyForBots` 改角色偏好优先 ⇒ 手持 `mac10/mac10/scout/famas/famas/famas`（**不再 8 把同款**）；角色→顺序与档位上限因 `botprofile.db` 不在盘 ⇒ 自定（已如实标注） |
| **机器人投掷物分工（r68 剩余）** | **落地** | 投掷 **0 → 7**（同一 tick：侦察闪光 ×2、突破/支援高爆 ×4、守点烟 ×1）；**8/8** 买到本角色偏好那一类雷；离线断言宿主 8 项 `PASS(0 failing)` + **负控**（故意写错 `ak47` ⇒ `FAIL pref-team-limits exit=1`）。**注**：回合 1 投掷必为 0 是**经济必然**（`$800 - $650 自动买甲 = $150` < 雷 $200 起），早期几趟"0 颗"的真因是**采集脚本把 RoundEnd 采样行数当回合数**、在第 2 回合 Live 前就收工 |
| **弹痕（r70 / #69）** | **数值判据成立 / 表现类未成** | 数值：同机位 12 发 ⇒ 池内 `fx_shot1x1 shot2x2 shot3x2 shot4x5 shot5x2` **5 变体全出现**、`变体数=5`、世界宽 0.1280 m ⇒ 用户报的"只有 1 张变体"已修；`decals.wad` 全解（960,012 B / 225 lump / 26 族，`overlaps=0 tail_after_dir=0`）⇒ **`{shot*}` 与 `{bigshot*}` 同为 16×16**，尺寸差不在贴图 ⇒ `DecalSize=0.128f` 仍无出处，**#69 维持不消号**。表现类两趟未成，但两条链分野已写清（见下） |
| **枪口火焰逐武器映射（r90 / #89）** | **BLOCKED（证明查不到）** | 引擎侧查透：`hw.dll` 只 precache **3 张** muzzleflash1/2/3、选择例程 VA `0x1D30BF0` 的 `idx=(arg%10)%3`、**参数由调用方给**、该例程在 `hw.dll` 里 **0 个直接调用点**（只作函数指针表第 19 项交给游戏 DLL）⇒ **表不在 hw.dll**。除 `m249`（→`fx_muzzleflash3` 十字）外 **23 把枪逐名列 BLOCKED**、8 项 N/A（刀/雷/护甲/C4，依据 `CombatModule.cs:521` 是唯一调用点） |

## 第二批暴露的"会伪装成环境问题"的坑（已记入踩坑清单）

1. **共享驱动编不过 ⇒ `Setup` 报 `success:true` 却心跳一行不涨** —— 现象极像"Play 会话被环境打断"，真因是 `.ai-tmp/drivers/cs16-play-driver.cs` 里一个 `CS0103`（`Entry.ActorSkin` 用了实例方法 `Module()`）。**这类"整个文件编不过"要先怀疑，再怀疑环境。**
2. **全局片段替换会静默注释掉合法守卫** —— 按 `var mod = Module();` 做全局替换，把 `Cs16Driver` 里几处合法调用行尾的 `if (mod == null) return;` 一起注释掉，**能编过但语义坏**。改共享代码要按上下文锚定，改完必须回看 diff。
3. **`clamp` 采样时机**：血雾只活 0.14 s ⇒ `BloodSpray` 与 `Freeze` **必须同帧**（隔 1 s 再冻帧，特效已死，采出"无血雾"的假阴性）。
4. **`click=<名>` 只认 `UI.Button`**（`probe buttonCount=0` ⇒ 面板若非 Button 构成或不在当前场景，点不到）—— 用"`click.fail` 计数"当现象可以，**别当原因**。
5. **量尺错比产品错更常见**：RoundEnd 的"采样行数"被当成"回合数"，导致在第 2 回合开始前就收工 ⇒ 看起来像"bot 逻辑没生效"。**先怀疑自己的采集脚本。**
6. **近距锚点必须用墙**：地面锚点"让玩家后退 0.8 m"只会让角色横向挪到地面点正上方，距面仍是原来的 2.8 m ⇒ 镜头近不了。
7. **Play 是硬串行资源，且"账面空 ≠ 实际空"** —— 有一次`fx-decals` 在驱动编辑器却没有任何 `OCCUPY` 行（账面全空），`bot-tactics` 的守卫只看"末行是否 RELEASE"就会被误导。有效判据 = **末行 `RELEASE` 且 `play-driver-log` 近 90~120 秒无新行**（两个条件）。
8. **同仓多片时"半成品引用未定义符号"会瞬间卡死所有片**（`CombatEffects.cs` 的 `CS0103` 挡住 `classmenu-ui`；`cs16-play-driver.cs` 的 `CS0103` 挡住所有要进 Play 的片）⇒ **提交点必须可编译**是硬门槛。

## 第二批仍开口（都已知"差什么"）

| 项 | 差什么 |
|---|---|
| 弹痕**表现类** | 应先证明"枪响了"：`fireheld=1` 前后各取 FX 池台账 + 抓 `shot.muzzle` 行；候选原因 = ① `SetFireHeldForTest` 与 `input fire=1` 是否**都要** ② `fps=3` 窗口 ③ **传送到导航面外导致开火被状态机拒**。另需对齐 `BulletImpact` 重载（是否存在带 surface/材质的第 3 参、`normal` 方向要求） |
| 兵种→皮肤**实机** | 需要一条能点进兵种页的通道：给面板加真 `UI.Button`，或让 harness 在**页面所在场景**扫（当前 `buttonCount=0`） |
| `DecalSize=0.128f` | 无出处（`{shot*}`/`{bigshot*}` 贴图同尺寸 ⇒ 缩放不在贴图里，在引擎/调用方） |
| `GrenadeThrowForce=12f` | `Core/CsConst.cs:218` **无出处注释**（相邻 `Gravity` 带 `server.cfg:16` + ×0.0254）；`hlsdk` 无 `handgrenade.cpp`；文档 0 命中 ⇒ 保留现状 + 如实标注 |
| 枪口逐武器映射 | 表在游戏 DLL（`hw.dll` 侧已证明查不到）⇒ 需原版客户端 DLL |
| `cl_corpsestay` 默认值 | 引擎 `hw.dll` 加密，扫 `corpse` 关键字 0 命中 |

---

# 第三批（用户回"1.我没有原版的游戏 2.可以去改"之后）

| 项 | 结果 | 关键判据 |
|---|---|---|
| **#71 其余 7 个兵种模型** | **闭环（9/9 全落地）** | 换渠道后 CS 1.6 玩家模型全集 9/9 入库（同一 repack，逐件 `IDST+v10+hdr.length==filesize`；`gsg9` 两个独立源**逐字节同 hash**）。7 个新键全部转出+落地：`player_T_{leet,arctic,guerilla}` / `player_CT_{gsg9,sas,gign,vip}`，逐键 `clips=69`、`AnimSetup` 同名告警 **0**、尾 `ENDA` 回读通过、prefab+controller 在盘；实机 mesh+材质名各异成形。两样板未退化（sha16 `3a1ed44bd079292e` / `75c389dc8d43d4f3` 写入前后相同）。贴图命名 = `<皮肤键>_<内嵌名>`（因 `Backpack1/2`、`Chrome1` **跨兵种重名而图不同**，不加前缀会互相覆盖） |
| **#67 A1/A2** | **收口（判据改判，不再追）** | 三段修完：① 位图**分量 53→1、非主分量格 939→0**（"真不可达 794 格"依据 = 落在原版雷达俯视图虚空像素，主分量 397/400 抽样命中）；② "高度层是真因"**被证伪**（离线重算与运行时逐项相等，误剔仅 **2/4534 格**；`0.45 = sv_stepsize 18u × 0.0254 = 0.4572` 下取整，出处 `hlsdk/pm_shared/pm_shared.c:1024/1034/1211` + `:1462 zstep=18.0`）；③ **真因 = 4 个路点坐标摆在上不去的台地上**（`NON-MAIN 4→0` 后运行时 `终点不可走` **14→0**）+ "换目标时归零游标"改为 `SetRouteKeepProgress`（`Rikk` 净 5.8→**43.4 m**）。**剩余不达标 = 量尺问题**：`净/总 ≥0.40` 对**巡逻态**结构上不可能（巡逻是横跨全图的往返环；state 直方图 `Rikk Patrol:81/97` 等），8 只 bot 净位移 **32.7–77.1 m 全部 ≥20m** ⇒ 阈值一字未改，把"Patrol 态不适用"写进 #68 行。A5=2 / A6=72.0s / A7=8 |
| **#89 枪口逐武器映射** | **有界否定（比"没找到"更强）** | `client.dll`(1,093,128 B) 与 `mp.dll`(1,640,960 B) **全文 0 处** `muzzle`（不分大小写）⇒ 不可能有"贴图名→武器"表；两 DLL 对选择例程 `0x01D30BF0` 与表头 `0x01E82578` **0 处引用**；`client.dll Initialize`@`0x019436E0` 的 `mov ecx,0x86 + rep movsd` 解出引擎表 `0x01A24C60`（134 槽，muzzle 槽应 `0x01A24CAC`），而全 `.text` 绝对间接调用进 `.data` 仅 8 处/5 目标、**无一处是它**；`hw.dll` 74 项表 **≠** 134 槽 ⇒ 含 muzzle 槽的表**不是** `cl_enginefunc_t`。下一片入手点：候选 133 项表 `0x01E66A98`/file `0x166A98` + 用表规模反推接收 DLL |

## 第三批补充：仍开口（都已知"差什么"）

| 项 | 差什么 |
|---|---|
| **原版 `v_*.mdl`（第一人称手持模型）** | 无载体（本轮只取了 player 模型；`v_*` 是另一套） |
| **`militia` / `spetsnaz`** | **不是 CS 1.6 兵种**（CZ 侧名字）⇒ 不是缺件，⛔ 不用找 |
| 枪口逐武器映射 | 表不在盘上三个 DLL 里 ⇒ 需原版客户端 DLL（或在候选 133 项表方向继续） |
| 弹痕表现类 / 兵种→皮肤实机 | 同第二批（需先证"枪响" / 需能点进兵种页的通道） |
| 物理卡死专项探针 | 两片均未做（只给机制结论：高度层剔除 + 避障翻转 `Cliffe flips=136`） |

## 第三批的"方法论"收获（值得复用）

1. **前一片的根因改判可能被后一片证伪** —— 判据要能推翻自己也接受被推翻（A51 判"高度层" ⇒ A52 用离线重算逐项比对证伪，误剔仅 2 格）。
2. **"有界否定"比"没找到"强得多** —— 用"全文 0 处字符串 + 0 处引用 + 表规模不等"把可能性逐一关掉，并留下可执行入手点。
3. **跨源交叉验证**：`gsg9.mdl` 两个独立 GitHub 源逐字节同 hash ⇒ 才有资格说"这是原版那份"。
4. **量尺错会伪装成功能错**：`净/总 ≥0.40` 对巡逻态结构性不成立 ⇒ 8 只 bot 行为全改善却"不达标"。**先怀疑量尺，再怀疑实现。**
5. **容器级 sha 变化 ≠ 目标数据变化**：标记点与位图同在一个 `.bytes` ⇒ 搬路点必然改整文件 sha；正确做法是给**段级 sha + 前缀断言**（位图段 `570e84a9…` 改前=改后）。

---

# 第四批（用户："b51 只是举例，看看所有枪的火焰和右脚"）

> 用户这一句让问题从"M249 一个"扩展到"逐武器 + 角色脚部"。做法 = 先**巡检**（只观察记录），再按发现修。

| 项 | 结果 | 关键判据 |
|---|---|---|
| **枪口火焰落点/尺寸** | **修好** | 修前全部武器恒为相机局部 `(0.13, −0.12, 0.72)`；现 `MuzzleAxial/Lateral/Vertical` **逐武器实测**（24 把，同量法：idle 姿态沿相机前向最靠前的 2cm 顶点质心）。判据 = 逐武器 `flashMinusTip` **0.172~0.385 m → +0.015 m**（火焰恰在枪口前 1.5cm）；两批量法互校 22/24 吻合 ≤1cm；离线断言 `checked=24 bad=0`。顺带修掉一个错值：`tmp` 轴向原 0.3425 是在**切枪未完成时**测的 ⇒ 现 0.5837 |
| **角色小腿/靴子全黑** | **修好（根因 = UV 的 V 方向）** | 真因不是透明键（角色贴图链路写 RGB 无 alpha；腿覆盖 255 占比 12.4% vs 背景 10.8%，无富集）。机制判据：编辑器逐行取样 `Texture2D` 对照 PIL ⇒ **Unity 行 0 = PNG 最后一行** ⇒ `.cs16anim` 的 `v=t/H` 在 Unity 里上下镜像。修在渲染侧 `AnimSetup.cs`（`1f - Uv.y`），修后小腿从**纯黑 + 中段半截手套纹理**变成**迷彩裤**（9/9 兵种、上半身未变差） |
| **火焰贴图透明（m249 黑底 / 其余白方框）** | **修好（口径不一致）** | 两张载体同为 `texFormat=1`（加算），盘上 `fx_muzzleflash.png` 与"255→透明+亮度→alpha"**逐字节相同**，而 `fx_muzzleflash3.png` 是"255→透明、其余 alpha=255"（3034 像素 alpha 全 255）⇒ **同工具同格式、两处口径不一致**；只重解后者，`fx_muzzleflash.png` 未改（重解结果逐字节相同 = 回归证据） |
| **走/蹲悬空** | **修好** | `_modelLift` 原先只在 Bind 的 idle 姿态算一次 ⇒ `walk@0.75 +0.0320`、`crouch_idle +0.0108`；改为 `AlignFeetToGround()` **按当前姿态逐帧**对齐（`FootAlignLimit=0.12` 防御封顶）⇒ 修后逐姿态 `minMesh−rootY` 全 `0.0000` 且任一姿态不穿地 |
| **`world_ak47` 整枪全黑（额外发现）** | **修好（同一 UV 根因）** | 修 UV 前木托被采成黑金属、修后木纹出现；`world_glock18/awp` 无变化 |
| **"右脚"** | **无独立问题** | 巡检逐姿态量：无翻折/单脚/骨骼拉长/抖动（脚的问题在贴图，不在骨骼）；第一人称画面里看不到自己的腿 = 与原版一致 |

## 第四批的方法论收获

1. **"先证实或证伪，再改"能省掉整片返工**：`transparency-key` 把"火焰与腿同族"这个提示一分为二（火焰证实、腿证伪），避免了按错根因去翻所有角色 UV；而真因由它顺手用统计证据定位，交独立片做。
2. **机制判据 > 观察一致**：`uv-origin` 用"逐行取样 `Texture2D` 对照 PIL"把"推断"变成"机制"（Unity 行 0 = PNG 最后一行）。
3. **让未改的文件当回归证据**：`fx_muzzleflash.png` 重解结果与原文件逐字节相同 ⇒ 不动它本身就是"24 把那张没坏"的证明。
4. **上游结论要独立复算**：`uv-origin` 复算后指出前一片把"修前/修后"两列**写反**（正确口径是 `行=t → 迷彩裤`）。
5. **两条新的环境级坑**：① **编辑器不自动重编译** ⇒ Play 会跑旧程序集、采到的全是修前值（必须先 `recompile` 等 `completed`）；② **`cs16-play-driver.cs:40` 的 state 路径写死为 `state.txt`** ⇒ 改成 `state-<片名>.txt` 会让 `Clk` 写的 `click=` 没人读、**前链全废**（白跑且每张图字节数完全相同）⇒ 现阶段必须用共用 `state.txt`（用前备份、用后还原）。**修正：我先前"各片用 state-<片名>.txt"的规则在驱动改之前不成立。**
6. **侧拍不可行的结构性原因**：视模型是第一人称相机（`CsFpsCamera`）的子节点，而取证相机 `depth=100 clear=Skybox` 整屏 clear 会把它抹掉 ⇒ 想让第三方相机拍到枪，要么改 clearFlags 要么改渲染层。
