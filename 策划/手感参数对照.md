# 手感参数对照（原版反汇编 vs 本工程）

> **本片性质**：只读取证。**不改任何代码**。差值本身不是判决 —— 改不改由后续片决定。
>
> **口径与载体**
> - 原版载体 = `原版资源/cs16src/cstrike/dlls/mp.dll`（1,640,960 B，ImageBase `0x10000000`）。
>   **文件偏移换算**：`.text`（VA `0x10001000`..`0x10105800`）= VA − `0x10000C00`；
>   `.rdata` / `.data`（VA `0x10106000` 起）= VA − `0x10001400`。
>   逐条出处都给了 `VA` + `file` + **原始字节**（hex），可原样复算 —— 复算法见文末 §Z。
> - 工程载体 = `client/Assets/Scripts/**`（Git 仓库路径 `CL/Scripts/**`）。
> - ⛔ 本表**不用** `hlsdk/**` 冒充 CS 出处（hlsdk 只用于"机制形状"，凡引用都标明）。
> - ⛔ 标注 `BLOCKED` 的 = 两边至少一边写不出出处；**本表不填"看起来合理"的数**。

---

## 1. 移动

| 项目 | 原版值 | 出处（文件 + 偏移 + 字节/反汇编） | 工程值 | 出处（文件:行） | 差值 | 结论 |
|---|---|---|---|---|---|---|
| 最大速度（枪械档）`GetMaxSpeed` 家族 | **每把枪写死为代码浮点常量**（权威归属见 §5.4，按"该类 `WeaponModel` 槽 = `models/p_<枪>.mdl`"逐类确证）：`AK47` **221**、`M4A1` **230**、`M3` **230**、`AUG`/`FAMAS`/`Galil`/`XM1014` **240**、`P90` **245**、`UMP45`/`TMP`/`MP5`/`MAC10`/`Elite` **250**（手枪系与刀同属"未覆盖、继承默认 250"）、`M249` **220**、`AWP`/`SG550`/`G3SG1` **210**、`Scout` **260 或 220**、`SG552` **235 或 200**（后两者看 `[player+0x5ac]==0x5A`）<br>⚠️ 本节上一版的 `Scout 245 / MAC10 230 / M4A1 240 / Galil 210` 是**被相邻函数字符串污染**的误判，已按槽 61 更正 | 常量：`mp.dll` VA `0x101421F8`=240 / `0x10142200`=250 / `0x101421F0`=230 / `0x101421E0`=220 / `0x101421E4`=221 / `0x101421FC`=245 / `0x1014220C`=260 / `0x101421DC`=210 / `0x101421F4`=235 / `0x101421D8`=200（file `0x140DF8`/`0x140E00`/`0x140DF0`/`0x140DE0`/`0x140DE4`/`0x140DFC`/`0x140E0C`/`0x140DDC`/`0x140DF4`/`0x140DD8`，字节 `00007043`/`00007a43`/`00006643`/`00005c43`/`00005d43`/`00007543`/`00008243`/`00005243`/`00006b43`/`00004843`）。取值函数（`fld dword[abs]; ret`）：240 → VA `0x100B52E0` file `0x0B46E0` 字节 `d905f8211410c3`；250 → `0x100B52F0`/`0x0B46F0` `d90500221410c3`；230 → `0x100B5310`/`0x0B4710`；220 → `0x100B5320`/`0x0B4720`；221 → `0x100B5340`/`0x0B4740`；245 → `0x100E1870`/`0x0E0C70`；260 → `0x10049860`/`0x048C60`。带分支：`0x100D59C0`/file `0x0D4DC0` 字节 `8b81a400000083b8ac0500005a7507d905dc211410c3d905c0211410c3` = `if (*(int*)(*(void**)(this+0xa4)+0x5ac)==0x5A) return 210; return 220;`；`0x100E2220`/`0x0E1620` = 260/220；`0x100E34F0`/`0x0E28F0` = 235/200；`0x100D66E0`/`0x0D5AE0` 字节 `80b940010000007403d9e8c3d90500221410c3` = `if (*(char*)(this+0x140)) return 1.0f; return 250;` | `SpeedKnife=5.4`（= **212.6 u/s**）、`SpeedPistol=5.2`（**204.7 u/s**）、`SpeedRifle=4.4`（**173.2 u/s**）、`SpeedAWP=3.6`（**141.7 u/s**） | `CL/Scripts/Core/CsConst.cs:90-93`；选用点 `CL/Scripts/Module/Match/CsInventory.cs:67-95` | 手枪档 −45 u/s（−18%）、步枪档 −77 u/s（−31%）、Scout 档 −113 u/s（−46%）、AWP 档 −68 u/s（−32%） | **不一致（工程普遍偏慢；步枪档差最大）** |
| 归属未确证的取值 | 只剩 1 个 **260**（函数 VA `0x10049860` file `0x048C60`）—— 它所在的 vtable（`0x10133218`）**没有 `WeaponModel` 槽**（非武器类，疑为 shield / VIP / 玩家基类），**无任何武器归属** | 240 的归属已解（见 §5.4：`0x100B52E0` 被 **AUG / FAMAS / Galil / XM1014** 四类共用） | — | — | — | 未取到（该 260 不属任何枪，本片不再追） |
| 全局速度钳制 `sv_maxspeed` | `320` units/s = 8.128 m/s | `原版资源/cs16src/cs16game/app/cstrike/server.cfg:15`（`listenserver.cfg:15` 同值）；引擎出厂 `320` · `hw.dll.orig:0x18b208` | **无全局钳制**（只按武器档给上限） | `CL/Scripts/Core/CsConst.cs:90-93` | 工程无此闸门（对 320 本身不构成超限，属"缺一层上限"） | 不一致（缺机制） |
| 地面摩擦 `sv_friction` | `4` | `原版资源/.../server.cfg:33`；引擎出厂 `4` · `hw.dll.orig:0x189e60`；实现 `HLSDK/pm_shared/pm_shared.c:1202`（**HLSDK 只证机制形状，数值取 server.cfg / 引擎**） | **未实现**（无对应常量） | `CL/Scripts/Core/CsConst.cs`（无此常量）；移动积分只有重力一项 · `CL/Scripts/Module/Match/CsMatch.cs:2130` | 缺整层"松手后减速" | **不一致（未实现）** |
| 地面加速度 `sv_accelerate` | 随包 `5`；引擎出厂 `10` | `原版资源/.../server.cfg:30`；`hw.dll.orig:0x18b220`；实现 `pm_shared.c:990` | **未实现**（速度直接设为目标值，无"逐帧逼近"） | 赋值 `CL/Scripts/Module/Match/CsInventory.cs:67-95` + 积分 `CsMatch.cs:2130`（无加速度项） | 缺"起步加速曲线" | **不一致（未实现）** |
| 空中加速度 `sv_airaccelerate` | `10` | `原版资源/.../server.cfg:31`；引擎出厂 `10` · `hw.dll.orig:0x189f38` | **未实现**（离地只受重力） | `CL/Scripts/Module/Match/CsMatch.cs:2130-2134` | 缺空中转向能力 | **不一致（未实现，跳跃手感影响大）** |
| 重力 | `800` units/s² = **20.32 m/s²** | `原版资源/.../server.cfg:16`；引擎出厂 `800` · `hw.dll.orig:0x189e90` | `20.32` m/s² | `CL/Scripts/Core/CsConst.cs:110`；应用 `CL/Scripts/Module/Match/CsMatch.cs:2130` | **0**（折算后一致） | 一致 |
| 跳跃初速度 | `268.328` u/s = **6.82 m/s** | `HLSDK/pm_shared/pm_shared.c:2596`（`sqrt(2*800*45)`；**HLSDK ⇒ 只证机制形状**，CS 侧未单独取证） | `6.82` m/s | `CL/Scripts/Core/CsConst.cs:103`；应用 `CL/Scripts/Module/Match/CsMatch.cs:1082/2093/2697` | **0** | 一致（但出处是 HLSDK，非 CS） |
| 台阶高度 `sv_stepsize` | `18` units = **0.4572 m** | 引擎 cvar 默认 18（对照表 N-24 已登记，出处同那里） | `0.45` m | `CL/Scripts/Core/CsConst.cs:112`；应用 `CL/Scripts/Module/Match/CsMatch.cs:2161/2190/2194` | **−0.0072 m**（−1.6%） | 不一致（量级极小） |
| 站高 / 蹲高 / 眼高 | 站 `72` units = 1.829 m、蹲 `50` units = **1.27 m**（`VEC_DUCK_HULL_MIN -18` ~ `VEC_DUCK_HULL_MAX 32`）。⚠️ **旧稿写的「蹲 `36` units = 0.914 m」已推翻** —— 那是站立 hull 的上限，不是蹲高；旧稿引的 `pm_shared.c:123` 实为 `STEP_SNOW` | `HLSDK/pm_shared/pm_shared.c:83-91`（逐行原文：`VEC_HULL_MIN/MAX = -36/36`、`VEC_DUCK_HULL_MIN/MAX = -18/32`） | 站 `1.80`、蹲 `1.25`、眼 `1.62`、半径 `0.36` | `CL/Scripts/Core/CsConst.cs:86-89` | 站 −0.029 m；蹲 **+0.02 m**（1.25 vs 1.27，比例 50/72 = 0.694 ⇒ 1.80×0.694 = 1.25） | **一致（本行结论已推翻）** |
| 下蹲速度倍率 | `0.333` | `HLSDK/pm_shared/pm_shared.c:123`（机制形状） | `0.34` | `CL/Scripts/Core/CsConst.cs:97`；应用 `CL/Scripts/Module/Match/CsInventory.cs:100` | +0.007 | 不一致（极小） |
| 慢走（Shift）倍率 | **BLOCKED**：原版慢走倍率的实现载体（GoldSrc 客户端 / `pm_shared` 的走向分支）现不在盘，取不到 `file:offset` | — | `0.42` | `CL/Scripts/Core/CsConst.cs:96`（注释自陈"本项目新增"）；应用 `CL/Scripts/Module/Match/CsInventory.cs:101` | — | BLOCKED（原版值取不到） |
| 安全坠落速度 / 致死坠落速度 | 安全 `580` u/s = 14.73 m/s、致死 `1024` u/s = 26.01 m/s | `HLSDK/pm_shared/pm_shared.c:116/115`（机制形状） | `MaxFallSpeed = -30` m/s（**语义不同**：速度钳制 vs 伤害阈值）、无坠落伤害 | `CL/Scripts/Core/CsConst.cs:111` | 语义不可直比 | 不一致（语义不同） |

### 1.1 `GetMaxSpeed` 归属确证依据（本片方法，可复算）

> ⚠️ **本小节的方法已被 §5.4 取代**（槽 89 = `Precache` 的字符串会被相邻函数污染，实测误判过 4 把枪）。
> 保留在此只为说明"槽位 78 = `GetMaxSpeed`"这个结构事实。

- 武器类的 vftable 在 `.rdata`，步长 `0x17C`（95 槽）；`GetMaxSpeed` 固定落在**槽位 78**，
  `Precache` 落在**槽位 89**（= `GetMaxSpeed` 槽地址 + `0x2C`）。
- 因此：取值函数指针所在地址 `G` ⇒ 该类 `Precache` 指针在 `G+0x2C`；把那个函数里的
  `models/w_*.mdl` / `models/v_*.mdl` / `weapons/*.wav` 串读出来即得武器归属。
  实测样例（`G` → `Precache` 首串）：`0x1013D9DC` → `models/w_usp.mdl`（USP=250）；
  `0x1013E444` → `models/w_scout.mdl`（Scout=245）；`0x1013DFCC` → `models/w_sg552.mdl`（SG552=210）；
  `0x1013E148` → `models/w_sg550.mdl`（SG550=260/220）；`0x1013F1A4` → `models/w_glock18.mdl`（Glock18=240）；
  `0x1013F320` → `models/w_galil.mdl`（Galil=210）；`0x1013E8B8` → `models/w_m3.mdl`（M3=220）；
  `0x1013EA34` → `models/w_mac10.mdl`（MAC10=230）；`0x1013E5C0` → `models/w_p228.mdl`（P228=250）；
  `0x1013E73C` → `models/w_mp5.mdl`（MP5=250）；`0x1013DB58` → `models/w_ump45.mdl`（UMP45=250）；
  `0x1013D6E0` → `models/w_m4a1.mdl` + `weapons/m4a1-1.wav`（M4A1=240）。
- 反汇编脚本：`.ai-tmp/test/feel/{spd.py,mapms.py,pre.py,verify.py}`（本片判据资产）。

---

## 2. 射击

| 项目 | 原版值 | 出处（文件 + 偏移 + 字节/反汇编） | 工程值 | 出处（文件:行） | 差值 | 结论 |
|---|---|---|---|---|---|---|
| **连发散布递增模型（"第一枪准"）** | `m_flAccuracy = base + shots³ / div`，再钳到 ≤ `1.0`。**M4A1 = `0.3 + shots³/220`**（`shots = m_iShotsFired`，成员在 `this+0x100`，结果写 `this+0xF8`） | `mp.dll` M4A1Fire VA `0x100DEF90`（file `0x0DE390`）：`0x100DEFF8 fmul dword[0x1014199C]`（= **1/220**，常量 file `0x14059C` 字节 `09f2943b`）；`0x100DEFFE fadd dword[0x10141A8C]`（= **0.3**，常量 file `0x14068C` 字节 `9a99993e`）；`0x100DF004 fst dword[edi+0xF8]`；`0x100DF00A..0x100DF021 fld1/fcom/fnstsw/test ah,5/jp` = `if (acc>1) acc=1` | **无此模型**：工程用"每发 +0.15°、上限 2.4°"的线性惩罚替代 | `CL/Scripts/Module/Combat/CsCombatTuning.cs:72`（`RecoilSpreadPerShot=0.15`）、`:76`（`RecoilSpreadMax=2.4`）；应用 `CL/Scripts/Module/Combat/Firearm.cs:112-113` | 机制不同：原版是 **3 次方**增长、且 base/div 逐枪不同；工程是线性封顶 | **不一致（机制级差异）** |
| 其它武器的 base / div（**值已取到、归属未逐把确证**） | 已读出这些组合：`0.35 + 1/200`、`0.3 + 1/215`（2 处）、`0.4 + 1/175`、`0.45 + 1/220`、`0.45 + 1/175`、`0.5 + 1/210`、`0.6 + 1/200`、另有多组 `0.9 / 0.92 / 0.98` 基准的手枪/狙击族 | 扫描判据 = `fst dword[reg+0xF8]` 前 0x40 B 内的 `fmul/fadd [abs]`（脚本 `.ai-tmp/test/feel/acc.py`）。站点（`fst` 的 VA / file）：`0x100D3FF0`/`0x0D33F0` = 1/200+0.35；`0x100D4A10`/`0x0D3E10` = 1/215+0.3；`0x100D7F90`/`0x0D7390` = 1/215+0.3；`0x100DDCB0`/`0x0DD0B0` = 1/175+0.4；`0x100DEF90`/`0x0DE390` = 1/220+0.3（**M4A1**）；`0x100DF920`/`0x0DED20` = 1/200+0.6；`0x100E01C0`/`0x0DF5C0` = 1/220+0.45；`0x100E1370`/`0x0E0770` = 1/175+0.45；`0x100E2FD0`/`0x0E23D0` = 1/220+0.3；`0x100E4B10`/`0x0E3F10` = 1/210+0.5 | 同上一行的线性模型（无逐武器 base/div） | `CL/Scripts/Module/Combat/CsCombatTuning.cs:72,76`；逐武器基础散布 `CL/Scripts/Core/CsWeapons.cs:154-183` | — | 未取到（**归属**未确证；值本身已取到） |
| 射速（循环时间）M4A1 | **`0.0875` s**（≈685.7 rpm） | 常量 VA `0x10141A2C`（file `0x14062C`，字节 `3333b33d`）；读点 `0x100DF27E fld dword[0x10141A2C]`（file `0x0DE67E`） | `Rpm = 666` ⇒ `60/666 = 0.09009` s | `CL/Scripts/Core/CsWeapons.cs:171`；换算 `CsWeapons.cs:108`（`SecondsPerShot`）/`:366-372`（`CycleTimeFor`） | **+0.0026 s**（射速 −3%） | 不一致（小） |
| 射速 FAMAS | **`0.0825` s**（≈727 rpm） | 常量 VA `0x10141A24`（file `0x140624`，字节 `c3f5a83d`）；读点 `0x100D7FC6 fld dword[0x10141A24]`（file `0x0D73C6`） | `CycleTime = 0.0825` s | `CL/Scripts/Core/CsWeapons.cs:283`；取值 `CsWeapons.cs:370` | **0** | **一致** |
| 射速 USP | **`0.15` s**（400 rpm） | 常量 VA `0x10141A4C`（file `0x14064C`，字节 `9999193e`）；读点 `0x100E5763 fld dword[0x10141A4C]`（file `0x0E4B63`） | `Rpm = 352` ⇒ `0.17045` s | `CL/Scripts/Core/CsWeapons.cs:155` | **+0.0205 s**（射速 −12%） | 不一致 |
| 射速 Glock18 | **`0.15` s**（400 rpm） | 同族常量 `0x10141A4C`（`fld 0.15` 亦见 Glock18 反汇编 `.ai-tmp/test/attack2/glock18.txt`） | `CycleTime = 0.15` s（覆盖 `Rpm=400`） | `CL/Scripts/Core/CsWeapons.cs:301`；取值 `CsWeapons.cs:370` | **0** | 一致 |
| 射速 AK47 / 其余 20+ 把 | **BLOCKED**：AK47 的 `PrimaryAttack` 未反汇编（本片只处理了盘上既有转储的 m4a1 / usp / glock18 / famas 四把） | — | `AK47 Rpm = 600` ⇒ 0.1 s | `CL/Scripts/Core/CsWeapons.cs:169` | — | BLOCKED（原版未取） |
| 首发精度 / 静止散布的**绝对角度** | **BLOCKED**：原版首发散布 = `flSpread` 实参（形如 `0.02 + 0.4 * m_flAccuracy` 的表达式）与 `VECTOR_CONE_*` 常量；本片只在 M4A1 段读到形参入口（`[ebp+8]`），未取到每把枪的实参常数 | M4A1Fire 入口 `0x100DEFC2 fld dword[ebp+8]`（file `0x0DE3C2`） | 逐武器 `Spread`（度）：USP 0.50 / M4A1 0.40 / AK47 0.45 / AWP 0.10 … | `CL/Scripts/Core/CsWeapons.cs:154-183` | — | BLOCKED（原版未取） |
| **后坐力 `punchangle`（每发抬升）** | **已取到**（13 把步枪/冲锋枪/机枪，逐把 6 参数 + 方向，见 §5.5）：AK47 `(1.5, 0.45, 0.225, 0.05, 6.5, 2.5, 7)`、M4A1 `(1, 0.45, 0.28, 0.045, 3.75, 3, 7)`、AUG `(1, 0.45, 0.275, 0.05, 4, 2.5, 7)`、Galil `(1, 0.45, 0.28, 0.045, 3.75, 3, 7)`、SG552 `(1, 0.45, 0.28, 0.04, 4.25, 2.5, 7)`、FAMAS `(1, 0.45, 0.275, ?, 4, 2.5, 7)`、MP5 `(0.9, 0.475, 0.35, 0.0425, 5, 3, 6)`、TMP `(1.1, 0.5, 0.35, 0.045, 4.5, 3.5, 6)`、UMP45 `(0.125, 0.65, 0.55, 0.0475, 5.5, 4, 10)`、MAC10 `(1.3, 0.55, 0.4, 0.05, 4.75, 3.75, 5)`、P90 `(0.9, 0.45, 0.35, 0.04, 5.25, 3.5, 4)`、M249 `(1.8, 0.65, 0.45, 0.125, 5, 3.5, 8)`；手枪/狙击/霰弹已补全（§5.8：只给 `up_base`，其余全 0）；`Glock18` 仍 BLOCKED | `mp.dll` 各武器 `fire` 函数内对 `0x10097F10` 的调用点（26 个），如 AK47 `0x100D4321`（file `0x0D3721`）、M4A1 `0x100DF325`（file `0x0DE725`）；参数常量 VA 逐条列在 §5.5 | 逐武器 `RecoilVert/RecoilHoriz`（度）：AK47 2.00/0.70、M4A1 1.80/0.60、Deagle 2.00/0.60、AWP 4.00/1.20 … | `CL/Scripts/Core/CsWeapons.cs:154-183`；表现层增益 `CL/Scripts/Module/Combat/CsCombatTuning.cs:45`（`RecoilViewScale=1`）、`:49`（`RecoilRiseTau=0.035`）、`:53`（`RecoilFallTau=0.13`） | 量纲/模型都不同（原版是 6 参数 kickback + 逐发累加，工程是"每发固定度数"） | **不一致（原版参数已取到，可直接照搬改）** |
| 后坐力"回落曲线" | **BLOCKED**：原版是"每发角度累加 + 客户端逐帧插值"，没有"上升/回落时间常数"这种量 | — | `RiseTau=0.035` / `FallTau=0.13` | `CL/Scripts/Module/Combat/CsCombatTuning.cs:49,53` | 机制不同 | 不一致（机制不同） |
| 开镜 / 蹲 / 空中 的散布倍率 | **BLOCKED**：原版这三档体现在 `flSpread` 实参的分支与 `m_flAccuracy` 钳制里，未取到 | — | `ZoomSpreadScale=0.25`、`CrouchSpreadScale=0.7`、`AirSpreadScale=0.5` | `CL/Scripts/Module/Combat/CsCombatTuning.cs:60,64,68` | — | BLOCKED（原版未取） |
| 准星展开 / 回收 | **机制不同（原版无"0~1 扩散值"）**：原版准星是"臂长随屏幕宽比例缩放"的绘制（`cl_dlls/client.dll:0x043170-0x043198`），`cl_crosshair_size` 默认 `0`(auto) · `client.dll:0x0e6df8` | `原版资源/cs16src/cstrike/cl_dlls/client.dll`（对照表 U-07 已登记） | 自定 0~1 模型：扩张 `6/s`、回缩 `2.6/s`、移动权重 `0.8`、连射权重 `0.7`、离地 `0.5`、满扩散 12 发、开镜 ×0.15 | `CL/Scripts/Module/Combat/CsCombatTuning.cs:97-103`；应用 `CL/Scripts/Module/Combat/CrosshairState.cs:43` | 不可直比（不是同一机制） | 不一致（机制不同） |
| 穿墙层数 / 护甲穿透阈值 | **BLOCKED**：原版逐武器穿透是 `PrimaryAttack` 里的射线循环，层数上限与护甲穿透立即数未取到 | — | `PenetrationArmorThreshold=0.6`、`MaxPenetrationLayers=2` | `CL/Scripts/Module/Combat/CsCombatTuning.cs:81,85` | — | BLOCKED（原版未取） |

---

## 3. 交互时长

| 项目 | 原版值 | 出处 | 工程值 | 出处（文件:行） | 差值 | 结论 |
|---|---|---|---|---|---|---|
| 切枪（主武器 / 手枪 / 刀） | **BLOCKED**：原版 deploy 时长写在每把武器类的 `Deploy()`（`m_flNextPrimaryAttack = 时间 + k`），本片未逐把反汇编 | — | `SwitchTimePrimary=0.6`、`SwitchTimePistol=0.3`、`SwitchTimeKnife=0.2` | `CL/Scripts/Core/CsConst.cs:210-212`；选用 `CL/Scripts/Module/Match/CsInventory.cs:106-116`；生效 `CsInventory.cs:437` | — | BLOCKED（原版未取） |
| 换弹时长 | **已取到 21 把**（原版值 + 常量 VA 全表在 §5.9）：AK47 **2.45** / M4A1 **3.05** / AWP **2.5** / Scout **2.0** / M249 **4.7** / Elite **4.5** / UMP45 **3.5** / P228·USP·FiveSeven **2.7** … 入口 = 共享函数 `DefaultReload = 0x100983C0`（`push` 前一个 `fld` 即 `fDelay`） | `mp.dll` 22 个调用点，逐个见 §5.9 | 逐武器 `ReloadTime` 2.2~5.7 s（P228 2.2 / M4A1 3.0 / AK47 3.0 / AWP 3.6 / M249 5.7 …） | `CL/Scripts/Core/CsWeapons.cs:154-183` | **≥0.5 s 的有 9 把**：Elite −2.1、AWP +1.1、Scout +1.0、M249 +1.0、UMP45 −0.7、SG550 +0.65、AK47 +0.55、Galil +0.55、MAC10 −0.55、P228/USP/FiveSeven −0.5 | **不一致** |
| 开镜（FOV 过渡 / 稳定） | **BLOCKED**：CS 的开镜 FOV 与计时由 CS 自己的 `cl_dlls/client.dll` 下发，HLSDK 里没有该实现（对照表 A-05 已登记）。**辅助证据**：`mp.dll` 里有 `weapons/zoom.wav` 引用点 VA `0x100E2590`/`0x100D5350`（file `0x0E1990`/`0x0D4750`） | `原版资源/cs16src/cstrike/cl_dlls/client.dll`（对照表 A-05） | `ZoomFov=40`、`ZoomTransitionTau=0.06`、`ScopeSettleTime=0.18` | `CL/Scripts/Core/CsConst.cs:162`、`CL/Scripts/Module/Combat/CsCombatTuning.cs:111,115`；应用 `CL/Scripts/Module/CameraRig/FirstPersonCamera.cs:165,235` | — | BLOCKED（原版未取；`ZoomFov=40` 工程自己标注"无出处"） |
| 投掷（初速 / 引信） | **BLOCKED**：手雷投掷初速与引信的原版值未取到 | — | 初速 `12` m/s、引信 `3.0` s | `CL/Scripts/Core/CsConst.cs:218-219`；应用 `CL/Scripts/Module/Combat/GrenadeThrower.cs:69-70` | — | BLOCKED（原版未取） |
| 刀（挥击时长 / 伤害 / 距离） | **BLOCKED**：未反汇编 `CKnife::Swing` | — | 0.4 s / 55 / 1.6 m | `CL/Scripts/Core/CsConst.cs:213-215` | — | BLOCKED（原版未取） |

---

## 4. 其它直接可比项

| 项目 | 原版值 | 出处 | 工程值 | 出处（文件:行） | 差值 | 结论 |
|---|---|---|---|---|---|---|
| 转身速率（`m_yaw`） | `0.022` 度/鼠标计数（**原版 cvar 默认值**；载体 `client.dll` 未逐字节取证） | 无 `file:offset`（工程注释自陈"原版 cvar 默认值"） | `DegreesPerMouseCount = 0.022` | `CL/Scripts/Module/Combat/CsCombatTuning.cs:38` | **0** | 一致（口径一致） |
| 俯仰角钳制 | **BLOCKED**（原版同一钳制在 GoldSrc 客户端视角侧，载体不在盘） | — | `±89°` | `CL/Scripts/Module/Combat/CsCombatTuning.cs:27`；`CsActor.Pitch` | — | BLOCKED |
| 脚步间隔 | **BLOCKED**：原版脚步节拍在引擎/客户端侧（`mp_footsteps 1` 只开关，`server.cfg:47`） | — | 实测 **3.40 Hz**（`v = SpeedRifle 4.4 m/s`） | `CL/Scripts/Module/Audio/CsAudioTuning.cs:55` | — | BLOCKED（原版未取） |
| 视点晃动（bob） | **机制不同**：原版 bob 是**速度驱动**（`bob = |simvel.xy| * cl_bob`） | `HLSDK/cl_dll/view.cpp:180-203`（对照表 A-08；机制形状） | `ViewBobAmount=0.035`、`ViewBobSpeed=9.5`（工程自定）；表现层另加 `BobBlendSpeed=3.2`、`BobRollDegrees=0.55` | `CL/Scripts/Core/CsConst.cs:165-166`；`CL/Scripts/Module/Combat/CsCombatTuning.cs:143,147` | 不可直比 | 不一致（机制不同） |
| 受击晃动 | **BLOCKED**（原版载体 `client.dll` 未取证） | — | 最大 `2.4°`、时长 `0.28 s`、满伤害参考 `45` | `CL/Scripts/Module/Combat/CsCombatTuning.cs:127,131,135` | — | BLOCKED |
| 视高平滑 / 落地沉降 | **BLOCKED**（原版是位置回放 `ViewInterp`，不是指数平滑 tau） | `HLSDK/cl_dll/view.cpp`（对照表 E-03） | `EyeHeightSmoothTau=0.045`、`LandDipAmount=0.05`、`LandDipTau=0.11` | `CL/Scripts/Module/Combat/CsCombatTuning.cs:120,152,156` | — | BLOCKED |

---

## Y. BLOCKED 逐条：试过什么

1. **`punchangle`（后坐力）** —— ⚠️ **已在第三轮解除（见 §5.5）**：下面这 3 条路当时确实 0 命中，
   第 4 条路（"每把枪的 `fire` 末尾共同调用的那一个函数"）成功，13 把枪的参数已取出。
   失败记录保留在此，供后续片判断哪些路不值得再走：
   - ① 全 `.text` 线性反汇编（361,559 条指令），找"`call` 前连续 ≥4 个 `push imm32` 且立即数是可疑浮点"的站点 ⇒ **0 命中**
     （脚本 `.ai-tmp/test/feel/kick.py`，输出 `out-kick.txt` 只有 `# insns 361559`）⇒ 说明 `KickBack` 的实参不是裸立即数。
   - ② 读既有转储 `.ai-tmp/test/attack2/{m4a1.txt,usp.txt,glock18.txt,famas.txt}` 里所有 `push 0x…` + `f=` 行 ⇒ **0 命中**。
   - ③ M4A1 `PrimaryAttack` 段（`0x100DF27E` 起）确实出现一组可疑浮点 `0.0875 / 1.5 / 3.75 / 3.5 / 3.25 / 2.25 / 3.05`，
     但**无法确证**哪个是 `KickBack(up_base, lateral_base, up_modifier, lateral_modifier, up_max, lateral_max, direction)` 的实参
     ⇒ 按"不许填看起来合理的数"归 BLOCKED（原始转储留在 `.ai-tmp/test/attack2/m4a1.txt` 供后续片接）。
2. **每把枪的射速 / 换弹 / 切枪 / 开镜 / 投掷** —— ⚠️ **换弹已在第四轮解除**（21 把，见 §5.9：入口 = 共享函数 `DefaultReload = 0x100983C0`）。
   **切枪**第四轮已证"不在武器类里"（`Deploy()` 内部无时间常量，见 §5.10）⇒ 仍 BLOCKED；
   **开镜时长**载体在 `cl_dlls/client.dll`（不在本片只读的 `mp.dll` 里）⇒ 仍 BLOCKED；
   **投掷 / 刀**未投入。全部入口见 §6 未决清单。⛔ 不拿 HLSDK 的数冒充。
3. **`GetMaxSpeed` 归属** —— ⚠️ **已在第三轮解除（见 §5.4）**：改用 **`WeaponModel` 槽（61）**
   （它同时引用 `models/p_<枪>.mdl` 与 `models/v_<枪>.mdl`，归属唯一）后，全部逐类确证；
   §1.1 里那套"槽 89 = `Precache` 的字符串"**会被相邻函数污染、实测误判过 4 把**（Scout/MAC10/M4A1/Galil），
   ⛔ 后续片不要再用它。只剩 1 个 260 属**非武器类**（该 vtable 无 `WeaponModel` 槽）。

---

## Z. 复算入口（判据资产）

脚本都在 `.ai-tmp/test/feel/`：

| 脚本 | 作用 |
|---|---|
| `vf.py` | 扫 `.rdata/.data` 里的 vftable 候选（连续 `.text` 指针数组） |
| `spd.py <lo> <hi>` | 找"返回某区间浮点常量"的小函数（本片据此找到 11 个 `GetMaxSpeed`） |
| `mapms.py` | 把取值函数地址 → 所在 vftable → 槽位 |
| `pre.py` | 打印每个 vftable 槽位函数里的字符串（据此确证武器归属） |
| `acc.py` | 扫 `fst dword[reg+0xF8]` 站点，取 `m_flAccuracy` 的 base/div |
| `kick.py` | 扫"`call` 前连续 `push` 浮点立即数"（后坐力实参，本片 0 命中） |
| `wi.py` / `findinfo.py` | 定位 `WeaponInfo` 表（stride `0x28`）并读出 iId / 名字 |
| `lib.py` | **`WeaponInfo` 表全量 dump**（33 条 × 10 字段 + 三个指针解串）—— 直接跑即得 §5.1 |
| `fire.py` | 枚举全部 `weapons/*.wav` 串 → xref → 所在函数 + 该函数里的浮点常量（243 个站点，全量转储 `out-fire.txt`） |
| `fire2.py <音效名列表>` | 每把枪的开火函数 + 浮点常量（**紧凑一行**，§5.2 的出处来源；`out-fire.txt` 为全量） |
| `own.py` | 按"槽 78 = `GetMaxSpeed`、槽 i 地址 = G + 4(i−78)"打印各槽函数的模型串（**已被 §5.4 取代**，仅留作对照） |
| `slots.py class <G>` | 打印某类 vtable **全 95 槽**（函数 VA / 模型串 / 浮点）⇒ 用于定位 `Precache`(0) / `WeaponModel`(61) / `GetMaxSpeed`(78) |
| `slots.py owner <取值函数VA>` | 反查"哪些 vtable 用这个 `GetMaxSpeed`"⇒ **§5.4 权威归属表的产出工具** |
| `kb.py` / `kb2.py` / `kb3.py` / `kb4.py` | 扫 `KickBack` 调用点并解参数（逐版递进；`kb4.py` 多了 `mov reg,imm`+`push reg`）|
| `kb3.py`（推荐） | `python .ai-tmp/test/feel/kb3.py 0x10097f10` ⇒ §5.5 的 13 把枪参数表（转储 `out-kickback.txt`）|
| `wuse.py` | 扫 `.text` 里对 `WeaponInfo` 表区间的引用 ⇒ §5.6 的"只有 3 处、全指向 iId" |
| `verify.py` | **一键复算**：按 VA 打印常量字节 + `GetMaxSpeed` 机器码 + `WeaponInfo` 全表 |

复算命令（工作目录 = 项目根）：

```
python .ai-tmp/test/feel/verify.py
python .ai-tmp/test/feel/lib.py
python .ai-tmp/test/feel/fire2.py "ak47-1.wav,m4a1-1.wav,glock18-1.wav,deagle-1.wav,awp1.wav,mp5-1.wav,famas-1.wav,galil-1.wav,aug-1.wav,sg552-1.wav,sg550-1.wav,g3sg1-1.wav,scout_fire-1.wav,m3-1.wav,xm1014-1.wav,tmp-1.wav,mac10-1.wav,ump45-1.wav,p228-1.wav,fiveseven-1.wav,p90-1.wav,m249-1.wav,elite-1.wav,usp1.wav"
python .ai-tmp/test/feel/own.py
```

期望输出要点（本片实跑得到）：

- `VA 0x10141A2C file 0x14062C  3333b33d  f=0.0875`
- `VA 0x10141A24 file 0x140624  c3f5a83d  f=0.0825`
- `VA 0x10141A4C file 0x14064C  9999193e  f=0.15`
- `VA 0x1014199C file 0x14059C  09f2943b  f=0.00454545`
- `VA 0x10141A8C file 0x14068C  9a99993e  f=0.3`
- `0x100B52F0 file 0x0B46F0  d90500221410c3…`（250）、`0x100B5340 file 0x0B4740  d905e4211410c3…`（221）、
  `0x100E1870 file 0x0E0C70  d905fc211410c3…`（245）、`0x10049860 file 0x048C60  d9050c221410c3…`（260）
- `WeaponInfo` 表位于 `.data` file `0x163CD8`，stride `0x28`，字段
  `+0x00 iId / +0x04 iCost / +0x10 iMaxClip / +0x14 iMaxAmmo / +0x1C pszName`
  （24 把枪 + c4 / knife / hegrenade / smokegrenade / flashbang / shield 共 33 条）

---

## 5. 第二轮补做（`WeaponInfo` 表重核 / `GetMaxSpeed` 归属 / 逐枪射击常数）

### 5.1 `WeaponInfo` 表重核（原版出处已修正）

**表位置（本片实测）**：`.data` file **`0x163CD8`**（VA `0x101650D8`），步长 **`0x28`**，
连续 31 条（24 把枪 + c4 / knife / hegrenade / smokegrenade / flashbang / shield）。
**字段**（逐条按指针解串确证）：

| 偏移 | 字段 | 证据 |
|---|---|---|
| `+0x00` | `iId` | p228=1、glock=2、scout=3、hegrenade=4、xm1014=5、c4=6、mac10=7、aug=8、smokegrenade=9、elite=10、fiveseven=11、ump45=12、sg550=13、galil=14、famas=15、usp=16、glock18=17、awp=18、mp5n=19、m249=20、m3=21、m4a1=22、tmp=23、g3sg1=24、flashbang=25、deagle=26、sg552=27、ak47=28、knife=29、p90=30、shield=99 |
| `+0x04` | `iCost` | 与 `CsWeapons.cs` 价格逐条相等 |
| `+0x08` | `iAmmoPrice`（每份弹药价） | **已解开**（原 BLOCKED）：由 `mp.dll` 的 `CBasePlayer::BuyAmmo`（VA `0x1002a8b0`）读取点确证 —— 见 §5.1 末 |
| `+0x0C` | `iAmmoBox`（每份补充弹数） | 同上（`0x1002a93d` 处作为 `GiveAmmo` 的 `nCount` 推入） |
| `+0x10` | `iMaxClip` | p228=13 / glock=20 / m3=8 / xm1014=7 / awp=10 / deagle=7 / m249=100 … 与工程逐条相等 |
| `+0x14` | `iMaxAmmo` | p228=52 / glock=120 / awp=30 / m3=32 / m249=200 … 与工程逐条相等 |
| `+0x18` | `iAmmoType`（索引） | 1=338Magnum、2=762Nato、3=556NatoBox、4=556Nato、5=buckshot、6=45acp、7=57mm、8=50AE、9=357SIG、10=9mm、11=Flashbang、12=HEGrenade、13=SmokeGrenade、14=C4（与 `+0x24` 串逐条对齐） |
| `+0x1C` | `pszName` | `weapon_ak47` 类名 |
| `+0x20` | `pszAmmoName` | `ammo_762nato` |
| `+0x24` | `pszAmmoDisplay` | `762Nato` |

**与工程比对结论**：24 把枪的 **`iCost` / `iMaxClip` / `iMaxAmmo` 逐条相等（72/72）** ——
即**工程这几列是对的**，只是文档登记的**基址 `0x10f708` / 步长 `0x20` 是错的**（`file 0x10f708`
在本文件里是字符串 `common/npc_step1.wav`）。因此"全文件搜伤害序列 0 命中"**不是**被这个错基址带偏的
（伤害本来就不在这张表里，见 §5.2），而是搜索目标本身不在该表。

**已解开（2026-09-26 追加）：`+0x08` = 每份弹药价、`+0x0C` = 每份补充弹数**

两条**互相独立**的证据链给出同一答案（24 把逐条相等 ⇒ 高置信）：

1. **买弹药通路侧**：`mp.dll` 的 `CBasePlayer::BuyAmmo`（VA `0x1002a8b0`，命令名 `buyammo1`/`buyammo2`
   在串表 VA `0x101105a8`/`0x101105b4`）查该表 —— `0x1002a92d` 用 `+0x08` 与玩家金钱比较（⇒ 价格）、
   `0x1002a93d` 把 `+0x0C` 作为 `GiveAmmo` 的 `nCount` 实参推入（⇒ 每份弹数）、
   `0x1002a8e9` 用 `+0x14` 判"已满不卖"（= `iMaxAmmo`，与上表一致）。
2. **表内容侧**：本节下方的逐条原值，与上述读取点推出的各枪"价格 / 每份弹数"逐条相等。

**反证的处理**：`+0x08` **不是每发伤害**（AK47 = 80 vs `fire` 函数里的实弹伤害 36）—— 这条判定**仍然成立**，
但它不再指向"未知"：80 恰是 AK47 的**每份弹药价**（买 30 发 $80），与"伤害"本就不同量，不该相等。

**逐条原值**（工程落地值即出自此处）：

- `+0x08`（24 把枪）：p228 50 / glock 20 / glock18 20 / scout 80 / xm1014 65 / mac10 25 / aug 60 /
  elite 20 / fiveseven 50 / ump45 25 / sg550 60 / galil 60 / famas 60 / usp 25 / awp 125 / mp5 20 /
  m249 60 / m3 65 / m4a1 60 / tmp 20 / g3sg1 80 / deagle 40 / sg552 60 / ak47 80 / p90 50。
- `+0x0C`：p228 13 / glock 30 / glock18 30 / scout 30 / xm1014 8 / mac10 12 / aug 30 / elite 30 /
  fiveseven 50 / ump45 12 / sg550 30 / galil 30 / famas 30 / usp 12 / awp 10 / mp5 30 / m249 30 /
  m3 8 / m4a1 30 / tmp 30 / g3sg1 30 / deagle 7 / sg552 30 / ak47 30 / p90 50。
  与 `iMaxClip` 相等的那些只是巧合（手枪/步枪的"一份"常正好一个弹匣）；**不等的那些证明它不是"按弹匣补"**：
  `m249` 30 vs `iMaxClip` 100、`scout` 30 vs 10、`xm1014` 8 vs 7、`awp` 10 vs 10（这个相等）—— 原版是按**弹种**定固定份量。

### 5.2 逐枪射击常数（出处 = 各武器类 `fire` 函数，由"该枪独有的开火音效串"圈定）

原版这些量**不在数据表里**，而是写在每把枪的 `PrimaryAttack` / `<X>Fire` 里 —— 本片用
"`weapons/<枪>-1.wav` 串的 xref → 所在函数"把每把枪的开火函数区间圈了出来（脚本
`.ai-tmp/test/feel/fire2.py`）。**因此"伤害/射速取不到"这个旧结论要改**：

| 项目 | 原版值 | 出处（VA / file / 字节） | 工程值 | 出处（文件:行） | 差值 | 结论 |
|---|---|---|---|---|---|---|
| **AK47 每发伤害** | **36** | 常量 VA `0x10142120`（file `0x140D20`）；AK47 `fire` 函数 VA `0x100D3D00`（file `0x0D3100`）内 `fld dword[0x10142120]` = `0x100D3D4A` | `Damage = 36` | `CL/Scripts/Core/CsWeapons.cs:169` | **0** | **一致** |
| **AUG 每发伤害** | **32** | 常量 VA `0x10142108`（file `0x140D08`）；AUG `fire` VA `0x100D4660`（file `0x0D3A60`）内 `0x100D46AA` | `Damage = 32` | `CL/Scripts/Core/CsWeapons.cs:173` | **0** | **一致** |
| **SG552 每发伤害** | **33** | 常量 VA `0x10142110`（file `0x140D10`）；SG552 `fire` VA `0x100E2CB0`（file `0x0E20B0`）内 `0x100E2CFA` | `Damage = 33` | `CL/Scripts/Core/CsWeapons.cs:172` | **0** | **一致** |
| **M249 每发伤害** | **32** | 常量 VA `0x10142108`（file `0x140D08`，同 AUG 共用）；M249 `fire` VA `0x100DD9B0`（file `0x0DCDB0`）内 `0x100DD9F4` 之前的 `fld` | `Damage = 32` | `CL/Scripts/Core/CsWeapons.cs:183` | **0** | **一致** |
| **AUG / SG552 射速** | **`0.0825` s**（≈727 rpm） | 常量 VA `0x10141A24`（file `0x140624` 字节 `c3f5a83d`）；读点 AUG `0x100D493A` 区、SG552 `0x100E2F2E`（file `0x0E232E`） | `Rpm = 666` ⇒ `0.09009` s | `CL/Scripts/Core/CsWeapons.cs:172-173` | **+0.0076 s**（射速 −9%） | 不一致 |
| **AWP 射速** | **`1.45` s**（≈41.4 rpm） | 常量 VA `0x10141D60`（file `0x140960`）；读点 AWP `fire` VA `0x100D5140`（file `0x0D4540`）内 `0x100D52F7`（file `0x0D46F7`） | `Rpm = 41` ⇒ `1.4634` s | `CL/Scripts/Core/CsWeapons.cs:176` | **+0.0134 s**（射速 −0.7%） | 不一致（小） |
| **开镜 FOV（第一档 / 第二档）** | AWP **90 → 40 → 10**；Scout **90 → 40 → 15**；SG550 **90 → 40 → 15**；G3SG1 **90 → 40 → 15**；四种狙击另有一组 `2.4`（同一函数内） | 常量 VA `0x10142184`=**90**（file `0x140D84`）、`0x10142128`=**40**（file `0x140D28`）、`0x10142090`=**10**（file `0x140C90`）、`0x101420B8`=**15**（file `0x140CB8`）、`0x10141F60`=**2.4**（file `0x140B60`）；读点分别在 AWP/Scout/SG550/G3SG1 的 `fire` 函数区间内（如 AWP `0x100D5284`、Scout `0x100E1A20`、SG550 `0x100E23F0`、G3SG1 `0x100D9A30`） | `DefaultFov = 90`、`ZoomFov = 40`（**只有一档**） | `CL/Scripts/Core/CsConst.cs:152,162` | 第一档 **0**（90 / 40 一致）；**工程没有第二档（10 / 15）** | 部分一致（缺第二档开镜） |
| 连发散布 base / div（弱确证：取自"含该枪开火音效串的函数"0x400 B 窗口，**窗口可能溢出到相邻函数**） | AK47 `1/200 + 0.35`、Galil `1/200 + 0.35`、SG552 `1/220 + 0.3`、M4A1 `1/220 + 0.3`、MP5 `1/220 + 0.45`、UMP45 `1/210 + 0.5`、MAC10 `1/200 + 0.6`、TMP `1/200 + 0.55`、P90 `1/175 + 0.45`、M249 `1/175 + 0.4`、Famas `1/215 + 0.3`、AWP `…+0.3`、XM1014 `…+0.25 / 2.25`、USP `…+0.25 / 0.125` | 常量 VA（1/200 `0x101419A8`、1/210 `0x101419A4`、1/215 `0x101419A0`、1/220 `0x1014199C`、1/175 `0x101419AC`、0.3 `0x10141A8C`、0.35 `0x10141AB0`、0.4 `0x10141AC0`、0.45 `0x10141AC8`、0.5 `0x10141AD4`、0.55 `0x10141AD8`、0.6 `0x10141AE4`）；对应读点在各自 `fire` 函数区间内 | 同 §2：线性 `+0.15°/发`、上限 `2.4°`，**无 base/div** | `CL/Scripts/Module/Combat/CsCombatTuning.cs:72,76` | 机制不同（每把枪的 base/div 原版就不同） | 不一致（**base/div 已逐枪取到，归属为弱确证**） |
| 各枪 `m_flNextPrimaryAttack` 的"干火/水下重试"常量 | `0.2` s（几乎每把枪的 `fire` 函数里都有 `0.2`） | 常量 VA `0x10141A60`（file `0x140C60`）；出现在 20+ 把枪的 `fire` 函数里（如 AK47 `0x100D3D36`、M4A1 `0x100DEBxx`、MP5 `0x100DFFDx`） | 未见对应常量（工程无"水下干火"分支） | — | — | 未取到（工程侧无对应量） |

### 5.3 交互时长：这一层的"取不到"已定位到具体位置

- 原版 `Deploy()` / `Reload()` / `Swing()` / 开镜计时**不引用**该枪的开火音效串 ⇒
  本轮的"开火音效 xref 圈类"方法**圈不到它们**（这也是它们仍 BLOCKED 的原因）。
- 但本轮已产出**每把枪开火函数的 VA 区间**（§5.2 的表格，如 AK47 `0x100D3D00`、SG552 `0x100E2CB0`），
  且已确证武器类的 **vftable 结构**（槽 78 = `GetMaxSpeed`、槽 89 = `Precache`、槽 61/64 = `WeaponModel`）。
  ⇒ 下一轮的入口 = **在同一 vftable 里逐个槽试解，定位 `Deploy`/`Reload`**，
  再读它们内部的 `push` 浮点立即数（换弹时长）/ 对 `m_flNextPrimaryAttack` 的写入（切枪时长）。
  本片不做（超范围），保持 BLOCKED。

### 5.4 `GetMaxSpeed` 权威归属表（按 `WeaponModel` 槽 61 逐类确证）

**确证方法**（取代 §1.1 里"槽 89 = Precache 的字符串"那套 —— 那套会被相邻函数污染，实测误判过 4 把）：
武器类 vftable 的 **槽 61 = `WeaponModel`**，它的函数里同时引用**同一把枪**的
`models/p_<枪>.mdl` 与 `models/v_<枪>.mdl` ⇒ 归属唯一。逐类跑法：
`python .ai-tmp/test/feel/slots.py owner 0x<取值函数VA>`。

| 武器 | maxspeed | 取值函数 VA（file / 字节） | 该类 vtable 起点 |
|---|---|---|---|
| **AK47** | **221** | `0x100B5340`（`0x0B4740`，`d905e4211410c3` = `fld [0x101421E4]; ret`） | `0x1013FF54` |
| **M4A1** | **230** | `0x100B5310`（`0x0B4710`，`d905f0211410c3`） | `0x1013E8FC` |
| **M3** | **230** | 同上（共用） | `0x1013EA78` |
| **AUG** / **FAMAS** / **Galil** / **XM1014** | **240** | `0x100B52E0`（`0x0B46E0`，`d905f8211410c3`） | `0x1013FDD8` / `0x1013F664` / `0x1013F06C` / `0x1013D5A8` |
| **P90** | **245** | `0x100E1870`（`0x0E0C70`，`d905fc211410c3`） | `0x1013E30C` |
| **UMP45 / TMP / MP5 / MAC10 / Elite** | **250** | `0x100B52F0`（`0x0B46F0`，`d90500221410c3`） | `0x1013D8A4` / `0x1013DA20` / `0x1013E488` / `0x1013E604` / `0x1013F7E0` |
| **M249** | **220** | `0x100B5320`（`0x0B4720`，`d905e0211410c3`） | `0x1013E780` |
| **AWP / SG550 / G3SG1** | **210** | `0x100D59C0`（`0x0D4DC0`，`8b81a400000083b8ac0500005a7507d905dc211410c3…`：`if ([player+0x5ac]==0x5A) return 210; return 220;`） | `0x1013FC5C` / `0x1013DE94` / `0x1013F1E8` |
| **Scout** | **260 或 220** | `0x100E2220`（`0x0E1620`，同型分支） | `0x1013E010` |
| **SG552** | **235 或 200** | `0x100E34F0`（`0x0E28F0`，同型分支） | `0x1013DD18` |
| （无 `WeaponModel` 槽，非枪） | 260 | `0x10049860`（`0x048C60`，`d9050c221410c3`） | `0x10133218` |
| **手枪系 / 刀** | **250**（未覆盖，继承基类默认） | — | — |

**与工程比对**（工程档位见 §1；1 u/s = 0.0254 m/s）：

| 原版档 | 代表枪 | 原版 m/s | 工程档 | 工程 m/s | 差值 |
|---|---|---|---|---|---|
| 250 | USP/P228/Glock18/Deagle + 5 把冲锋枪 | 6.35 | `SpeedPistol 5.2` | 5.2 | **−1.15（−18%）** |
| 230 / 221 | M4A1 / AK47 / M3 | 5.84 / 5.61 | `SpeedRifle 4.4` | 4.4 | **−1.44 / −1.21（−25% / −22%）** |
| 240 | AUG/FAMAS/Galil/XM1014 | 6.10 | `SpeedRifle 4.4` | 4.4 | **−1.70（−28%）** |
| 210 | AWP/SG550/G3SG1 | 5.33 | `SpeedAWP 3.6` | 3.6 | **−1.73（−33%）** |

⇒ §1 的结论不变且更硬：**工程每一档都偏慢 18%~33%，且工程只有 4 档而原版有 8 个不同值**。

### 5.5 `punchangle`（后坐力）—— 第三轮已取出（推翻 §Y 的 BLOCKED）

**新路子的关键**：先在 AK47 的类代码区（`0x100D3D00..0x100D4400`）逐条读完，
发现每把枪的 `fire` 末尾都调用**同一个函数 `0x10097F10`**，而它就是后坐力入口 ——
它读出 `[ebp+0xc]..[ebp+0x24]`（6 个 float + 1 个 int）后原样转发给 `[[ecx+4]]`/`[ecx+8]`，
`ret 0x20`（= 8 个 dword 实参）⇒ **签名 = (pPlayer, up_base, lateral_base, up_modifier, lateral_modifier, up_max, lateral_max, direction_change)**。
参数顺序由它在 `[esp+0]..[esp+0x14]` 的写入槽位直接给出（`0x10097F63-0x10097F8F` 的反汇编即证据）。

AK47 调用点 `0x100D4321`（file `0x0D3721`）原文（节选）：

```
0x100D42E0  d905681f1410  fld dword ptr [0x10141F68]   ; 2.5
0x100D42E6  6a07          push 7
0x100D42E8  83ec18        sub esp, 0x18
0x100D42EB  d95c2414      fstp dword ptr [esp + 0x14]  ; lateral_max = 2.5
0x100D42EF  d90568201410  fld dword ptr [0x10142068]   ; 6.5  → [esp+0x10] up_max
0x100D42F9  d905081a1410  fld dword ptr [0x10141A08]   ; 0.05 → [esp+0x0C] lateral_modifier
0x100D4303  d905701a1410  fld dword ptr [0x10141A70]   ; 0.225→ [esp+0x08] up_modifier
0x100D430D  d905c81a1410  fld dword ptr [0x10141ac8]   ; 0.45 → [esp+0x04] lateral_base
0x100D4317  d905701d1410  fld dword ptr [0x10141d70]   ; 1.5  → [esp]      up_base
0x100D4320  57            push edi                     ; pPlayer
0x100D4321  e8ea3bfcff    call 0x10097F10
```

**逐把参数**（`(up_base, lateral_base, up_modifier, lateral_modifier, up_max, lateral_max, direction)`；
调线性按"该调用点所在函数的代码邻接"归属 —— 每把枪的 `WeaponModel`/`Precache` 串都在同一段）：

| 武器 | 调用点 VA（file） | 参数 |
|---|---|---|
| AK47 | `0x100D4321`（`0x0D3721`） | `(1.5, 0.45, 0.225, 0.05, 6.5, 2.5, 7)` |
| M4A1 | `0x100DF325`（`0x0DE725`） | `(1, 0.45, 0.28, 0.045, 3.75, 3, 7)` |
| Galil | `0x100DA956`（`0x0D9D56`） | `(1, 0.45, 0.28, 0.045, 3.75, 3, 7)` |
| AUG | `0x100D4D36`（`0x0D4136`） | `(1, 0.45, 0.275, 0.05, 4, 2.5, 7)` |
| FAMAS | `0x100D8323`（`0x0D7723`） | `(1, 0.45, 0.275, ?, 4, 2.5, 7)`（`lateral_modifier` 未取到，该槽用别的指令写） |
| SG552 | `0x100E32F8`（`0x0E26F8`） | `(1, 0.45, 0.28, 0.04, 4.25, 2.5, 7)` |
| MP5 | `0x100E04D6`（`0x0DF8D6`） | `(0.9, 0.475, 0.35, 0.0425, 5, 3, 6)` |
| TMP | `0x100E458D`（`0x0E398D`） | `(1.1, 0.5, 0.35, 0.045, 4.5, 3.5, 6)` |
| UMP45 | `0x100E4E20`（`0x0E4220`） | `(0.125, 0.65, 0.55, 0.0475, 5.5, 4, 10)` |
| MAC10 | `0x100DFC3B`（`0x0DF03B`） | `(1.3, 0.55, 0.4, 0.05, 4.75, 3.75, 5)` |
| P90 | `0x100E1680`（`0x0E0A80`） | `(0.9, 0.45, 0.35, 0.04, 5.25, 3.5, 4)` |
| M249 | `0x100DDFCB`（`0x0DD3CB`） | `(1.8, 0.65, 0.45, 0.125, 5, 3.5, 8)` |

**手枪 / 狙击 / 霰弹的 12 个调用点已在 §5.8 取到**（⚠️ 本节上一版说它们"`(2,0,0,0,0,0)` 明显不成立、不采信" ——
**那个判断是错的**：那 5 个 0 是编译器用 `fldz` + `fst` 复用同一个 0.0 写的，`[esp]` 取的是残留 `st(1)`。更正见 §5.8）。
当时的调用点登记（保留，供对账）：`0x100D5818`(AWP)、`0x100D6EC0`(Deagle)、`0x100D7937`(Elite)、
`0x100D8BEA`(FiveSeven)、`0x100DA0D2`(G3SG1)、`0x100DB54B`(Glock18)、`0x100DE806`(M3)、
`0x100E0E3A`(P228)、`0x100E2073`(Scout)、`0x100E2A88`(SG550)、`0x100E596D`(USP)、`0x100E61C1`(XM1014)。

### 5.6 `WeaponInfo` 的 `+0x08` / `+0x0C` 语义 —— **已解除**（2026-09-26）

**结论**：`+0x08` = **每份弹药价**、`+0x0C` = **每份补充弹数**（推导与逐条原值见 §5.1 末）。

**为什么上一轮判 BLOCKED、这一轮怎么解开的**：上一轮走的是"读该表的**访问形态**"这条路（全 `.text` 只有
3 处引用且全指向 `iId`，运行时按 `基址 + i*0x28` 索引 ⇒ 没有字段级常量引用 ⇒ 反推不通）。这一轮**换了判据**：
不去看"谁索引这张表"，而是去 `mp.dll` 里找**语义已知的使用者** —— `CBasePlayer::BuyAmmo` 拿它做"比价 / 补弹"，
字段语义由**使用者行为**确定，不需要字段级引用。

- 全 `.text` 里**只有 3 处**引用该表：`0x1002D24F`、`0x1002F982`、`0x100B552D`，
  且**三处全部指向同一个地址 `0x10165128`**（= 数组里 p228 那条的 `+0x00`，即 `iId` 字段），
  ⇒ 使用者是运行时按 `基址 + i*0x28` 索引的，**没有任何"常量折叠式"的直接字段引用**
  ⇒ 无法从访问形态反推 `+0x08` / `+0x0C` 的语义。
- 已有的**反证**仍然成立：`+0x08` **不是每发伤害**（AK47 = 80，而同类 `fire` 函数里的实弹伤害立即数 = 36）。
- ⇒ 保留 BLOCKED，24 把的逐条原值已登记在 §5.1（不重复）。

### 5.7 交互时长（切枪 / 换弹 / 开镜时长 / 投掷 / 刀）—— 仍 BLOCKED，位置已收敛

- 本轮试了第四条路：**按 vftable 逐槽**。AK47 的 vtable（起点 `0x1013FF54`）全 95 槽都打印过
  （`python .ai-tmp/test/feel/slots.py class 0x1014008C`），槽 0 = `Precache`（`weapons/ak47-1.wav` 等）、
  槽 61/64 = `WeaponModel`（`models/p_ak47.mdl` + `models/v_ak47.mdl`）、槽 78 = `GetMaxSpeed`（`fld [0x101421E4]; ret`）。
  **但 `Deploy()` / `Reload()` 没有可识别的模型串或唯一串**（它们调 `DefaultDeploy`/`DefaultReload` 并把
  `models/v_*.mdl` 当实参传进去 ⇒ 串出现在**被调函数**里，不在自己的槽函数里）⇒ 逐槽法看不到它们。
- 下一轮的确定入口（本片不越界）：`DefaultDeploy` / `DefaultReload` 是**共享函数**，
  先按 `weapons/*_clipout.wav`（已全量枚举在 `out-fire.txt`）或 `models/v_*.mdl` **从调用点反查**，
  再读它们的 `push` 浮点立即数（`fDelay`）。逐条原始数据：
  `python .ai-tmp/test/feel/fire.py`（243 个站点全量转储 `out-fire.txt`）。
- ⇒ 维持 BLOCKED，且已写明"取不到的是哪一层"（不是没找）。

### 5.8 `punchangle` 补全（手枪 / 狙击 / 霰弹）—— 并更正 §5.5 末尾的"不采信"判断

⚠️ **本片第四次自我更正**：第三轮把 `(2, 0, 0, 0, 0, 0)` 判为"6 个修正量不可能全 0 ⇒ 不可信"，
**这个判断是错的**。逐步反汇编后真相是：这些站点用
```
fldz                      ; 压入一个 0.0
fst  [esp+0x14] ... [esp+0x08]   ; 4 次不弹出，把 0.0 写进 4 个槽
fstp [esp+0x04]                  ; 第 5 个槽
fstp [esp]                       ; 弹出的是上一条 fld 残留的 st(1) = 真正的 up_base
```
（Deagle 站点 `0x100D6E9D-0x100D6EBC` 的原文即此；AWP `0x100D57FB-0x100D5814`、Scout `0x100E2050-0x100E206F` 同型。）
⇒ **手枪/狙击/霰弹只给 `up_base`，其余 6 个一律 0，`direction_change = 0`** —— 这是原版真实行为，不是解析错误。

| 武器 | 参数 `(up_base, lateral_base, up_modifier, lateral_modifier, up_max, lateral_max, direction)` | 调用点 VA（file） | 归属证据 |
|---|---|---|---|
| **Deagle** | `(2, 0, 0, 0, 0, 0, 0)` | `0x100D6EC0`（`0x0D62C0`） | 站点上下文完整反汇编（`fldz`×5 槽 + `fld 2`） |
| **AWP** | `(2, 0, 0, 0, 0, 0, 0)` | `0x100D5818`（`0x0D4C18`） | 同上（`0x100D57CE fld 2` → 残留 st(1)） |
| **Scout** | `(2, 0, 0, 0, 0, 0, 0)` | `0x100E2073`（`0x0E1473`） | 同上 |
| **USP** | `(2, 0, 0, 0, 0, 0, 0)` | `0x100E596D`（`0x0E4D6D`） | 代码邻接标称 |
| **P228** | `(2, 0, 0, 0, 0, 0, 0)` | `0x100E0E3A`（`0x0E023A`） | 代码邻接标称 |
| **FiveSeven** | `(2, 0, 0, 0, 0, 0, 0)` | `0x100D8BEA`（`0x0D7FEA`） | 代码邻接标称 |
| **Elite** | `(2, 0, 0, 0, 0, 0, 0)` | `0x100D7937`（`0x0D6D37`） | 代码邻接标称（与 Elite 换弹站点 `0x100D79CE` 相邻） |
| **XM1014** | `(0.75, 0, 0, 0, 0, 0, 0)` | `0x100E61C1`（`0x0E55C1`） | 代码邻接标称 |
| **M3** | `(2.5, 0, 0, 0, 0, 0, 0)` | `0x100DE806`（`0x0DDC06`） | 代码邻接标称 |
| **G3SG1** | `(0.25, ?, 0, 0, 0, 0, 0)` | `0x100DA0D2`（`0x0D94D2`） | 代码邻接标称（第 2 槽未取出） |
| **SG550** | `(0.25, ?, 0, 0, 0, 0, 0)` | `0x100E2A88`（`0x0E1E88`） | 代码邻接标称（第 2 槽未取出） |
| **Glock18** | **BLOCKED**：本期两次解析都得到全 0（`(0,0,0,0,0,0,0)`），与"Glock 有后坐力"直觉矛盾 ⇒ 不填 | `0x100DB54B`（`0x0DA94B`） | — |

工具：`python .ai-tmp/test/feel/kb5.py 0x10097f10`（**x87 栈模型**，比 `kb3.py`/`kb4.py` 精确 —— 它们把 `fstp` 当成"上一个 `fld` 的值"，会在 `fldz` 之后取错）。

### 5.9 换弹时长（已取到 21 把）—— 新入口 = 共享函数 `DefaultReload = 0x100983C0`

**怎么找到的**：不按"音效串"圈类，而是**按参数形态找共享被调函数** ——
扫全部 `call` 站点，挑"恰好 1 个浮点实参 + 2~4 个整型实参"的站点并按 target 分组 ⇒
`0x100983C0` 有 **22 个站点**（21 把枪 + 1 个引擎内部点），每个站点前面都是同一形态：

```
0x100D447C  fld dword ptr [0x10141F64]   ; = 2.45   ← 换弹时长（fDelay 实参）
0x100D4482  fstp dword ptr [esp]
0x100D4485  push 1                       ; iAnim
0x100D4487  push dword ptr [eax + 0x4824]
0x100D448D  push esi
0x100D448E  call 0x100983C0              ; ← DefaultReload(iClipSize, iAnim, fDelay, iBody)
```

| 武器 | 原版换弹（秒） | 站点 VA（file） | 常量 VA（file） | 工程 `ReloadTime`（`CsWeapons.cs`） | 差值 |
|---|---|---|---|---|---|
| **AK47**（逐步确证） | **2.45** | `0x100D448E`（`0x0D388E`） | `0x10141F64`（`0x140964`） | 3.0（`:169`） | **+0.55** |
| **M249**（逐步确证） | **4.7** | `0x100DE14E`（`0x0DD54E`） | `0x1014204C`（`0x140A4C`） | 5.7（`:183`） | **+1.0** |
| **AUG** | 3.3 | `0x100D4EB2`（`0x0D42B2`） | — | 3.0（`:173`） | −0.3 |
| **AWP** | 2.5 | `0x100D58B2`（`0x0D4CB2`） | — | 3.6（`:176`） | **+1.1** |
| **Deagle** | 2.2 | `0x100D6F8E`（`0x0D638E`） | — | 2.4（`:157`） | +0.2 |
| **Elite** | 4.5 | `0x100D79CE`（`0x0D6DCE`） | — | 2.4（`:159`） | **−2.1** |
| **FiveSeven** | 2.7 | `0x100D8CBE`（`0x0D80BE`） | — | 2.2（`:158`） | −0.5 |
| **G3SG1** | 3.5 | `0x100DA172`（`0x0D9572`） | — | 4.0（`:177`） | +0.5 |
| **Galil** | 2.45 | `0x100DAACE`（`0x0D9ECE`） | — | 3.0（`:167`） | **+0.55** |
| **Glock18** | 2.2 | `0x100DB647`（`0x0DAA47`） | — | 2.2（`:154`） | **0** |
| **M4A1** | 3.05 | `0x100DF4A5`（`0x0DE8A5`） | — | 3.0（`:171`） | −0.05 |
| **MAC10** | 3.15 | `0x100DFDCE`（`0x0DF1CE`） | — | 2.6（`:163`） | **−0.55** |
| **MP5** | 2.63 | `0x100E064E`（`0x0DFA4E`） | — | 2.6（`:161`） | −0.03 |
| **P228** | 2.7 | `0x100E0F1C`（`0x0E031C`） | — | 2.2（`:156`） | **−0.5** |
| **P90** | 3.4 | `0x100E180E`（`0x0E0C0E`） | — | 3.3（`:165`） | −0.1 |
| **Scout** | 2.0 | `0x100E2112`（`0x0E1512`） | — | 3.0（`:175`） | **+1.0** |
| **SG550** | 3.35 | `0x100E2B22`（`0x0E1F22`） | — | 4.0（`:178`） | **+0.65** |
| **SG552** | 3.0 | `0x100E3472`（`0x0E2872`） | — | 3.0（`:172`） | **0** |
| **TMP** | 2.12 | `0x100E471E`（`0x0E3B1E`） | — | 2.4（`:162`） | +0.28 |
| **UMP45** | 3.5 | `0x100E4F9E`（`0x0E439E`） | — | 2.8（`:164`） | **−0.7** |
| **USP** | 2.7 | `0x100E5A64`（`0x0E4E64`） | — | 2.2（`:155`） | **−0.5** |

**归属方法**：`python .ai-tmp/test/feel/rel2.py` —— 从站点**向前**扫描最近的
`models/v_<枪>.mdl` / `weapons/*.wav` 引用（类代码块内必有自己那把枪的模型串），
距离 0x450~0x700（各站点的"距站点"列已打印）⇒ 稳定。
**未取到**：`M3` / `XM1014` 的换弹站点未被筛出（参数形态不同），维持 BLOCKED。
**Scout/SG550/SG552 三连**的归属按块序推断（三者的 `models/v_*.mdl` 被 `weapons/zoom.wav` 挡在前面），标注为"推断"。

### 5.10 切枪时长 / 开镜时长 —— BLOCKED（已证明"不在武器类里"）

- **切枪**：已按 §5.7 的入口做完 —— 用 `models/v_<枪>.mdl` + `models/p_<枪>.mdl` 的引用找到
  **`Deploy()` 本体**（AK47 `0x100D3EB0` file `0x0D32B0`、M4A1 `0x100DECE0` file `0x0DE0E0`：
  函数内同时出现 `models/p_<枪>.mdl`、`models/v_<枪>.mdl` 与 animExt 串 `"ak47"` / `"rifle"`，
  即三个 `DefaultDeploy` 实参）。**但它内部没有任何时间常量**
  （全部浮点只有共同栈 cookie `-0.00294341` 与干火常量 `0.2`）
  ⇒ **结论：CS 1.6 的切枪延时不在武器类里**，来自共享路径（`SelectItem` / `ItemPostFrame` 对
  `m_flNextAttack` 的写入）。
- **开镜时长**：CS 的开镜 FOV 与计时在 CS 自己的 `cl_dlls/client.dll`，`mp.dll` 里只有 FOV 数值
  （已取到，见 §5.2：AWP `90/40/10`、Scout·SG550·G3SG1 `90/40/15`）⇒ 时长维持 BLOCKED（载体在另一文件）。
- **投掷 / 刀**：本轮未再投入（超"只攻两块"的范围）。刀只取到 `KickBack` 无（刀不走 kickback），
  挥击时长 / 伤害仍未取到。

---

## 6. 未决清单（冻结时点：本片第四轮结束）

> 本文件自本轮结束**冻结**。下列各项是"接力入口已备好、值未取到"的，接片请按"入口"一列继续，
> ⛔ 不要从头重做，也⛔ 不要把本表里标注"弱确证 / 推断 / 代码邻接"的归属当成已确证事实去改代码。

| # | 未决项 | 已排除的路（别再走） | 接力入口（下一步做什么） |
|---|---|---|---|
| U-1 | `Glock18` 的 `punchangle` | 两次解析（`kb3`/`kb4`/`kb5`）都得到全 0，与直觉矛盾 | 逐步反汇编 `0x100DB54B` 前 0xA0 字节，看它是不是走了与手枪不同的分支（Glock 有连发模式） |
| U-2 | `G3SG1` / `SG550` 的 `punchangle` 第 2 槽（`lateral_base`） | 窗口内没出现该槽的写入指令 | 把 `kb5.py` 的窗口从 `0x180` 加到 `0x300` 再跑 `0x100DA0D2` / `0x100E2A88` |
| U-3 | 手枪/狙击/霰弹 `punchangle` 的**归属** | 已用"最近前置模型串"法给 `rel2.py`（换弹）做过，但**没对这批 12 个 kickback 站点做** | 把 `rel2.py` 的 `SITES` 换成 §5.8 的 12 个 kickback 站点地址重跑（一条命令） |
| U-4 | **切枪时长** | 武器类 `Deploy()` 内部无时间常量（已证），逐槽法也看不到（§5.7） | 在 `mp.dll` 里找 `CBasePlayer::SelectItem` / `ItemPostFrame`（特征：引用 `weapon_*` 类名串做类查找 + 写 `m_flNextAttack`），读它对 `m_flNextAttack` 的常数 |
| U-5 | **开镜时长** | 不在 `mp.dll`（已证：`mp.dll` 只有 FOV 数值） | 载体是 `cl_dlls/client.dll`，需换工具/换文件做（本片全程只读 `mp.dll`） |
| U-6 | `M3` / `XM1014` 的换弹时长 | 参数形态不符，未被 `rel.py` 筛出 | 直接看这两个类的代码块（`models/v_m3.mdl` / `models/v_xm1014.mdl` 的引用点）附近有无 `call 0x100983C0`，或它们的换弹是否走别的函数 |
| U-7 | 投掷（初速/引信）、刀（挥击时长/伤害/距离） | 本片未投入 | 同类做法：先按参数形态找共享函数，再逐类读 |
| ~~U-8~~ | ~~`WeaponInfo` 的 `+0x08` / `+0x0C` 语义~~ | **已解除（2026-09-26）**：`+0x08` = 每份弹药价、`+0x0C` = 每份补充弹数 —— 由 `mp.dll` 的 `CBasePlayer::BuyAmmo` 读取点（`0x1002a92d` 比价 / `0x1002a93d` 作 `GiveAmmo` 的 `nCount`）确证，与 §5.1 的 24 条原值逐条相等 | 无需接力（工程已按此落值，见 `client/Assets/Scripts/Core/CsAmmoBuy.cs`） |
| U-9 | 1 个 `260` 的 `GetMaxSpeed`（`0x10049860`） | 所在 vtable 无 `WeaponModel` 槽 ⇒ 非武器类 | 用 `slots.py class 0x10133218` 看同类槽位里有什么串（可能是 shield / VIP / 观察者） |
| U-10 | 手枪/狙击/霰弹的**散布** base/div（§2 那批"值已取到、归属未逐把确证"） | — | 用 `rel2.py` 的"最近前置模型串"法给 `acc.py` 的站点重新归属（同 U-3 的做法） |

**可复算入口汇总**（工作目录 = 项目根）：

```
python .ai-tmp/test/feel/verify.py            # 常量字节 + GetMaxSpeed 机器码 + WeaponInfo 全表
python .ai-tmp/test/feel/lib.py               # WeaponInfo 全量 dump（33 条 × 10 字段）
python .ai-tmp/test/feel/kb5.py 0x10097f10    # punchangle 全 26 站点（x87 栈模型，推荐）
python .ai-tmp/test/feel/kb3.py 0x100983C0    # 换弹 22 站点（slots[0] = fDelay）
python .ai-tmp/test/feel/rel.py               # 按"参数形态"找共享函数（换弹就是这样找到的）
python .ai-tmp/test/feel/rel2.py              # 站点 → 最近前置武器模型串（归属判定）
python .ai-tmp/test/feel/slots.py class 0x1014008C   # 某类 vtable 全 95 槽
python .ai-tmp/test/feel/slots.py owner 0x100B5340   # GetMaxSpeed 反查归属
python .ai-tmp/test/feel/dep.py ak47          # 找 Deploy() 本体（含 DefaultDeploy 三实参）
python .ai-tmp/test/feel/wuse.py              # WeaponInfo 表的 .text 引用点
```

---

## 附：顺带发现（出处不一致，供主 agent 判断，本片未改任何文档）

`策划/对照表.md` §1.1、§BLOCKED-2 与 `Core/CsWeapons.cs` 类注释里登记的 `mp.dll` 偏移
（`WeaponInfo[]` 基址 `mp.dll:0x10f708`、步长 `0x20`）**在盘上这份 `mp.dll` 上取不到**：
`file 0x10f708` 处是字符串 `common/npc_step1.wav`（`.rdata`），不是数组。

盘上这份 `mp.dll` 的 `WeaponInfo` 表实测在 **`.data` file `0x163CD8`、步长 `0x28`**，
字段偏移与文档一致（`iCost=+0x04` / `iMaxClip=+0x10` / `iMaxAmmo=+0x14` / `pszName=+0x1C`），
且 `+0x00` 是 **`iId`**（p228=1、glock=2、scout=3、hegrenade=4、xm1014=5、c4=6、mac10=7、aug=8、
smokegrenade=9、elite=10、fiveseven=11、ump45=12、sg550=13、galil=14、famas=15、usp=16、glock18=17、
awp=18、mp5n=19、m249=20、m3=21、m4a1=22、tmp=23、g3sg1=24、flashbang=25、deagle=26、sg552=27、
ak47=28、knife=29、p90=30、shield=99）。

⇒ 结论：**要么文档的偏移取自另一份 `mp.dll`，要么 `0x10f708` 这个数本身就是错的**。
**已按正确基址重核**（见 §5.1）：24 把枪的 `iCost` / `iMaxClip` / `iMaxAmmo` **72/72 逐条相等** ⇒
工程这几列本来就对；而"全文件搜伤害序列 0 命中"**不是**被错基址带偏 ——
伤害本来就不在该表里，而在各武器类的 `fire` 函数（已取到，见 §5.2）。
逐条原始数据见 `python .ai-tmp/test/feel/lib.py`（表全量）与 `python .ai-tmp/test/feel/verify.py`（常量字节）。
